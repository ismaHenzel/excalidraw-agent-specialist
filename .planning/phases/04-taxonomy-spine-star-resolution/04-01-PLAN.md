---
phase: 04-taxonomy-spine-star-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/diagram-types/tech-architecture.md
  - .claude/agents/excalidraw/kb/README.md
  - .claude/agents/excalidraw/kb/group-container.md
  - .claude/agents/excalidraw/kb/icon-block.md
  - .claude/agents/excalidraw/kb/multi-zoom-overview.md
  - .claude/agents/excalidraw/kb/fan-out.md
  - .claude/agents/excalidraw/kb/convergence.md
  - .claude/agents/excalidraw/kb/linear-pipeline.md
  - .claude/agents/excalidraw/examples_excalidraw/star_schema.excalidraw
autonomous: true
requirements: [DTKB-01, DTKB-02, DTKB-03, EX-02]

must_haves:
  truths:
    - "A diagram-types/ directory exists as a SIBLING of kb/ (not nested inside it)"
    - "diagram-types/README.md holds a single authoritative resolver table mapping each type to its recipe file, composed kb/ sub-patterns, and canonical example PNG"
    - "diagram-types/tech-architecture.md exists as the smoke-test type recipe, composing kb/ primitives by reference (no re-derived geometry)"
    - "The two KB layers cross-reference each other: type files link down to primitives; kb/README.md documents the two-layer relationship"
    - "The legacy star_schema.excalidraw is explicitly resolved (re-authored OR grandfathered with a documented reason recorded in diagram-types/README.md) before any new canonical example is authored"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/README.md"
      provides: "Authoritative family->type resolver table + legacy-example resolution record"
      contains: "| Family | Type |"
    - path: ".claude/agents/excalidraw/diagram-types/tech-architecture.md"
      provides: "Smoke-test type recipe composing existing kb/ primitives"
      min_lines: 20
    - path: ".claude/agents/excalidraw/kb/README.md"
      provides: "Two-layer (primitives vs types) relationship note linking to ../diagram-types/"
      contains: "diagram-types"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/tech-architecture.md"
      to: ".claude/agents/excalidraw/kb/group-container.md"
      via: "@../kb/ composition reference"
      pattern: "kb/group-container"
    - from: ".claude/agents/excalidraw/kb/README.md"
      to: ".claude/agents/excalidraw/diagram-types/README.md"
      via: "layers note link"
      pattern: "diagram-types"
---

<objective>
Create the diagram-types/ KB layer (the TYPE layer) as a sibling of kb/, with a single authoritative resolver table in diagram-types/README.md, the first type recipe (tech-architecture.md, used as the Phase-4 smoke-test type), bidirectional cross-references between the two KB layers, and the one-time resolution of the legacy star_schema.excalidraw example.

Purpose: This is the structural spine the rest of milestone v1.1 builds on. Per ARCHITECTURE.md, diagram-types/ is the composition layer that @-references the existing kb/ primitive layer; the resolver table is the single source of truth shared by the command and specialist so the mapping cannot drift (anti-pattern: duplicating the resolver in two files). EX-02 (resolving star_schema.excalidraw) is a one-time gate that MUST land before any new canonical example is authored in later phases.

Output: A new diagram-types/ directory, its README.md resolver table (DTKB-01) with the EX-02 resolution recorded, tech-architecture.md (DTKB-02), the two-layer cross-references (DTKB-03), and a resolved star_schema.excalidraw (EX-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.claude/agents/excalidraw/kb/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold diagram-types/ + authoritative resolver table + two-layer cross-references</name>
  <read_first>
    - .claude/agents/excalidraw/kb/README.md (the file being modified; mirror its index shape and "Adding a new pattern" procedure)
    - .planning/research/ARCHITECTURE.md (the "Recommended Project Structure", the "Resolver table" under Data Flow, and "Pattern 1/2/3" cross-reference shapes — copy the column set from the Data-Flow resolver table)
    - .planning/REQUIREMENTS.md (DTKB-01, DTKB-02, DTKB-03 exact wording)
  </read_first>
  <action>
    Create the directory .claude/agents/excalidraw/diagram-types/ as a SIBLING of kb/ (NOT nested under kb/ — Anti-Pattern 1 in ARCHITECTURE.md).

    Create .claude/agents/excalidraw/diagram-types/README.md as the authoritative TYPE-layer index. It MUST contain (per DTKB-01, DTKB-03):
    - A short "Two-layer KB" note distinguishing the TYPE layer (this dir, one file per diagram type) from the PRIMITIVE layer (../kb/, one file per layout sub-pattern), stating that type files compose primitives by @-reference and never re-derive geometry, and linking to ../kb/README.md.
    - A single markdown resolver table titled so it is greppable, with EXACTLY these columns: Family, Type, Type file, Composes (kb sub-patterns), Example PNG. Seed it from the resolver table in ARCHITECTURE.md "Data Flow". For Phase 4, the only fully-wired row is the smoke-test type tech-architecture (Family "Tech Architecture", Type file tech-architecture.md, Composes group-container, icon-block, multi-zoom-overview, fan-out, convergence, linear-pipeline, Example architecture_overview.png reuse). Subsequent-phase rows (sequence, class, use-case, activity, star-schema, snowflake-schema, er, data-vault) MAY be listed as planned placeholders, but mark any not-yet-authored recipe/example clearly so the smoke-test row is the only one claimed wired this phase.
    - A "Legacy example resolution" section anchor (heading "## Legacy example resolution") — Task 3 records the star_schema.excalidraw decision here; create the heading now with a one-line "decision recorded by EX-02 task" placeholder so the anchor exists.
    - An "Adding a diagram type" procedure mirroring kb/README.md's "Adding a new pattern" (create file, add example pair, add ONE resolver-table row under the right family).

    Edit .claude/agents/excalidraw/kb/README.md (DTKB-03): add a short "## Layers" note stating primitives live here and per-type recipes live in ../diagram-types/, linking to ../diagram-types/README.md, so the two layers cannot silently drift.

    Add the upward back-reference line "> Used by types: tech-architecture" (DTKB-03, Pattern 2) to each kb/ primitive the smoke-test type composes: group-container.md, icon-block.md, multi-zoom-overview.md, fan-out.md, convergence.md, linear-pipeline.md. These are the only kb files touched this phase (the smoke-test type's primitives), per phase_intent.

    Do NOT touch scripts/, the validator, the verifier, or the render pipeline.
  </action>
  <acceptance_criteria>
    - Directory .claude/agents/excalidraw/diagram-types/ exists and .claude/agents/excalidraw/kb/diagram-types/ does NOT exist (sibling, not nested).
    - diagram-types/README.md contains a markdown table whose header row includes the literal strings "| Family | Type |" and "Composes (kb sub-patterns)".
    - diagram-types/README.md contains a heading matching "Legacy example resolution".
    - kb/README.md contains the string "diagram-types" and a link to ../diagram-types/README.md.
    - Each of group-container.md, icon-block.md, multi-zoom-overview.md, fan-out.md, convergence.md, linear-pipeline.md contains the line "Used by types: tech-architecture".
  </acceptance_criteria>
  <verify>
    <automated>test -d .claude/agents/excalidraw/diagram-types && test -f .claude/agents/excalidraw/diagram-types/README.md && grep -q "| Family | Type |" .claude/agents/excalidraw/diagram-types/README.md && grep -q "Composes (kb sub-patterns)" .claude/agents/excalidraw/diagram-types/README.md && grep -q "tech-architecture" .claude/agents/excalidraw/diagram-types/README.md && grep -qi "Legacy example resolution" .claude/agents/excalidraw/diagram-types/README.md && grep -q "diagram-types" .claude/agents/excalidraw/kb/README.md && for f in group-container icon-block multi-zoom-overview fan-out convergence linear-pipeline; do grep -q "Used by types: tech-architecture" .claude/agents/excalidraw/kb/$f.md || exit 1; done && ! test -d .claude/agents/excalidraw/kb/diagram-types</automated>
  </verify>
  <done>diagram-types/ is a sibling of kb/; README.md has the 5-column resolver table with a wired tech-architecture row and a "Legacy example resolution" anchor; kb/README.md links to ../diagram-types/; all six composed kb primitives carry a "Used by types: tech-architecture" back-ref; no kb/diagram-types/ nesting exists.</done>
</task>

<task type="auto">
  <name>Task 2: Author tech-architecture.md smoke-test type recipe</name>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/README.md (the resolver table row this recipe must match exactly — created in Task 1)
    - .planning/research/ARCHITECTURE.md (Pattern 1 "TYPE file @-references PRIMITIVE files" — the exact Composes/Ground-truth section shape; Anti-Pattern 5 "Re-deriving geometry inside type files")
    - .claude/agents/excalidraw/kb/README.md (the primitive index — confirm the exact filenames the recipe references)
    - .claude/agents/excalidraw/kb/group-container.md (one example primitive to confirm the @-reference target shape)
  </read_first>
  <action>
    Create .claude/agents/excalidraw/diagram-types/tech-architecture.md as the smoke-test type recipe (DTKB-02). It MUST state, in this order, four labelled sections:
    - Purpose — what a Tech Architecture diagram is for / when to use it (technologies, services, clouds and their relationships), reframing the existing architecture/overview material.
    - How to draw it — the required structural elements + conventions, expressed as PROSE that DEFERS to the composed primitives. Do NOT paste coordinate math or JSON skeletons (Anti-Pattern 5) — name the structural choices and point at the primitive files for geometry.
    - Composes (primitive layer) — a bullet list of @../kb/<pattern>.md links for exactly the primitives named in the resolver row: group-container.md, icon-block.md, multi-zoom-overview.md, fan-out.md, convergence.md, linear-pipeline.md. Each bullet names WHY that primitive is composed (one phrase).
    - Ground truth — a link to ../examples/architecture_overview.png (reused, no new example this phase).

    The recipe must be discoverable from and consistent with the resolver-table row created in Task 1 (same composed-primitive list, same example PNG). Do NOT touch scripts/, the validator, the verifier, or the render pipeline.
  </action>
  <acceptance_criteria>
    - tech-architecture.md exists and contains headings matching "Purpose" and "Composes".
    - tech-architecture.md contains the strings "kb/group-container.md" and "kb/multi-zoom-overview.md" (down-references to the primitive layer).
    - tech-architecture.md contains "architecture_overview.png".
    - tech-architecture.md contains NO inlined element JSON (no occurrence of the literal '"type": "rectangle"').
    - tech-architecture.md has at least 20 non-blank lines.
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/tech-architecture.md && grep -qi "Purpose" .claude/agents/excalidraw/diagram-types/tech-architecture.md && grep -qi "Composes" .claude/agents/excalidraw/diagram-types/tech-architecture.md && grep -q "kb/group-container.md" .claude/agents/excalidraw/diagram-types/tech-architecture.md && grep -q "kb/multi-zoom-overview.md" .claude/agents/excalidraw/diagram-types/tech-architecture.md && grep -q "architecture_overview.png" .claude/agents/excalidraw/diagram-types/tech-architecture.md && ! grep -q '"type": "rectangle"' .claude/agents/excalidraw/diagram-types/tech-architecture.md && [ "$(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/diagram-types/tech-architecture.md)" -ge 20 ]</automated>
  </verify>
  <done>tech-architecture.md exists with Purpose / How to draw it / Composes / Ground truth sections, @-references all six composed kb primitives plus architecture_overview.png, contains no inlined element JSON, and matches the resolver-table row from Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the legacy star_schema.excalidraw (EX-02) and record the decision</name>
  <read_first>
    - .claude/agents/excalidraw/examples_excalidraw/star_schema.excalidraw (the file under decision — confirm it has 0 groupIds, 0 containerId, 0 boundElements, fully-unbound arrows, roundness {type:2}/{type:3})
    - .planning/research/PITFALLS.md (the "Load-bearing finding" header + Pitfall 1 + Technical Debt Patterns rows on groupIds/multi-line text — the exact non-compliance the recipe forbids)
    - .planning/research/SUMMARY.md ("CRITICAL OPEN DECISION" — Option A re-author vs Option B grandfather, and the consequence of each)
    - .claude/agents/excalidraw/diagram-types/README.md (the "Legacy example resolution" anchor created in Task 1 — the decision is recorded here)
    - .planning/REQUIREMENTS.md (EX-02 exact wording)
  </read_first>
  <action>
    Resolve EX-02 with an explicit, recorded decision and a corresponding action. The committed default for this plan is OPTION B — GRANDFATHER: do NOT mutate the geometry of the legacy star_schema.excalidraw in this phase; instead label it as a pre-v1.1 legacy example and record the grandfather reason, because (a) the new compliant star-schema example is authored in Phase 6 against the compartmented-box recipe, and (b) star is NOT the Phase-4 smoke-test type (Tech Architecture is), so re-authoring it now is out of this phase's critical path. If the executor finds a compelling reason to re-author instead (Option A), it MAY do so — but it MUST then bring the file to the grouped/bound/sharp convention (groupIds on every box unit, sharp roundness:null on formal boxes and connectors, bound titles) and record that it chose Option A. Exactly one option must be chosen and recorded; the decision MUST NOT be left implicit.

    Record the decision in the "## Legacy example resolution" section of diagram-types/README.md (replacing the Task-1 placeholder): state which option was taken, the reason, the file paths it applies to (examples_excalidraw/star_schema.excalidraw + examples/example_star_schema.png), and — for Option B — an explicit note that the canonical/compliant star-schema example is deferred to Phase 6 and that the legacy file is NOT a safe template for the new grouped/bound/sharp recipe.

    Do NOT touch scripts/, the validator, the verifier, or the render pipeline. This task is documentation + (only under Option A) source geometry — never pipeline changes.
  </action>
  <acceptance_criteria>
    - diagram-types/README.md "Legacy example resolution" section names star_schema.excalidraw and example_star_schema.png.
    - The section contains exactly one committed disposition keyword — "grandfather"/"grandfathered" (Option B, default) OR "re-author"/"re-authored" (Option A) — with a stated reason.
    - The Task-1 placeholder text "decision recorded by EX-02 task" no longer appears in diagram-types/README.md.
    - scripts/, the verifier file, and the validator are unmodified (git/file mtimes for those paths unchanged — this task touches only diagram-types/README.md and, under Option A only, examples_excalidraw/star_schema.excalidraw).
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "Legacy example resolution" .claude/agents/excalidraw/diagram-types/README.md && grep -Eqi "grandfather|grandfathered|re-author|re-authored" .claude/agents/excalidraw/diagram-types/README.md && grep -q "star_schema" .claude/agents/excalidraw/diagram-types/README.md && grep -q "example_star_schema.png" .claude/agents/excalidraw/diagram-types/README.md && ! grep -q "decision recorded by EX-02 task" .claude/agents/excalidraw/diagram-types/README.md</automated>
  </verify>
  <done>The legacy star_schema.excalidraw is explicitly resolved (grandfathered by default, or re-authored to grouped/bound/sharp under Option A), the decision + reason + affected paths are recorded in the "Legacy example resolution" section of diagram-types/README.md, the placeholder is gone, and no pipeline files were touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KB-author -> diagram-types/ files | Markdown authored by the executor; consumed later by the specialist subagent as instructions. No untrusted external input crosses here. |
| diagram-types/ recipe -> render pipeline | A type recipe could (incorrectly) name an illegal arrowhead token or an absolute icon path that a later example would inherit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | diagram-types/README.md resolver table | mitigate | Single authoritative table (no duplicate copy that could drift); cross-ref both directions so a stale row is discoverable from kb/ side. |
| T-04-02 | Information disclosure | star_schema.excalidraw icon file_paths | accept | Legacy file is grandfathered and not used as a new template; no new absolute/`../` file_paths introduced this phase (PITFALLS Security Mistakes — path-traversal in icon embed is out of scope this milestone). |
| T-04-03 | Tampering | scripts/ + validator + verifier (frozen v1.0) | mitigate | This plan touches only diagram-types/, kb/*.md docs, and (Option A only) the legacy source; acceptance criteria assert the pipeline is unmodified. |
</threat_model>

<verification>
- diagram-types/ is a sibling of kb/ (not nested); resolver table present with the 5 mandated columns and a wired tech-architecture row.
- tech-architecture.md composes the six named kb primitives by @-reference, inlines no geometry, and reuses architecture_overview.png.
- kb/README.md and the six composed kb primitives carry the two-layer cross-references.
- star_schema.excalidraw is explicitly resolved with a recorded decision; no pipeline files touched.
</verification>

<success_criteria>
- DTKB-01: diagram-types/ exists as a sibling of kb/ with an authoritative resolver-table README.
- DTKB-02: tech-architecture.md states purpose + how-to-draw + composed sub-patterns, referencing kb/ primitives (no re-derived geometry).
- DTKB-03: the two layers cross-reference each other (type file links down; kb/README documents the relationship; composed primitives carry back-refs).
- EX-02: the legacy star_schema.excalidraw is explicitly resolved (grandfathered with a documented reason by default, or re-authored to the grouped/bound/sharp convention) and recorded in diagram-types/README.md.
</success_criteria>

<output>
Create .planning/phases/04-taxonomy-spine-star-resolution/04-01-SUMMARY.md when done.
</output>
