---
phase: 04-taxonomy-spine-star-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/diagram-types/notation-conventions.md
  - .claude/agents/excalidraw/diagram-types/compartmented-box.md
autonomous: true
requirements: [DTKB-04, INT-02]

must_haves:
  truths:
    - "The diagram-type KB commits to exactly ONE convention for each ambiguous notation forced by the fixed arrow|bar|dot|triangle|null arrowhead set"
    - "The reusable compartmented-box construction is documented once for reuse by all later compartmented types"
    - "Multi-line single text blocks are explicitly disallowed for compartments"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/notation-conventions.md"
      provides: "Committed arrowhead-workaround conventions (crow's-foot, diamonds, generalization triangle, actor, multiplicity)"
      min_lines: 25
    - path: ".claude/agents/excalidraw/diagram-types/compartmented-box.md"
      provides: "Reusable sharp-rect + line-divider + per-row monospace text bound-by-groupIds construction"
      min_lines: 25
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/notation-conventions.md"
      to: "arrowhead enum"
      via: "legal token set arrow|bar|dot|triangle|null"
      pattern: "bar.*dot.*triangle|arrow.*bar.*dot"
---

<objective>
Document, once, the two reusable conventions that all later compartmented/relationship diagram types depend on: (1) the notation WORKAROUNDS forced by the fixed Excalidraw arrowhead set (DTKB-04), committing to a single convention per ambiguous notation; and (2) the reusable compartmented-box construction (INT-02).

Purpose: Per PITFALLS Pitfall 1, an illegal arrowhead token renders silently as a plain line and erases the diagram's semantics — so the legal-encoding table must be committed and embeddable verbatim in every affected type file. Per PITFALLS Pitfall 2/3 and INT-02, a compartmented box must be a sharp rectangle + horizontal line dividers + per-row monospace text bound by groupIds (never a single multi-line text), or dividers can't be placed and the verifier's width-fit check breaks. The user chose to let the planner LOCK these conventions rather than run discuss-phase; the research-recommended defaults are adopted as the committed conventions.

Output: diagram-types/notation-conventions.md (DTKB-04) and diagram-types/compartmented-box.md (INT-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the arrowhead-workaround notation conventions (DTKB-04)</name>
  <read_first>
    - .planning/research/SUMMARY.md ("Hard arrowhead constraint and workarounds" table — the legal-encoding column is the committed convention source)
    - .planning/research/PITFALLS.md (Pitfall 1 "Faking crow's-foot/aggregation/inheritance" — the per-notation committed defaults; UX Pitfalls row on "Degraded notation" — surface substitutions as deliberate house conventions)
    - .planning/REQUIREMENTS.md (DTKB-04 exact wording)
  </read_first>
  <action>
    Create .claude/agents/excalidraw/diagram-types/notation-conventions.md. It MUST:
    - State the legal arrowhead set verbatim: arrow, bar, dot, triangle, null — and warn that any other token renders silently as a plain line (no error), erasing the relationship semantics (Pitfall 1).
    - Provide a committed notation -> legal-encoding table that COMMITS to exactly ONE convention per ambiguous notation (adopt the research-recommended defaults as the locked house conventions):
      - Association / message direction: endArrowhead "arrow", solid.
      - Dependency / return message: endArrowhead "arrow" + strokeStyle "dashed".
      - Generalization (inheritance): endArrowhead "triangle" — FILLED (house convention; not hollow).
      - Realization: endArrowhead "triangle" + strokeStyle "dashed".
      - Aggregation / composition: small (~14px) diamond element grouped at the owner end — white fill = aggregation, solid fill = composition.
      - ER "one": endArrowhead "bar".
      - ER "zero/optional": endArrowhead "dot" or a small ellipse glyph.
      - ER "many" (crow's-foot): the COMMITTED single choice — pick ONE of {grouped 3-line crow's-foot glyph} OR {textual multiplicity label like 0..*/1..*} and state it as the binding house default, noting the other is NOT used so diagrams stay consistent.
      - Actor representation: the COMMITTED single choice — labelled box (research-recommended) stated as the binding default, not stick-figure.
      - Multiplicity: textual labels (e.g. 1, 0..*, 1..*) where a glyph is not committed.
    - Note that exact glyph geometry (crow's-foot pixel sizing/rotation, diamond exact size) is deferred to the phases that ship ER/Class (Phase 7) — this file commits the CONVENTION choice, not the pixel geometry.
    - State that each affected later type file must embed/reference this table so the convention is consistent across diagrams.

    Do NOT inline full .excalidraw element JSON blocks; name the property keys and values in prose/table form. Do NOT touch scripts/, the validator, or the verifier.
  </action>
  <acceptance_criteria>
    - notation-conventions.md exists with at least 25 non-blank lines.
    - It contains all five legal arrowhead tokens: arrow, bar, dot, triangle, null.
    - It commits to FILLED triangle for generalization (contains "triangle" and "filled" case-insensitively).
    - It states exactly one committed crow's-foot/"many" choice and one committed actor choice (the words "crow" and "actor" both appear, each with a stated single default).
    - It contains no full element JSON (no literal '"type": "arrow"' object block).
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/notation-conventions.md && [ "$(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/diagram-types/notation-conventions.md)" -ge 25 ] && for t in arrow bar dot triangle null; do grep -qiw "$t" .claude/agents/excalidraw/diagram-types/notation-conventions.md || exit 1; done && grep -qi "filled" .claude/agents/excalidraw/diagram-types/notation-conventions.md && grep -qi "crow" .claude/agents/excalidraw/diagram-types/notation-conventions.md && grep -qi "actor" .claude/agents/excalidraw/diagram-types/notation-conventions.md && ! grep -q '"type": "arrow"' .claude/agents/excalidraw/diagram-types/notation-conventions.md</automated>
  </verify>
  <done>notation-conventions.md commits to one convention per ambiguous notation (filled generalization triangle, a single crow's-foot/multiplicity default, labelled-box actor, diamond aggregation/composition, bar/dot ER endpoints), lists the legal arrowhead set, warns about silent illegal-token failure, and defers pixel geometry to Phase 7.</done>
</task>

<task type="auto">
  <name>Task 2: Document the reusable compartmented-box construction (INT-02)</name>
  <read_first>
    - .planning/research/SUMMARY.md ("The reusable compartmented-box construction" paragraph — the canonical recipe)
    - .planning/research/PITFALLS.md (Pitfall 2 "Multi-line text defeats compartment layout"; Pitfall 3 "Misaligned compartment dividers and drifting row columns"; Technical Debt Patterns rows on multi-line text and skipping groupIds)
    - .planning/REQUIREMENTS.md (INT-02 exact wording)
  </read_first>
  <action>
    Create .claude/agents/excalidraw/diagram-types/compartmented-box.md documenting the ONE reusable construction reused by UML class boxes, ER entity boxes, and star/snowflake/data-vault table boxes. It MUST state:
    - The construction: one sharp rectangle (roundness null) + horizontal line dividers spanning the FULL box width + one bound/centered text for the title + N separate free-floating left-aligned monospace (fontFamily 3) text rows for attributes/columns — ALL elements in a single groupIds group.
    - The HARD prohibition: multi-line single text blocks (text containing \n) are explicitly DISALLOWED for compartments, because they defeat divider placement and break the verifier's per-element width-fit check (Pitfall 2). State this as a binding rule.
    - The alignment rules (Pitfall 3): all row text shares a common left x; divider line x-range equals the box x-range (full-width); coordinates on the 20-grid; row pitch a multiple of 20.
    - That the box is GROUPED via groupIds so it can be placed/translated as a unit (the legacy star_schema.excalidraw failed this — 0 groupIds — and is NOT a template).
    - A note that exact parametric offsets (header height, row pitch numbers) are finalized in Phase 6 (first compartmented type to ship); this file fixes the CONSTRUCTION and the rules, which later type files reference rather than re-derive.

    Do NOT inline full .excalidraw element JSON blocks; describe element types, the property keys (roundness, fontFamily, groupIds, strokeStyle), and the geometry rules in prose/table form. Do NOT touch scripts/, the validator, or the verifier.
  </action>
  <acceptance_criteria>
    - compartmented-box.md exists with at least 25 non-blank lines.
    - It names all four construction elements: a sharp rectangle, line dividers, a title text, and per-row monospace texts; and names groupIds.
    - It explicitly disallows multi-line single text blocks (contains "multi-line" and a prohibition word such as "disallow"/"never"/"forbidden").
    - It states the divider-spans-full-width and rows-share-left-x alignment rules.
    - It contains no full element JSON (no literal '"type": "rectangle"' object block).
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/compartmented-box.md && [ "$(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/diagram-types/compartmented-box.md)" -ge 25 ] && grep -qi "groupIds" .claude/agents/excalidraw/diagram-types/compartmented-box.md && grep -qi "divider" .claude/agents/excalidraw/diagram-types/compartmented-box.md && grep -qi "monospace\|fontFamily" .claude/agents/excalidraw/diagram-types/compartmented-box.md && grep -qi "multi-line" .claude/agents/excalidraw/diagram-types/compartmented-box.md && grep -Eqi "disallow|never|forbidden" .claude/agents/excalidraw/diagram-types/compartmented-box.md && ! grep -q '"type": "rectangle"' .claude/agents/excalidraw/diagram-types/compartmented-box.md</automated>
  </verify>
  <done>compartmented-box.md documents the sharp-rect + full-width line-dividers + per-row monospace text + groupIds construction once, explicitly disallows multi-line single text for compartments, states the alignment rules, and defers exact parametric offsets to Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KB-author -> convention docs | Markdown authored now; later consumed by the specialist as binding drawing rules. The risk is a convention that, if wrong, propagates a silent render defect into every diagram of every affected type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | notation-conventions.md arrowhead table | mitigate | Committed table lists ONLY legal tokens (arrow/bar/dot/triangle/null) and warns illegal tokens render silently; embeddable verbatim so no per-type re-derivation drifts. |
| T-04-05 | Tampering | compartmented-box.md multi-line prohibition | mitigate | Binding rule against multi-line text keeps the verifier's width-fit check valid; groupIds rule keeps box units translatable without divider desync. |
</threat_model>

<verification>
- notation-conventions.md commits exactly one convention per ambiguous notation and lists only legal arrowhead tokens.
- compartmented-box.md documents the construction once, disallows multi-line compartment text, and states the alignment + groupIds rules.
- No scripts/ / validator / verifier files touched.
</verification>

<success_criteria>
- DTKB-04: notation workarounds documented, one committed convention per ambiguous notation (crow's-foot/multiplicity, aggregation/composition diamonds, generalization triangle, actor representation).
- INT-02: the reusable compartmented-box construction is documented once, with multi-line single text explicitly disallowed for compartments.
</success_criteria>

<output>
Create .planning/phases/04-taxonomy-spine-star-resolution/04-02-SUMMARY.md when done.
</output>
