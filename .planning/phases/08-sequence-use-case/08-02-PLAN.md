---
phase: 08-sequence-use-case
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/sequence.md
  - .claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw
  - .claude/agents/excalidraw/examples/sequence_login_flow.png
autonomous: false
requirements: [UML-01]
must_haves:
  truths:
    - "diagram-types/sequence.md exists and composes kb/lifeline-activation.md + notation-conventions.md by @-reference without re-deriving geometry"
    - "A canonical sequence example pair (sequence_login_flow.excalidraw + .png) exists, with each activation bar centered on its lifeline x and message Y values increasing top-to-bottom"
    - "The canonical sequence example passes the full validate→render→verify loop (structural green + EX-03 visual approval)"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/sequence.md"
      provides: "Sequence TYPE-layer recipe (UML-01)"
      contains: "Layer: TYPE recipe"
      min_lines: 40
    - path: ".claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw"
      provides: "Canonical sequence JSON ground truth"
      contains: "lifeline"
    - path: ".claude/agents/excalidraw/examples/sequence_login_flow.png"
      provides: "Rendered canonical sequence PNG (sibling, passes loop)"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/sequence.md"
      to: ".claude/agents/excalidraw/kb/lifeline-activation.md"
      via: "@-reference (all geometry)"
      pattern: "lifeline-activation"
    - from: ".claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw"
      to: "activation bar rectangles"
      via: "message arrow startBinding/endBinding (NOT to lifeline line)"
      pattern: "endBinding"
---

<objective>
Author the `diagram-types/sequence.md` TYPE-layer recipe (UML-01) and produce the canonical
sequence example pair, passing the full validate→render→verify loop. This mirrors Phase 7's
`07-02-PLAN.md` (er.md + er_retail_orders example) exactly — a TYPE recipe that composes the
Wave-1 primitive by `@`-reference, plus a canonical example that is ground truth.

Per UML-01: authoring a sequence request (participant heads, dashed lifelines, activation bars,
ordered solid messages, dashed returns) must yield a rendered PNG that passes the full loop, with
each activation bar centered on its lifeline x and message Y values increasing top-to-bottom.

Subject (RESEARCH Open Question 2): a login/authentication sequence — User → AuthService →
UserDB, ~4-6 messages including at least one dashed return. Modest element count to avoid
3-iteration-cap pressure.

Purpose: The TYPE layer composes primitives; it never re-derives geometry. sequence.md
`@`-references `kb/lifeline-activation.md` (from 08-01) for ALL coordinate math and
`notation-conventions.md` for message encodings.

Output:
- `diagram-types/sequence.md` — new TYPE recipe
- `examples_excalidraw/sequence_login_flow.excalidraw` + `examples/sequence_login_flow.png` —
  canonical pair passing the full loop (EX-01/EX-03 exit criteria for the sequence type)

NOTE: Resolver row wiring is deferred to `08-04-PLAN.md` (EX-03 gate — wire only after the
example passes). This plan does NOT touch `diagram-types/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sequence-use-case/08-RESEARCH.md
@.planning/phases/08-sequence-use-case/08-PATTERNS.md
@.claude/agents/excalidraw/kb/lifeline-activation.md
@.claude/agents/excalidraw/diagram-types/snowflake-schema.md
@.claude/agents/excalidraw/diagram-types/notation-conventions.md
@.planning/phases/08-sequence-use-case/08-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification):
- `diagram-types/sequence.md` (new file) — TYPE recipe; headings: layer-header line, `## Purpose`, `## How to draw it`, `## Composes (primitive layer)`, `## Ground truth`
- `examples_excalidraw/sequence_login_flow.excalidraw` (new file) — element ids such as `ptcpt_user_head`, `activation_user_1`, lifeline line elements, message arrows
- `examples/sequence_login_flow.png` (new rendered sibling)
- The real example filename `sequence_login_flow` is filled into `kb/lifeline-activation.md`'s "See in examples" placeholder by this plan

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 7 prerequisites complete (dependency gate)</name>
  <files>.planning/phases/07-er-class/07-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-sequence-use-case/08-RESEARCH.md (Assumption A5 — Phase 8 must be blocked on Phase 7 Plan 04 completion: class.md, class_order_domain.excalidraw, and both wired resolver rows must exist)
    - .planning/ROADMAP.md (Phase 7 plan checklist — 07-03, 07-04)
  </read_first>
  <action>
Confirm Phase 7 has fully shipped before authoring any Phase 8 example (RESEARCH A5). Check that
`07-03-SUMMARY.md` and `07-04-SUMMARY.md` exist, that `examples_excalidraw/class_order_domain.excalidraw`
(or the actual class example filename) exists with a sibling PNG, and that the `er` and `class`
resolver rows in `diagram-types/README.md` are wired (NOT marked `_(planned)_`). If any prerequisite
is missing, STOP and report which Phase 7 deliverable is outstanding — do not proceed to author the
sequence example against an incomplete dependency. This is a hard gate per A5 (as of research time,
only Plans 01-02 of Phase 7 had completed summaries).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-er-class/07-04-SUMMARY.md &amp;&amp; ! grep -q 'class.*_(planned' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; echo PHASE7_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-er-class/07-04-SUMMARY.md` exists
    - `diagram-types/README.md` `class` and `er` rows are no longer marked `_(planned)_`
    - A class canonical example pair exists in `examples_excalidraw/` + `examples/`
    - IF any prerequisite is missing: the task halts with an explicit report naming the outstanding Phase 7 deliverable (do NOT continue to Task 2)
  </acceptance_criteria>
  <done>Phase 7 Plan 04 is confirmed complete (summaries present, resolver rows wired, class example exists), OR execution halts with a named outstanding-dependency report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author diagram-types/sequence.md recipe + canonical example pair (full loop)</name>
  <files>.claude/agents/excalidraw/diagram-types/sequence.md, .claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw, .claude/agents/excalidraw/examples/sequence_login_flow.png, .claude/agents/excalidraw/kb/lifeline-activation.md</files>
  <behavior>
    - sequence.md opens with the TYPE layer-header line and composes kb/lifeline-activation.md + notation-conventions.md by @-reference (no coordinate math in the type file)
    - sequence_login_flow.excalidraw: every activation-bar rectangle has center x == its lifeline line x (bar.x + bar.width/2 == line.x)
    - sequence_login_flow.excalidraw: message arrow Y values are monotonically non-decreasing (first message lowest Y, last highest Y)
    - sequence_login_flow.excalidraw: every message arrow startBinding/endBinding points at an activation-bar or participant-head rectangle id — never a lifeline line element
    - Structural verifier returns issues == [] for the example; rendered PNG passes EX-03 visual review
  </behavior>
  <read_first>
    - .claude/agents/excalidraw/kb/lifeline-activation.md (THE geometry source — all pixel values, JSON skeletons, and the bind-to-rectangle-not-line rule come from here; the type file references it, never re-derives)
    - .claude/agents/excalidraw/diagram-types/snowflake-schema.md (EXACT recipe-structure analog: layer-header line, "reuse verbatim — do not re-derive" framing, numbered "How to draw it" steps each citing a primitive by @-ref, binding-rules block, "Composes (primitive layer)" section, "Ground truth" section)
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md (Association = arrow+solid; Dependency = arrow+dashed — message and return encodings)
    - .planning/phases/08-sequence-use-case/08-PATTERNS.md (sequence.md section gives the 5 numbered "How to draw it" steps and binding-rules block verbatim; example JSON section gives the element inventory and verbatim participant-head/lifeline/activation-bar/message/return skeletons)
    - .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw (envelope/connector structure to imitate — DO NOT imitate star_schema.excalidraw, which is grandfathered/non-compliant)
    - .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw (freshest compliant bound-elbow-arrow example from Phase 7)
  </read_first>
  <action>
Author the sequence TYPE recipe and its canonical example, then drive the full loop until green.

(a) Create `diagram-types/sequence.md` mirroring `snowflake-schema.md` structure:
- Layer-header line: `> Layer: TYPE recipe. Composes primitives from ../kb/ ...; does not re-derive their geometry. Indexed in README.md.` Adapt to name `@../kb/lifeline-activation.md` + `@./notation-conventions.md`; note no compartmented-box dividers are needed for participant heads.
- "Reuse verbatim — do not re-derive" framing: sequence reuses `kb/lifeline-activation.md` VERBATIM — do not re-derive participant spacing (180px pitch), activation-bar width (12px), or message Y pitch (40px).
- `## Purpose` — what a sequence diagram is for / when to use it.
- `## How to draw it` — numbered steps (from PATTERNS): 1. participant head boxes (120×40, 180px pitch) per `@../kb/lifeline-activation.md`; 2. dashed vertical lifeline `line` from each head's bottom center; 3. activation bar `rectangle` per active participant (x = lifeline_center_x − 6); 4. message arrows (solid `arrow` for calls, dashed `arrow` for returns) per `@./notation-conventions.md`, bound to activation bars NOT lifeline lines, monotonic Y; 5. message text labels `fontFamily: 3`, `fontSize: 16`.
- Binding-rules block: `endArrowhead: "arrow"` on all messages; returns use `strokeStyle: "dashed"`; no `crowsfoot/diamond/hollow` tokens; every arrow start/end within 8px of a rectangle border via `startBinding`/`endBinding` to activation-bar (or participant-head) rectangle ids; NEVER bind to `line` elements.
- `## Composes (primitive layer)` — bullet links to `@../kb/lifeline-activation.md` (all geometry), `@./notation-conventions.md` (legal-encoding table), and `@./compartmented-box.md` (participant-head header height 40px verbatim; no dividers).
- `## Ground truth` — point at `../examples/sequence_login_flow.png`.

(b) Author `examples_excalidraw/sequence_login_flow.excalidraw` — a login sequence:
participants User, AuthService, UserDB (3 head rectangles 120×40 at x-pitch 180; lifeline_center_x
= 60, 240, 420 region per the primitive). For each: a dashed zero-width lifeline `line` at
lifeline_center_x, and activation bar rectangle(s) at x = lifeline_center_x − 6, width 12.
Messages (monotonic Y, ≥40px pitch): User→AuthService `login(user, pwd)` (solid); AuthService→UserDB
`findUser(user)` (solid); UserDB→AuthService `userRecord` (dashed return); AuthService→User `token`
(dashed return). Every message arrow `elbowed: true`, `roundness: null`, `roughness: 0`, bound via
startBinding/endBinding to activation-bar rectangle ids. Copy the verbatim skeletons from
`kb/lifeline-activation.md` / PATTERNS; imitate `snowflake_schema.excalidraw` for the file envelope
(`type, version, source, elements, appState, files`). Use the same envelope keys as existing examples.

(c) Run the full loop: `bash .claude/agents/excalidraw/scripts/render/validate_and_render.sh
examples_excalidraw/sequence_login_flow.excalidraw`, read the resulting PNG, invoke the
`excalidraw_verifier` subagent. Auto-fix on any failure (≤3 iterations), overwriting the single
sibling PNG each iteration. The example must reach structural green (issues == []) before the
human EX-03 visual gate (Task 3).

(d) Fill the real filename `sequence_login_flow.png` into `kb/lifeline-activation.md`'s "See in
examples" placeholder.
  </action>
  <verify>
    <automated>python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw &amp;&amp; test -f .claude/agents/excalidraw/examples/sequence_login_flow.png &amp;&amp; grep -q "Layer: TYPE recipe" .claude/agents/excalidraw/diagram-types/sequence.md &amp;&amp; grep -q "lifeline-activation" .claude/agents/excalidraw/diagram-types/sequence.md</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/sequence.md` exists, contains `Layer: TYPE recipe`, and `@`-references `../kb/lifeline-activation.md` and `./notation-conventions.md`
    - `sequence.md` contains NO bare pixel coordinate math beyond citing the primitive (recipe references geometry, does not re-derive it)
    - `examples_excalidraw/sequence_login_flow.excalidraw` exists with ≥3 participant-head rectangles, ≥3 dashed zero-width lifeline lines, ≥2 activation-bar rectangles, and ≥4 message arrows
    - `verifier_structural.py sequence_login_flow.excalidraw` returns issues == [] (exit 0, empty issue list)
    - No illegal arrowhead token in the example: `grep -ciE 'endArrowhead"?\s*:\s*"?(crowsfoot|hollow|open)' sequence_login_flow.excalidraw` == 0
    - `examples/sequence_login_flow.png` exists (sibling rendered)
    - `kb/lifeline-activation.md` "See in examples" now names `sequence_login_flow.png` (placeholder filled)
  </acceptance_criteria>
  <done>sequence.md recipe composes the primitive by reference; the login-flow example exists, renders, and passes structural verification with empty issues; the primitive's example placeholder is filled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: EX-03 visual gate — sequence example</name>
  <what-built>The canonical sequence example `examples/sequence_login_flow.png`, rendered from `examples_excalidraw/sequence_login_flow.excalidraw`, which passed structural verification (issues == []).</what-built>
  <how-to-verify>
1. Open `.claude/agents/excalidraw/examples/sequence_login_flow.png`.
2. Confirm 3 participant head boxes across the top (User, AuthService, UserDB) with dashed vertical lifelines descending from each.
3. Confirm each activation bar (thin rectangle) is centered ON its lifeline — the dashed line bisects the bar, not running alongside it.
4. Confirm messages read top-to-bottom in temporal order: login → findUser → userRecord (dashed return) → token (dashed return). Solid arrows for calls, dashed arrows for returns.
5. Confirm all labels are legible monospace, no text overflow, no raw-emoji tofu boxes.
  </how-to-verify>
  <resume-signal>Type "approved" to clear the EX-03 gate for sequence, or describe the visual defects to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored JSON → validate→render→verify loop | The example JSON is the only input; rendered through the frozen v1.0 loop. No network/auth/persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | Example uses an illegal arrowhead token, silently collapsing message semantics to a bare line | mitigate | Validator rejects illegal tokens (exit 1); acceptance grep asserts no crowsfoot/hollow/open; only Association(arrow+solid)/Dependency(arrow+dashed) used |
| T-08-04 | Spoofing | Message arrow bound to a lifeline `line` element passes loosely but fails the anchor semantic | mitigate | `check_arrow_endpoint_unanchored` accepts only rectangle/ellipse/diamond borders; acceptance asserts bindings target activation-bar/participant rectangles |
| T-08-05 | Repudiation | Resolver row wired before the example passes (broken ground truth imitated forever) | mitigate | This plan does NOT touch README.md; wiring is gated to 08-04 after EX-03 approval (Pitfall 5) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `diagram-types/sequence.md` exists, carries the TYPE layer-header, and composes
  `kb/lifeline-activation.md` + `notation-conventions.md` by `@`-reference with no re-derived geometry.
- `sequence_login_flow.excalidraw` passes `verifier_structural.py` with empty issues; the sibling
  PNG exists and clears the EX-03 human visual gate.
- No illegal arrowhead token appears in the example; message arrows bind to rectangles, not lines.
- The resolver row is NOT wired here (deferred to 08-04).
</verification>

<success_criteria>
- UML-01 recipe shipped: a sequence request can be authored from `diagram-types/sequence.md`
  composing the Wave-1 primitive.
- The canonical sequence example pair exists and passes the full validate→render→verify loop
  (structural green + EX-03 visual approval) — EX-01/EX-03 exit criteria met for the sequence type.
</success_criteria>

<output>
Create `.planning/phases/08-sequence-use-case/08-02-SUMMARY.md` when done. Record the EX-03
approval and the final example element count + any loop iterations needed.
</output>
