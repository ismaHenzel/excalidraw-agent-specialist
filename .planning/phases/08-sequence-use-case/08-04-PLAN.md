---
phase: 08-sequence-use-case
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/kb/lifeline-activation.md
  - .claude/agents/excalidraw/kb/group-container.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [UML-01, UML-03]
must_haves:
  truths:
    - "The sequence and use-case resolver rows in diagram-types/README.md are wired (no longer marked _(planned)_) and point at their real recipe files and example PNGs"
    - "kb/lifeline-activation.md carries `> Used by types: sequence` and kb/group-container.md adds `, use-case` to its back-ref so the two layers cannot drift"
    - "UML-01 and UML-03 are marked complete in REQUIREMENTS.md traceability"
    - "An end-to-end regression confirms /excalidraw dispatch resolves both new types and the existing wired types still pass structural verification"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/README.md"
      provides: "Single authoritative resolver table with sequence + use-case wired"
      contains: "sequence.md"
    - path: ".claude/agents/excalidraw/kb/lifeline-activation.md"
      provides: "Primitive with sequence back-ref (unchanged from 08-01)"
      contains: "Used by types: sequence"
    - path: ".claude/agents/excalidraw/kb/group-container.md"
      provides: "Primitive with use-case added to its back-ref"
      contains: "use-case"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/examples/sequence_login_flow.png"
      via: "resolver row Example PNG column"
      pattern: "sequence_login_flow"
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/examples/use_case_checkout.png"
      via: "resolver row Example PNG column"
      pattern: "use_case_checkout"
---

<objective>
Wire the `sequence` and `use-case` resolver rows in `diagram-types/README.md` (the single
authoritative family to type map), complete the two-layer back-references, mark UML-01 + UML-03
complete in REQUIREMENTS.md, and run the end-to-end regression. This is the Wave-3 finalization —
the exact analog of Phase 7's `07-04-PLAN.md` resolver-wiring plan.

Per Pitfall 5 / EX-03 gate: resolver rows are wired ONLY after each canonical example passes the
full validate→render→verify loop with EX-03 human visual approval. Sequence and use-case rows are
independent — each is wired because its own example (08-02 / 08-03) passed its EX-03 gate. Wiring a
placeholder row before the example passes means the specialist imitates broken ground truth forever.

Purpose: Close the two-layer cross-reference loop (type files already link down to primitives;
this plan completes the primitive-side `> Used by types:` back-refs) and flip the requirement
traceability so the phase is verifiably done.

Output:
- `diagram-types/README.md` — sequence + use-case rows wired, "Wired rows" note updated
- `kb/lifeline-activation.md` back-ref confirmed; `kb/group-container.md` back-ref extended with
  `, use-case`
- `REQUIREMENTS.md` — UML-01 + UML-03 marked complete
- End-to-end regression record in the SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sequence-use-case/08-RESEARCH.md
@.planning/phases/08-sequence-use-case/08-PATTERNS.md
@.claude/agents/excalidraw/diagram-types/README.md
@.planning/phases/08-sequence-use-case/08-02-SUMMARY.md
@.planning/phases/08-sequence-use-case/08-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files MODIFIED by THIS plan (exclude from drift verification):
- `diagram-types/README.md` — two resolver rows changed from `_(planned)_` to wired; "Wired rows" note paragraph extended
- `kb/lifeline-activation.md` — `> Used by types: sequence` (authored in 08-01; confirmed here)
- `kb/group-container.md` — `> Used by types:` line extended with `, use-case`
- `REQUIREMENTS.md` — UML-01, UML-03 checkboxes + traceability rows flipped to Complete

<tasks>

<task type="auto">
  <name>Task 1: Confirm both EX-03 gates passed before wiring (gate)</name>
  <files>.planning/phases/08-sequence-use-case/08-02-SUMMARY.md, .planning/phases/08-sequence-use-case/08-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-sequence-use-case/08-02-SUMMARY.md (must record sequence EX-03 visual approval + structural-green example)
    - .planning/phases/08-sequence-use-case/08-03-SUMMARY.md (must record use-case EX-03 visual approval + structural-green example)
    - .planning/phases/08-sequence-use-case/08-PATTERNS.md (EX-03 gate discipline — wire ONLY after validate_and_render.sh exits 0, verifier issues == [], and EX-03 human approval; rows are independent)
  </read_first>
  <action>
Confirm the EX-03 gate is cleared for each type before touching the resolver table (Pitfall 5). For
each of sequence and use-case: verify the example file pair exists
(`examples_excalidraw/sequence_login_flow.excalidraw` + `examples/sequence_login_flow.png`;
`examples_excalidraw/use_case_checkout.excalidraw` + `examples/use_case_checkout.png`), re-run the
structural verifier on each and confirm issues == [], and confirm the corresponding SUMMARY records
EX-03 visual approval. If either type's gate is not cleared, wire ONLY the cleared row and report the
uncleared one — never wire a row whose example has not passed. This is a hard gate.
  </action>
  <verify>
    <automated>python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/sequence_login_flow.excalidraw &amp;&amp; python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/use_case_checkout.excalidraw &amp;&amp; test -f .claude/agents/excalidraw/examples/sequence_login_flow.png &amp;&amp; test -f .claude/agents/excalidraw/examples/use_case_checkout.png</automated>
  </verify>
  <acceptance_criteria>
    - Both example pairs exist (source + rendered PNG) for sequence and use-case
    - `verifier_structural.py` returns issues == [] for BOTH example sources (exit 0)
    - `08-02-SUMMARY.md` and `08-03-SUMMARY.md` each record EX-03 visual approval
    - If a type's gate is uncleared: only the cleared row is wired in Task 2, with the uncleared type explicitly reported (do NOT wire an unpassed row)
  </acceptance_criteria>
  <done>Both EX-03 gates are confirmed cleared (or the uncleared type is reported and excluded from wiring).</done>
</task>

<task type="auto">
  <name>Task 2: Wire resolver rows + complete two-layer back-refs</name>
  <files>.claude/agents/excalidraw/diagram-types/README.md, .claude/agents/excalidraw/kb/group-container.md, .claude/agents/excalidraw/kb/lifeline-activation.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/README.md (FULL — resolver table with the `_(planned — Phase 8)_` sequence row at ~line 25 and use-case row at ~line 27; the "Wired rows" note at ~line 30; the "Adding a diagram type" 4-step procedure at ~line 48)
    - .claude/agents/excalidraw/kb/group-container.md (line 3 back-ref: `> Used by types: tech-architecture, activity, star-schema` — extend with `, use-case`)
    - .claude/agents/excalidraw/kb/lifeline-activation.md (confirm `> Used by types: sequence` from 08-01; no change unless missing)
    - .planning/phases/08-sequence-use-case/08-PATTERNS.md (README EDIT section — exact edits: sequence row composes `lifeline-activation, notation-conventions`; use-case row composes `group-container, notation-conventions`; both Example PNG columns; "Wired rows" note additions; back-ref additions)
  </read_first>
  <action>
Apply the resolver wiring and back-refs (per PATTERNS README-EDIT section), for each EX-03-cleared
type from Task 1:

1. Replace the `sequence` resolver row: Type file `sequence.md`, Composes
   `lifeline-activation, notation-conventions`, Example PNG `../examples/sequence_login_flow.png`.
   Remove the `_(planned — Phase 8)_` marker.
2. Replace the `use-case` resolver row: Type file `use-case.md`, Composes
   `group-container, notation-conventions`, Example PNG `../examples/use_case_checkout.png`.
   Remove the `_(planned — Phase 8)_` marker.
3. Update the "Wired rows" note paragraph (~line 30) to add: sequence (wired as of Phase 8 —
   canonical `sequence_login_flow.excalidraw` passes the full loop) and use-case (wired as of
   Phase 8 — canonical `use_case_checkout.excalidraw` passes the full loop).
4. Extend `kb/group-container.md` line-3 back-ref from `> Used by types: tech-architecture, activity, star-schema`
   to append `, use-case`. Confirm `kb/lifeline-activation.md` already carries `> Used by types: sequence`
   (authored in 08-01); add it if missing.

Do NOT add a `lifeline-activation` back-ref to any type other than sequence; do NOT modify any other
resolver row. Wire only the EX-03-cleared rows from Task 1.
  </action>
  <verify>
    <automated>grep -q 'sequence_login_flow.png' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; grep -q 'use_case_checkout.png' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; ! grep -E 'sequence\.md.*_\(planned' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; ! grep -E 'use-case\.md.*_\(planned' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; grep -q 'use-case' .claude/agents/excalidraw/kb/group-container.md</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/README.md` sequence row names `sequence.md`, composes `lifeline-activation, notation-conventions`, Example PNG `../examples/sequence_login_flow.png`, and is no longer marked `_(planned)_`
    - `diagram-types/README.md` use-case row names `use-case.md`, composes `group-container, notation-conventions`, Example PNG `../examples/use_case_checkout.png`, and is no longer marked `_(planned)_`
    - The "Wired rows" note paragraph mentions both sequence and use-case as wired in Phase 8
    - `kb/group-container.md` line-3 back-ref includes `use-case` (alongside the existing tech-architecture, activity, star-schema)
    - `kb/lifeline-activation.md` carries `> Used by types: sequence`
    - No other resolver row was modified (only sequence + use-case rows changed) — `git diff` shows changes confined to those two rows + the note paragraph
  </acceptance_criteria>
  <done>Both resolver rows are wired to real recipe files and example PNGs, the "Wired rows" note is updated, and the two-layer back-refs are complete and bidirectional.</done>
</task>

<task type="auto">
  <name>Task 3: Mark UML-01 + UML-03 complete and run end-to-end regression</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (UML-01 line 42, UML-03 line 44 checkboxes; Traceability table rows for UML-01/UML-03 at ~lines 107-108 — flip Status to Complete, matching the format used for the already-Complete UML-04 / DM-01 / DM-03 rows)
    - .claude/agents/excalidraw/diagram-types/README.md (the wired resolver table — the regression confirms every wired type still resolves and every wired example still passes structural verification)
  </read_first>
  <action>
(a) In `REQUIREMENTS.md`: flip `UML-01` and `UML-03` checkboxes from `[ ]` to `[x]`, and change
their Traceability-table Status cells from `Pending` to `Complete` — matching the exact format used
for the already-Complete UML-04, DM-01, DM-03 rows.

(b) End-to-end regression (SC-4 analog from Phase 7): re-run `verifier_structural.py` on every
already-wired canonical example to confirm Phase 8's additions did not regress prior types — at
minimum `activity_order_fulfillment.excalidraw`, `star_schema_v2.excalidraw`,
`snowflake_schema.excalidraw`, the Phase 7 `er`/`class` examples, plus the two new Phase 8 examples.
Each must still return issues == []. Record the regression results (per-file pass/fail) in the
SUMMARY. If the center-x check was added in 08-01, confirm it does not false-positive on any
non-sequence wired example (each prior example's narrow rectangles, if any, do not trigger
`activation_bar_center_x_mismatch`).
  </action>
  <verify>
    <automated>grep -q '\[x\] \*\*UML-01' .planning/REQUIREMENTS.md &amp;&amp; grep -q '\[x\] \*\*UML-03' .planning/REQUIREMENTS.md &amp;&amp; for f in activity_order_fulfillment star_schema_v2 snowflake_schema sequence_login_flow use_case_checkout; do python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/$f.excalidraw || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `REQUIREMENTS.md` shows `[x] **UML-01` and `[x] **UML-03`
    - `REQUIREMENTS.md` Traceability rows for UML-01 and UML-03 read `Complete`
    - Every wired canonical example (activity, star_schema_v2, snowflake_schema, er, class, sequence_login_flow, use_case_checkout) returns issues == [] under `verifier_structural.py` (exit 0)
    - IF the center-x check was added: no prior (non-sequence) wired example raises `activation_bar_center_x_mismatch`
    - The SUMMARY records the per-file regression results
  </acceptance_criteria>
  <done>UML-01 and UML-03 are marked complete in REQUIREMENTS.md, and the full end-to-end regression confirms no prior wired type regressed and both new types pass structural verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver table → specialist dispatch | The resolver row is the contract the specialist reads to pick a recipe + example; a wrong/premature row poisons every future diagram of that type. No network/auth/persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Repudiation | Resolver row wired before its example passed EX-03 (broken ground truth imitated forever) | mitigate | Task 1 gate confirms structural-green + EX-03 approval per type before Task 2 wires; rows wired independently |
| T-08-08 | Tampering | Two-layer back-ref left incomplete, allowing TYPE/PRIMITIVE layers to silently drift | mitigate | Acceptance asserts both primitive back-refs present (sequence on lifeline-activation, use-case on group-container) and type files link down |
| T-08-09 | Tampering | Phase 8 verifier/back-ref change regresses an already-wired prior example | mitigate | Task 3 regression runs `verifier_structural.py` over all wired examples; center-x false-positive explicitly checked |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Both resolver rows are wired to real recipe files + example PNGs, with the "Wired rows" note updated.
- Two-layer back-refs are complete and bidirectional (lifeline-activation → sequence;
  group-container → use-case).
- UML-01 + UML-03 are marked complete in REQUIREMENTS.md.
- The end-to-end regression confirms all wired examples (old + new) pass structural verification.
</verification>

<success_criteria>
- The single authoritative resolver table dispatches both new UML types to their recipes +
  examples; the two KB layers cross-reference each other so they cannot drift.
- UML-01 and UML-03 are verifiably complete and the phase introduces no regression to prior types.
</success_criteria>

<output>
Create `.planning/phases/08-sequence-use-case/08-04-SUMMARY.md` when done. Record the per-file
regression results and confirm both EX-03 gates were cleared before wiring.
</output>
