---
phase: 05-tech-architecture-activity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/diagram-types/activity.md
autonomous: true
requirements: [UML-04]
must_haves:
  truths:
    - "diagram-types/activity.md exists and states the Activity type's purpose, how-to-draw conventions, and composed primitives"
    - "The recipe composes only existing kb primitives by @-reference and re-derives no geometry"
    - "Swimlanes are expressed as parallel group-container lanes, not a new primitive"
    - "The recipe adopts a real diamond for decision gates and explicit-dimension text per the verifier contract"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/activity.md"
      provides: "Activity diagram TYPE-layer recipe"
      min_lines: 30
      contains: "Composes (primitive layer)"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/activity.md"
      to: "kb/decision-branch.md, kb/linear-pipeline.md, kb/feedback-loop.md, kb/group-container.md, kb/task-list.md, kb/decision-marker.md"
      via: "@-reference composition links"
      pattern: "@\\.\\./kb/(decision-branch|linear-pipeline|feedback-loop|group-container|task-list|decision-marker)\\.md"
---

<objective>
Author `diagram-types/activity.md` — the UML Activity TYPE-layer recipe (UML-04) — by composing only
existing `kb/` primitives, mirroring the structure and prose discipline of the already-shipped
`tech-architecture.md`.

Purpose: Activity is a "zero-new-notation" type by roadmap design: every element maps to an existing
primitive (start/end ellipses, action rectangles via linear-pipeline/task-list, decision diamonds via
decision-branch, binary gates via decision-marker, loop-backs via feedback-loop, optional swimlanes via
parallel group-containers). The recipe answers "what is Activity for and how do I assemble it from
primitives" without re-deriving any coordinate math (DTKB-02/DTKB-03). This is the authoring half of
UML-04; the example pair that proves it passes the loop is Plan 03.

Output: a new `diagram-types/activity.md` recipe with Purpose / How to draw it / Composes / Ground truth
sections following the `diagram-types/README.md` "Adding a diagram type" procedure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tech-architecture-activity/05-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New file created by this plan:
- `.claude/agents/excalidraw/diagram-types/activity.md` — the Activity recipe (sections: Purpose,
  How to draw it, Composes (primitive layer), Ground truth)

New strings the recipe introduces (prose conventions, NOT new code/primitives):
- "Activity" as the type name; "swimlane" expressed as parallel `group-container` lanes
- UML node conventions: filled `ellipse` initial node, `ellipse` ring final node, `rectangle` action
  node, real `diamond` decision gate
No new kb primitive, no new arrowhead glyph, no script, no schema. The recipe references the canonical
example PNG path that Plan 03 will produce (`../examples/<activity-name>.png`) — that file itself is a
Plan 03 artifact.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author diagram-types/activity.md composing existing primitives</name>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/tech-architecture.md (the structural template to mirror: Purpose / How to draw it / Composes (primitive layer) / Ground truth, with @-reference links and zero geometry)
    - .claude/agents/excalidraw/diagram-types/README.md ("Adding a diagram type" 4-step procedure; the activity resolver row listing the composed primitives)
    - .claude/agents/excalidraw/kb/decision-branch.md (decision diamond geometry — note the diamond is verifier-anchorable per 05-RESEARCH.md, the rotated-rectangle hedge is resolved)
    - .claude/agents/excalidraw/kb/linear-pipeline.md (sequential action chain)
    - .claude/agents/excalidraw/kb/task-list.md (vertical action stack with side I/O)
    - .claude/agents/excalidraw/kb/feedback-loop.md (loop-back routing rule: route around, never cross)
    - .claude/agents/excalidraw/kb/decision-marker.md (inline binary pass/fail gate)
    - .claude/agents/excalidraw/kb/group-container.md (swimlane lanes mechanism)
    - .planning/phases/05-tech-architecture-activity/05-RESEARCH.md (Pattern 1 element→primitive mapping table; Pattern 2 swimlanes-via-group-container; Anti-Patterns to Avoid)
  </read_first>
  <action>
    Create `.claude/agents/excalidraw/diagram-types/activity.md` mirroring tech-architecture.md's
    structure exactly, with these four sections:

    (1) Purpose — define a UML Activity diagram as start node → action nodes → decision gates →
    (optional swimlanes) → end node, possibly with loop-backs; state when to reach for it (a request
    to diagram a process/workflow's step-by-step behaviour, distinct from Tech Architecture's
    "what are the parts" framing).

    (2) How to draw it — prose conventions only, no `x:`/`y:` numbers. State: initial node = filled
    `ellipse`; final node = `ellipse` ring; action node = `rectangle` (one standalone `text` label per
    node, never a single multi-line text block); decision gate = a real `diamond` (state explicitly that
    `diamond` is a first-class verifier-anchorable shape, so the decision-branch rotated-rectangle hedge
    does NOT apply here); binary pass/fail = decision-marker inline ✗/✓; loop-back = feedback-loop routed
    AROUND the flow, never crossing it; optional swimlanes = N adjacent `group-container` lanes (one per
    role/partition, titled top-left, non-overlapping with clear gutters), cross-lane transitions are
    arrows crossing the shared lane border. Note every connector is a sharp elbow arrow
    (`elbowed: true`, `roundness: null`, orthogonal points) and every text carries explicit
    `width`/`height` — defer the actual geometry to the composed primitives.

    (3) Composes (primitive layer) — link each composed primitive with `@../kb/<pattern>.md` and one
    line on WHY: linear-pipeline (sequential actions), task-list (vertical action stack / side I/O),
    decision-branch (labelled decision diamond), decision-marker (inline binary gate), feedback-loop
    (loop back to an earlier action), group-container (swimlane lanes). Do NOT re-derive any geometry —
    this is the DTKB-02/DTKB-03 layer boundary; coordinate math is a layer violation here.

    (4) Ground truth — reference `@../examples/<activity-name>.png` as the canonical reference (the
    example pair is authored in Plan 03; use the same `<activity-name>` slug Plan 03 will use, e.g.
    `activity_*`). State that the example must be imitated for node/lane/loop composition.

    Add the top-of-file layer banner identical to tech-architecture.md
    ("Layer: TYPE recipe. Composes primitives from ../kb/; does not re-derive their geometry. Indexed in README.md.").
    Per 05-RESEARCH.md anti-patterns: do NOT invent a `swimlane.md` primitive or new arrowhead glyphs,
    and do NOT inline any coordinate math.
  </action>
  <verify>
    <automated>cd .claude/agents/excalidraw && test -f diagram-types/activity.md && test $(grep -c '@\.\./kb/' diagram-types/activity.md) -ge 5 && grep -qi 'swimlane' diagram-types/activity.md && grep -qi 'group-container' diagram-types/activity.md && grep -qi 'diamond' diagram-types/activity.md && ! grep -qE '"[xy]":\s*[0-9]' diagram-types/activity.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/activity.md` exists with the four sections Purpose, How to draw it, Composes (primitive layer), Ground truth
    - `grep -c '@\.\./kb/' diagram-types/activity.md` returns ≥ 5, and the links include decision-branch, linear-pipeline, feedback-loop, group-container, task-list (decision-marker optional but recommended)
    - The file contains the word `swimlane` and expresses it via `group-container` (no string `swimlane.md` and no reference to a swimlane primitive file)
    - The file references a real `diamond` for decision gates and states the rotated-rectangle hedge does not apply
    - No line matches `"x":`/`"y":` followed by a digit (no coordinate math — `! grep -qE '"[xy]":\s*[0-9]'` succeeds)
    - The Ground truth section references an `../examples/<activity-name>.png` path using an `activity`-prefixed slug
  </acceptance_criteria>
  <done>diagram-types/activity.md exists, composes only existing primitives by @-reference with zero geometry, expresses swimlanes via group-container, and adopts the real-diamond + explicit-dimension-text conventions the verifier requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Author-controlled Markdown only. No untrusted input, network, auth, or runtime state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | recipe re-derives geometry, drifting from kb primitives | mitigate | Acceptance criteria forbid `"x":`/`"y":` numbers; recipe composes by @-reference only (DTKB-03 anti-drift) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (05-RESEARCH.md Package Legitimacy Audit: "Not applicable") |
</threat_model>

<verification>
- UML-04 (authoring half): `test -f diagram-types/activity.md` and ≥5 `@../kb/` composition links
- Layer boundary: no coordinate math in the TYPE-layer recipe
- Swimlanes expressed via group-container, not a new primitive
</verification>

<success_criteria>
- diagram-types/activity.md authored with Purpose / How to draw it / Composes / Ground truth
- Composes only existing primitives by @-reference; re-derives no geometry
- Real-diamond decision gates, explicit-dimension text, feedback-loop routed around, swimlanes via group-container all stated
</success_criteria>

<output>
Create `.planning/phases/05-tech-architecture-activity/05-02-SUMMARY.md` when done
</output>
