---
phase: 09-data-vault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/diagram-types/data-vault.md
autonomous: true
requirements: [DM-04]
must_haves:
  truths:
    - "diagram-types/data-vault.md exists and composes @../diagram-types/compartmented-box.md + @../kb/fan-out.md + @../kb/tree-hierarchy.md + @../kb/convergence.md + @./notation-conventions.md by @-reference without re-deriving any box/connector geometry"
    - "data-vault.md documents the 3-role semantic palette (hub/link/satellite fills + strokes) AND mandates a text role label («hub»/«link»/«sat») on every box AND mandates an in-canvas legend — never color-alone (DM-04 SC-2)"
    - "data-vault.md states the connector rules: endArrowhead 'arrow' only, anchored to box RECTANGLE ids (never row texts or line dividers), elbowed sharp connectors"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/data-vault.md"
      provides: "Data-vault TYPE-layer recipe (DM-04) — hub/link/satellite composition + 3-role palette section"
      contains: "«hub»"
      min_lines: 50
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/data-vault.md"
      to: ".claude/agents/excalidraw/diagram-types/compartmented-box.md"
      via: "@-reference (box geometry reused verbatim for hub/link/satellite)"
      pattern: "compartmented-box"
    - from: ".claude/agents/excalidraw/diagram-types/data-vault.md"
      to: ".claude/agents/excalidraw/kb/fan-out.md"
      via: "@-reference (hub->link spine geometry)"
      pattern: "fan-out"
---

<objective>
Author the `diagram-types/data-vault.md` TYPE-layer recipe (DM-04). This is the LAST v1.1 type.
Hub/link/satellite boxes are the SAME locked compartmented box that star/snowflake/ER/class
already use — the recipe composes `@../diagram-types/compartmented-box.md` verbatim and never
re-derives box geometry. The ONE genuinely new design surface is the 3-role semantic palette
(SC-2): a distinct fill per role PAIRED WITH a mandatory text role label («hub»/«link»/«sat»)
and an in-canvas legend, so the three classes remain distinguishable in grayscale — never
color-alone.

This plan mirrors the prose half of Phase 8's `08-02-PLAN.md` and Phase 6's `06-01-PLAN.md`
Task 1 (recipe authoring), minus the example. The example pair is authored in `09-02-PLAN.md`;
the resolver row is wired in `09-03-PLAN.md` only after the example passes EX-03.

Per RESEARCH (Pattern 2, the 3-Role Palette section), the recommended triad — already documented
in the specialist Semantic Color Palette — is: hub `#93c5fd` (stroke `#1e3a5f`), link `#fed7aa`
(stroke `#c2410c`), satellite `#fef3c7` (stroke `#b45309`); luminance ≈ 0.53 / 0.73 / 0.89 for
maximum grayscale spread within the documented palette. The text role label, NOT the color, is
the SC-2 carrier.

Purpose: The TYPE layer composes primitives; it never re-derives geometry. data-vault.md
@-references the locked compartmented-box construction (hub/link/satellite boxes), fan-out
(hub→link spine), tree-hierarchy (satellite attachments), convergence (≥2 hubs → one link), and
notation-conventions (legal plain-association arrows).

Output:
- `diagram-types/data-vault.md` — new TYPE recipe with a dedicated 3-role palette + role-label +
  legend section (the distinctive Phase-9 design content)

NOTE: This plan does NOT touch `diagram-types/README.md` (resolver), does NOT author the example,
and does NOT touch any kb back-ref — those are deferred to 09-02 / 09-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-vault/09-RESEARCH.md
@.claude/agents/excalidraw/diagram-types/compartmented-box.md
@.claude/agents/excalidraw/diagram-types/snowflake-schema.md
@.claude/agents/excalidraw/diagram-types/notation-conventions.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification):
- `diagram-types/data-vault.md` (new file) — TYPE recipe; headings: layer-header line,
  `## Purpose`, `## How to draw it`, `## 3-role palette + role label + legend` (the new SC-2
  section), `## Composes (primitive layer)`, `## Ground truth`
- The recipe `## Ground truth` section points at `../examples/data_vault_sales.png`, a
  placeholder filename that 09-02 will author and 09-03 will index. Use `data_vault_sales` as the
  declared canonical subject (mirrors `star_schema_v2`, `er_retail_orders`, `use_case_checkout`
  naming).

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 8 prerequisites + the locked primitives data-vault composes (dependency gate)</name>
  <files>.claude/agents/excalidraw/diagram-types/compartmented-box.md</files>
  <read_first>
    - .planning/phases/09-data-vault/09-RESEARCH.md (Locked Decisions — Data Vault depends on Phase 6 table-box + Phase 7 relationships, both COMPLETE; the composed primitives compartmented-box / fan-out / tree-hierarchy / convergence must all exist and be finalized)
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (the LOCKED box construction with finalized offsets: header 40px, row pitch 20px, left-pad 12px, fontSize 16, fontFamily 3, width rule len*0.6*16 rounded to 20-grid, HARD prohibition on multi-line single text — reused verbatim, never re-derived)
    - .claude/agents/excalidraw/diagram-types/README.md (resolver table — confirm the reserved data-vault row at ~line 24 is still marked `_(planned — Phase 9)_` and lists the composition set group-container, fan-out, tree-hierarchy, convergence, evidence-card)
  </read_first>
  <action>
Confirm the dependency chain before authoring the recipe. Verify that `compartmented-box.md`
states its offsets are FINALIZED (header height 40, row pitch 20, left-pad 12, fontSize 16) and
NOT deferred — these are reused verbatim by hub/link/satellite boxes. Verify the composed
primitive files exist: `kb/fan-out.md`, `kb/tree-hierarchy.md`, `kb/convergence.md`,
`kb/group-container.md`, and `diagram-types/notation-conventions.md`. Verify the reserved
`data-vault` resolver row in `diagram-types/README.md` is still marked `_(planned — Phase 9)_`
(it MUST NOT already be wired — wiring is gated to 09-03 after EX-03). If any locked primitive is
missing or the compartmented-box offsets are still deferred, STOP and report which dependency is
outstanding — do not author the recipe against an incomplete foundation.
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/compartmented-box.md &amp;&amp; test -f .claude/agents/excalidraw/kb/fan-out.md &amp;&amp; test -f .claude/agents/excalidraw/kb/tree-hierarchy.md &amp;&amp; test -f .claude/agents/excalidraw/kb/convergence.md &amp;&amp; test -f .claude/agents/excalidraw/diagram-types/notation-conventions.md &amp;&amp; grep -Eq 'data-vault\.md.*_\(planned' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; echo DEPS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `compartmented-box.md`, `kb/fan-out.md`, `kb/tree-hierarchy.md`, `kb/convergence.md`, and `diagram-types/notation-conventions.md` all exist
    - `compartmented-box.md` states finalized offsets (grep `row pitch` matches; no "deferred to Phase 6" language remains for the offsets)
    - `diagram-types/README.md` data-vault row is still marked `_(planned — Phase 9)_` (NOT yet wired)
    - IF any dependency is missing or offsets are still deferred: the task halts with an explicit report naming the outstanding dependency (do NOT continue to Task 2)
  </acceptance_criteria>
  <done>All locked primitives data-vault composes exist with finalized offsets, the reserved resolver row is still planned, OR execution halts with a named outstanding-dependency report.</done>
</task>

<task type="auto">
  <name>Task 2: Author diagram-types/data-vault.md recipe with the 3-role palette + role-label + legend section</name>
  <files>.claude/agents/excalidraw/diagram-types/data-vault.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/snowflake-schema.md (EXACT recipe-structure analog: layer-header line, "reuse verbatim — do not re-derive" framing, numbered "How to draw it" steps each citing a primitive by @-ref, binding-rules block, "Composes (primitive layer)" section, "Ground truth" section)
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (the box construction + finalized offsets hub/link/satellite reuse verbatim; the HARD prohibition on multi-line single text; the full-width-divider / common-left-x alignment rules)
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md (legal arrowhead set arrow|bar|dot|triangle|null; data-vault connectors are plain associations → endArrowhead "arrow")
    - .claude/agents/excalidraw/kb/fan-out.md (hub → link spine dispatch geometry)
    - .claude/agents/excalidraw/kb/tree-hierarchy.md (satellite attachment indent geometry, thin elbows)
    - .claude/agents/excalidraw/kb/convergence.md (≥2 hubs → one link convergence geometry)
    - .claude/agents/excalidraw/excalidraw_specialist.md (Semantic Color Palette table at ~line 59 — confirm hub #93c5fd/#1e3a5f Tertiary, link #fed7aa/#c2410c Start/Trigger, satellite #fef3c7/#b45309 Decision are the documented palette entries the recipe draws from)
    - .planning/phases/09-data-vault/09-RESEARCH.md (Pattern 1 hub/link/satellite columns; Pattern 2 the 3-role palette + mandatory label + legend; the 3-Role Palette recommendation table with luminance values; Pitfalls 1-7)
  </read_first>
  <action>
Author `diagram-types/data-vault.md` mirroring `snowflake-schema.md` structure exactly. Do NOT
inline full element JSON; describe the construction in prose that @-references the primitives.

(a) Layer-header line: a `> Layer: TYPE recipe.` line naming the composed files
`@../diagram-types/compartmented-box.md`, `@../kb/fan-out.md`, `@../kb/tree-hierarchy.md`,
`@../kb/convergence.md`, `@./notation-conventions.md`, and stating it does not re-derive their
geometry and is indexed in README.md.

(b) "Reuse verbatim — do not re-derive" framing: hub, link, and satellite boxes are the SAME
compartmented box star/snowflake/ER/class use — reuse `@../diagram-types/compartmented-box.md`
offsets VERBATIM (header 40px, row pitch 20px, left-pad 12px, fontSize 16, fontFamily 3,
width = max_over_rows(len*0.6*16) rounded UP to the 20-grid). The ONLY differences between the
three classes are `backgroundColor` (role fill) and the role-label text — NOT the geometry.

(c) `## Purpose` — what a Data Vault model is for (hub = business-key table, link = relationship
table between hubs, satellite = descriptive/history table attached to a hub or link; raw-vault
slice; when to reach for it). Scope is exactly hub/link/satellite (Business Vault / PIT / Bridge
are out of scope per RESEARCH Deferred Ideas).

(d) `## How to draw it` — numbered steps, each citing a primitive by @-ref:
1. Hub boxes (spine) — compartmented box per `@../diagram-types/compartmented-box.md`, fill
   `#93c5fd`, stroke `#1e3a5f`; rows = `<biz>_HK PK`, business key column(s), `LOAD_DTS`,
   `RECORD_SRC`.
2. Link boxes — same box construction, fill `#fed7aa`, stroke `#c2410c`; rows = `LINK_*_HK PK`,
   two-or-more referenced `HUB_*_HK` FKs, `LOAD_DTS`, `RECORD_SRC`.
3. Satellite boxes — same box construction, fill `#fef3c7`, stroke `#b45309`; rows = parent
   `*_HK FK`, `LOAD_DTS PK`, `HASH_DIFF`, descriptive attribute(s), `RECORD_SRC`.
4. Hub→link spine connectors via `@../kb/fan-out.md` (a hub fanning to its links) and
   `@../kb/convergence.md` (≥2 hubs converging into one link).
5. Satellite attachments via `@../kb/tree-hierarchy.md` thin (`strokeWidth: 1.5`) indented elbow
   connectors off the parent hub/link.

(e) `## 3-role palette + role label + legend` (THE NEW SC-2 SECTION — the distinctive Phase-9
content). State as HARD RULES:
- Role fill per class: hub `#93c5fd`, link `#fed7aa`, satellite `#fef3c7` (from the documented
  Semantic Color Palette; luminance ≈ 0.53 / 0.73 / 0.89 — widest grayscale spread available in
  the documented palette).
- MANDATORY text role label on EVERY box: `«hub»` / `«link»` / `«sat»` stereotype text
  (`fontFamily: 3`; guillemets confirmed safe per Phase 7) placed in/above the box header; ASCII
  `[HUB]`/`[LINK]`/`[SAT]` is the zero-risk fallback if guillemets tofu. State explicitly: the
  LABEL, not the color, is the SC-2 carrier — color-alone distinction is DISALLOWED.
- MANDATORY in-canvas legend: a small `@../kb/group-container.md` box (or a 3-row swatch+label
  strip) decoding role↔color so a grayscale reader recovers the mapping.
- State that the EX-03 visual gate must confirm grayscale-distinguishability (no automated color
  check exists in the loop).

(f) Binding-rules block: `endArrowhead: "arrow"` on all connectors (plain association — Data
Vault relationships are not cardinality-decorated in this scope); no `crowsfoot`/`diamond`/
`hollow` tokens (render silently as bare lines); `elbowed: true`, `roundness: null`,
`roughness: 0`, ≥3 orthogonal points; bind via `startBinding`/`endBinding` to the box RECTANGLE
ids ONLY — NEVER to row texts or `line` dividers (not in the anchorable set; trips
`arrow_endpoint_unanchored`). Boxes are sharp (`roundness: null`); one shared `groupIds` per box
(frame + dividers + title + role label + every row text).

(g) `## Composes (primitive layer)` — bullet @-ref links to `@../diagram-types/compartmented-box.md`
(box construction, all offsets), `@../kb/fan-out.md` (hub→link spine), `@../kb/tree-hierarchy.md`
(satellite attachments), `@../kb/convergence.md` (≥2 hubs → one link), `@../kb/group-container.md`
(legend box), and `@./notation-conventions.md` (legal-arrowhead encoding). This list must match
the resolver composition set 09-03 will wire (group-container, fan-out, tree-hierarchy,
convergence — evidence-card is optional and may be dropped if the example does not compose it).

(h) `## Ground truth` — point at `../examples/data_vault_sales.png` (the canonical example 09-02
authors).
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "Layer: TYPE recipe" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "compartmented-box" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "fan-out" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "tree-hierarchy" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "«hub»" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "«link»" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "«sat»" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -qi "legend" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "#93c5fd" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "#fed7aa" .claude/agents/excalidraw/diagram-types/data-vault.md &amp;&amp; grep -q "#fef3c7" .claude/agents/excalidraw/diagram-types/data-vault.md</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/data-vault.md` exists, contains `Layer: TYPE recipe`, and @-references `../diagram-types/compartmented-box.md`, `../kb/fan-out.md`, `../kb/tree-hierarchy.md`, `../kb/convergence.md`, and `./notation-conventions.md`
    - The recipe contains NO bare box/connector pixel coordinate math beyond citing the primitive (recipe references geometry, does not re-derive it)
    - The recipe contains the four standard sections (Purpose / How to draw it / Composes (primitive layer) / Ground truth) PLUS the dedicated 3-role palette + role-label + legend section
    - All three role labels `«hub»`, `«link»`, `«sat»` appear in the recipe; the word `legend` appears; and all three role fills `#93c5fd`, `#fed7aa`, `#fef3c7` appear
    - The recipe states color-alone distinction is DISALLOWED and the text role label is the SC-2 carrier
    - The recipe states connectors use `endArrowhead: "arrow"` only, anchored to box RECTANGLE ids (never row texts / line dividers)
    - `## Ground truth` references `../examples/data_vault_sales.png`
  </acceptance_criteria>
  <done>data-vault.md recipe composes the locked primitives by @-reference, documents the 3-role palette + mandatory role label + legend (SC-2), states the legal-arrowhead connector rules, and points its ground truth at the planned data_vault_sales.png — no geometry re-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored recipe prose → specialist dispatch | The recipe is read by the specialist to author future data-vault diagrams; a recipe that permits color-alone role distinction poisons every future data-vault diagram (SC-2). No network/auth/persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Recipe permits color-only role distinction (no mandatory label), silently failing SC-2 grayscale safety | mitigate | Acceptance asserts «hub»/«link»/«sat» labels + legend + "color-alone DISALLOWED" language present; the label is documented as the SC-2 carrier (RESEARCH Pitfall 1) |
| T-09-02 | Tampering | Recipe re-derives box/connector geometry, drifting from the locked compartmented-box / fan-out / tree-hierarchy primitives | mitigate | Acceptance asserts @-references to all composed primitives and no bare coordinate math; "reuse verbatim — do not re-derive" framing required (INT-02) |
| T-09-03 | Tampering | Recipe permits an illegal arrowhead token (crowsfoot/diamond/hollow), collapsing connectors to bare lines | mitigate | Recipe states endArrowhead "arrow" only + lists the forbidden tokens (notation-conventions / DTKB-04) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `diagram-types/data-vault.md` exists, carries the TYPE layer-header, and composes
  compartmented-box + fan-out + tree-hierarchy + convergence + notation-conventions by
  @-reference with no re-derived geometry.
- The recipe documents the 3-role palette (hub #93c5fd / link #fed7aa / satellite #fef3c7) AND
  mandates the «hub»/«link»/«sat» role label AND mandates an in-canvas legend; color-alone is
  DISALLOWED (SC-2).
- The recipe states the legal connector rules (endArrowhead "arrow", anchor to rectangle ids).
- This plan does NOT touch the resolver, the example, or kb back-refs (deferred to 09-02/09-03).
</verification>

<success_criteria>
- DM-04 recipe shipped: a data-vault request can be authored from `diagram-types/data-vault.md`
  composing the locked primitives, with the 3-role palette + mandatory role label + legend that
  satisfies SC-2's grayscale-safety requirement.
- The genuinely new design surface (the 3-role palette + label + legend convention) is documented
  once, ready for the canonical example (09-02) to instantiate.
</success_criteria>

<output>
Create `.planning/phases/09-data-vault/09-01-SUMMARY.md` when done. Record the chosen 3-role triad
(fill + stroke per role) and the role-label form (guillemet vs ASCII) so 09-02 authors the example
to it verbatim.
</output>
