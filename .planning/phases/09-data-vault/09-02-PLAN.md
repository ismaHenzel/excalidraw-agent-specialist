---
phase: 09-data-vault
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw
  - .claude/agents/excalidraw/examples/data_vault_sales.png
autonomous: false
requirements: [DM-04]
must_haves:
  truths:
    - "A canonical data-vault example pair (data_vault_sales.excalidraw + .png) exists with ≥1 hub, ≥1 link, ≥1 satellite box, each carrying its «hub»/«link»/«sat» role label and its role fill"
    - "The example contains an in-canvas legend (3 swatch+label rows) decoding role↔color so a grayscale reader recovers the mapping"
    - "The canonical example passes the full validate→render→verify loop: verifier_structural.py returns issues == [] AND the EX-03 visual gate confirms the three roles are distinguishable IN GRAYSCALE"
  artifacts:
    - path: ".claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw"
      provides: "Canonical data-vault JSON ground truth (hub/link/satellite, role fills + labels, legend, plain-arrow spine + sat elbows)"
      contains: "«hub»"
    - path: ".claude/agents/excalidraw/examples/data_vault_sales.png"
      provides: "Rendered canonical data-vault PNG (sibling, passes loop)"
  key_links:
    - from: ".claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw"
      to: "box rectangle ids"
      via: "spine/satellite arrow startBinding/endBinding (NOT to row texts or line dividers)"
      pattern: "endBinding"
    - from: ".claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw"
      to: ".claude/agents/excalidraw/diagram-types/data-vault.md"
      via: "example authored TO the recipe (3-role palette + role labels + legend)"
      pattern: "«sat»"
---

<objective>
Author the canonical data-vault example pair (`data_vault_sales.excalidraw` + `.png`) TO the
recipe authored in 09-01, and drive it through the full validate→render→verify loop until green,
then the EX-03 visual gate (the ONLY judge of SC-2 grayscale-safety). This mirrors Phase 8's
`08-02-PLAN.md` Task 2 + Task 3 (example authoring + EX-03 checkpoint) and Phase 6's
`06-01-PLAN.md` Task 2 + Task 3.

Per DM-04 / SC-1: authoring a data-vault request (hubs as the spine, links between hubs,
satellites attached to hubs/links) must yield a rendered PNG that passes the full loop. Per
SC-2: the hub/link/satellite distinction must use the documented 3-role palette PAIRED WITH a
text role label so the three classes remain distinguishable in grayscale — the EX-03 gate
explicitly converts the PNG to grayscale and confirms readability.

Subject (RESEARCH Discretion / Open Question): a small recognizable raw-vault Sales slice —
e.g. HUB_CUSTOMER + HUB_PRODUCT as the spine, LINK_ORDER relating them, and a satellite or two
(SAT_CUSTOMER_DETAIL off the customer hub, SAT_ORDER_DETAIL off the link). Modest element count
to avoid 3-iteration-cap pressure.

Purpose: The canonical example is the ground truth the specialist imitates for every future
data-vault diagram. It MUST pass the full loop (structural green + EX-03 visual approval WITH
explicit grayscale confirmation) BEFORE the resolver row is wired (09-03) — wiring a broken
ground truth means the agent imitates it forever (Pitfall 7).

Output:
- `examples_excalidraw/data_vault_sales.excalidraw` + `examples/data_vault_sales.png` — canonical
  pair passing the full loop (EX-01/EX-03 exit criteria for the data-vault type)

NOTE: Resolver row wiring + kb back-refs are deferred to `09-03-PLAN.md` (EX-03 gate — wire only
after the example passes). This plan does NOT touch `diagram-types/README.md` or any kb file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-vault/09-RESEARCH.md
@.claude/agents/excalidraw/diagram-types/data-vault.md
@.claude/agents/excalidraw/diagram-types/compartmented-box.md
@.claude/agents/excalidraw/diagram-types/notation-conventions.md
@.planning/phases/09-data-vault/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification):
- `examples_excalidraw/data_vault_sales.excalidraw` (new file) — element ids such as
  `hub_customer_box`, `hub_customer_role`, `link_order_box`, `sat_customer_box`, legend ids
  (`legend_box`, `sw_hub`/`sw_link`/`sw_sat`, `lbl_hub`/`lbl_link`/`lbl_sat`), spine/satellite
  arrow ids — author's choice, internal to the example
- `examples/data_vault_sales.png` (new rendered sibling)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author data_vault_sales example pair and drive the full validate→render→verify loop to structural green</name>
  <files>.claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw, .claude/agents/excalidraw/examples/data_vault_sales.png</files>
  <behavior>
    - data_vault_sales.excalidraw has ≥1 hub box (fill #93c5fd), ≥1 link box (fill #fed7aa), ≥1 satellite box (fill #fef3c7), each a sharp grouped compartmented box with a full-width line header divider and per-row monospace text
    - every hub/link/satellite box carries exactly one role-label text from {«hub», «link», «sat»} matching its role fill
    - an in-canvas legend exists: 3 swatch rectangles (#93c5fd, #fed7aa, #fef3c7) each paired with a label text (Hub / Link / Satellite)
    - every spine/satellite arrow startBinding/endBinding points at a box RECTANGLE id — never a row text or a line divider; endArrowhead is "arrow"; no crowsfoot/diamond/hollow token
    - no text element contains a newline (no multi-line single text); every box element shares one groupIds id
    - verifier_structural.py returns issues == [] for the example; the sibling PNG renders
  </behavior>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/data-vault.md (the recipe just authored — author the example TO it: 3-role palette, mandatory role labels, legend, connector rules)
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (the finalized offsets — apply them exactly: header 40, row pitch 20, left-pad 12, fontSize 16, fontFamily 3, width = max row len*0.6*16 rounded UP to 20-grid; HARD prohibition on multi-line single text; full-width divider x==box.x and points[-1][0]==box.width; common left x = box.x+12)
    - .planning/phases/09-data-vault/09-RESEARCH.md (Pattern 1 hub/link/satellite column inventories; Pattern 2 palette+label+legend; Code Examples section — annotated hub box / spine+satellite connector / legend skeletons; Pitfall 2 width-fit for long HK names; "Minimum required text-element fields")
    - .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw (file envelope to imitate — type/version/source/elements/appState/files; bound elbow arrows; grouped sharp boxes; DO NOT imitate the grandfathered non-compliant star_schema.excalidraw)
    - .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw (freshest compliant bound-elbow-arrow + compartmented-box example from Phase 7)
    - .claude/agents/excalidraw/scripts/render/validate_and_render.sh (the FROZEN entry point; usage: bash scripts/render/validate_and_render.sh <file>.excalidraw)
  </read_first>
  <action>
Author `examples_excalidraw/data_vault_sales.excalidraw` for a small recognizable raw-vault Sales
slice, then drive the full loop until green.

(a) Boxes — apply the compartmented-box finalized offsets VERBATIM (header 40, row pitch 20,
left-pad 12 so every row/title/role-label text x = box.x + 12, fontSize 16, fontFamily 3). Size
each box.width >= max_over_rows(len(row.text) * 0.6 * 16) rounded UP to the 20-grid (DV hash-key
names are long — `HUB_CUSTOMER_HK  PK` ≈ 19 chars → width ≥ 200; use ≥260 for safety per Pitfall 2).
Author at minimum:
- HUB_CUSTOMER (fill #93c5fd, stroke #1e3a5f, label «hub»): rows CUSTOMER_HK PK, CUSTOMER_BK,
  LOAD_DTS, RECORD_SRC.
- HUB_PRODUCT (fill #93c5fd, label «hub»): rows PRODUCT_HK PK, PRODUCT_BK, LOAD_DTS, RECORD_SRC.
- LINK_ORDER (fill #fed7aa, stroke #c2410c, label «link»): rows ORDER_HK PK, CUSTOMER_HK FK,
  PRODUCT_HK FK, LOAD_DTS, RECORD_SRC.
- SAT_CUSTOMER_DETAIL (fill #fef3c7, stroke #b45309, label «sat»): rows CUSTOMER_HK FK,
  LOAD_DTS PK, HASH_DIFF, NAME, EMAIL, RECORD_SRC.
Each box: sharp rectangle (roundness null, roughness 0), full-width `line` header divider
(x == box.x, points [[0,0],[width,0]] so points[-1][0] == box.width exactly), the «role» label
text in/above the header, one title text, N free-floating per-row monospace texts at left x =
box.x+12 pitch 20, ALL elements of the box sharing ONE groupIds id. Include all required text
fields (type,x,y,width,height,text,originalText,fontSize,fontFamily:3,lineHeight:1.25,textAlign,
verticalAlign,strokeColor,roundness,groupIds) so text_missing_dimensions never fires.

(b) Connectors — hub→link spine via fan-out/convergence geometry (HUB_CUSTOMER → LINK_ORDER,
HUB_PRODUCT → LINK_ORDER), satellite attachment via tree-hierarchy thin (strokeWidth 1.5) indented
elbow (HUB_CUSTOMER → SAT_CUSTOMER_DETAIL). Every arrow: elbowed true, roundness null, roughness 0,
≥3 orthogonal points, endArrowhead "arrow", startArrowhead null, startBinding/endBinding to the box
RECTANGLE ids (gap 4) — NEVER to a row text or a `line` divider (trips arrow_endpoint_unanchored).
No crowsfoot/diamond/hollow token anywhere.

(c) Legend (SC-2 grayscale guarantee) — a small group-container rectangle plus 3 swatch
rectangles (#93c5fd, #fed7aa, #fef3c7) each paired with a label text "Hub" / "Link" / "Satellite",
positioned in a clear canvas region (e.g. bottom-left). The legend lets a grayscale reader recover
role↔color.

(d) Render via: `bash .claude/agents/excalidraw/scripts/render/validate_and_render.sh
.claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw` — this writes the sibling
PNG. Confirm the rendered PNG lands at `examples/data_vault_sales.png` (the resolver ground-truth
location). Read the PNG, invoke the `excalidraw_verifier` subagent. Auto-fix on any failure
(≤3 iterations), overwriting the single sibling PNG each iteration. The example must reach
structural green (issues == []) before the human EX-03 gate (Task 2).
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/examples/data_vault_sales.png &amp;&amp; python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw &amp;&amp; python3 -c "import json; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw')); els=d['elements']; t=[e for e in els if e['type']=='text']; assert any('«hub»' in e.get('text','') or '[HUB]' in e.get('text','') for e in t), 'no hub role label'; assert any('«link»' in e.get('text','') or '[LINK]' in e.get('text','') for e in t), 'no link role label'; assert any('«sat»' in e.get('text','') or '[SAT]' in e.get('text','') for e in t), 'no sat role label'; assert all('\n' not in e.get('text','') for e in t), 'multi-line row text present'; print('LABELS_OK')" &amp;&amp; test $(grep -ciE 'endArrowhead"?\s*:\s*"?(crowsfoot|hollow|diamond|open)' .claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `examples_excalidraw/data_vault_sales.excalidraw` exists with ≥2 hub boxes (fill #93c5fd), ≥1 link box (fill #fed7aa), ≥1 satellite box (fill #fef3c7)
    - Each hub/link/satellite box carries exactly one role-label text from {«hub», «link», «sat»} (or the ASCII fallback) matching its fill
    - An in-canvas legend exists: 3 swatch rectangles (#93c5fd, #fed7aa, #fef3c7) each paired with a Hub/Link/Satellite label text
    - `verifier_structural.py data_vault_sales.excalidraw` returns issues == [] (exit 0, empty issue list)
    - No `text` element contains a `\n` (per-row separate text; no multi-line)
    - No illegal arrowhead token: `grep -ciE 'endArrowhead"?\s*:\s*"?(crowsfoot|hollow|diamond|open)'` == 0
    - Every spine/satellite arrow binds to a box rectangle id via startBinding/endBinding (no arrow_endpoint_unanchored error from the verifier)
    - `examples/data_vault_sales.png` exists (rendered by validate_and_render.sh, not hand-edited)
  </acceptance_criteria>
  <done>data_vault_sales pair exists and renders; ≥2 hubs + ≥1 link + ≥1 satellite each carry a role label + role fill; an in-canvas legend is present; verifier_structural.py returns empty issues; no multi-line text and no illegal arrowhead token.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: EX-03 visual gate — data-vault example (WITH explicit grayscale check)</name>
  <files>.claude/agents/excalidraw/examples/data_vault_sales.png</files>
  <what-built>The canonical data-vault example `examples/data_vault_sales.png`, rendered from `examples_excalidraw/data_vault_sales.excalidraw`, which passed structural verification (issues == []). This is the FIRST color-semantic data-model example in the repo — SC-2 (grayscale-distinguishable role distinction) has NO automated guard, so this visual gate is the only judge.</what-built>
  <how-to-verify>
1. Invoke the excalidraw_verifier subagent on
   `.claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw` with its sibling PNG,
   and confirm the verifier-report shows `passed: true` with an empty `issues` array (EX-03 exit gate).
2. Open `.claude/agents/excalidraw/examples/data_vault_sales.png` and confirm in COLOR:
   - hub box(es) light-blue (#93c5fd), link box light-amber (#fed7aa), satellite box pale-yellow
     (#fef3c7); each carries its «hub»/«link»/«sat» role label legibly in the header.
   - the hub→link spine arrows connect box borders (not floating, not landing on text/dividers);
     satellite attaches to its hub via a thin indented elbow.
   - an in-canvas legend (3 swatch+label rows: Hub / Link / Satellite) is visible and legible.
   - every compartment divider spans the full box width; rows share a common left x; no row text
     pokes past its border; boxes are sharp-cornered; no raw-emoji tofu boxes.
3. CONVERT THE PNG TO GRAYSCALE (e.g. `convert .claude/agents/excalidraw/examples/data_vault_sales.png
   -colorspace Gray /tmp/data_vault_sales_gray.png`) and confirm: the three roles are STILL
   distinguishable — via the «hub»/«link»/«sat» labels and the legend (luminance ordering helps but
   the LABEL is the carrier). This is the SC-2 judgement. If two roles look identical in grayscale
   AND their labels are not clearly readable, the example fails — strengthen the labels (e.g. a header
   role band) and re-render.
  </how-to-verify>
  <resume-signal>Type "approved" if the verifier reports passed:true AND the three roles are distinguishable in BOTH color and grayscale (label + legend legible); otherwise describe the specific defect (which role, which check) so it can be fixed and re-rendered.</resume-signal>
  <action>This is a verification checkpoint, not an authoring task — do not write or mutate any diagram/recipe file here. Perform the verification described in <how-to-verify>: invoke excalidraw_verifier on the example + its sibling PNG, confirm passed:true, then human-eyeball the PNG in COLOR and again CONVERTED TO GRAYSCALE for the SC-2 role-distinguishability judgement the verifier cannot enforce. Pause for the resume-signal.</action>
  <verify><human-check>Verifier report shows passed:true with empty issues, AND the PNG satisfies every bullet in <how-to-verify> in BOTH color and grayscale — the three roles distinguishable via the «hub»/«link»/«sat» labels + legend (SC-2 grayscale confirmation).</human-check></verify>
  <done>excalidraw_verifier reports passed:true on the example (EX-03) and the operator has confirmed the three roles are distinguishable in color AND grayscale (SC-2); defects, if any, were fixed and re-rendered before approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored JSON → validate→render→verify loop | The example JSON is the only input; rendered through the frozen v1.0 loop. No network/auth/persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Example distinguishes roles by color alone (no/illegible role label), silently failing SC-2 in grayscale | mitigate | Acceptance asserts each box carries a «hub»/«link»/«sat» label + a 3-row legend; Task 2 EX-03 gate explicitly converts to grayscale and confirms distinguishability (the only SC-2 judge — RESEARCH Pitfall 1) |
| T-09-05 | Tampering | Spine/satellite arrow bound to a row text or `line` divider, tripping arrow_endpoint_unanchored or rendering a floating arrow | mitigate | check_arrow_endpoint_unanchored accepts only rectangle/ellipse/diamond borders; acceptance asserts bindings target box rectangle ids (Pitfall 3) |
| T-09-06 | Tampering | Box width too small for long HK names → text_overflow_static error | mitigate | Width rule len*0.6*16 rounded to 20-grid applied; ≥260 for long hash-key rows (Pitfall 2); verifier_structural.py overflow check is the automated guard |
| T-09-07 | Repudiation | Resolver row wired before the example passes EX-03 (broken ground truth imitated forever) | mitigate | This plan does NOT touch README.md; wiring is gated to 09-03 after EX-03 approval (Pitfall 7) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `data_vault_sales.excalidraw` passes `verifier_structural.py` with empty issues; the sibling PNG
  exists and clears the EX-03 human visual gate INCLUDING the explicit grayscale-distinguishability
  check (the only SC-2 judge).
- ≥2 hubs + ≥1 link + ≥1 satellite, each with its role fill + role label; an in-canvas legend
  is present.
- No multi-line text; no illegal arrowhead token; arrows bind to box rectangles, not lines/texts.
- The resolver row is NOT wired here (deferred to 09-03).
</verification>

<success_criteria>
- DM-04 / SC-1 satisfied: the canonical data-vault example pair exists and passes the full
  validate→render→verify loop (structural green).
- DM-04 / SC-2 satisfied: the three roles are distinguishable in grayscale via the documented
  palette + mandatory role labels + legend — confirmed at the EX-03 visual gate with an explicit
  grayscale conversion.
- EX-01/EX-03 exit criteria met for the data-vault type, clearing the gate for resolver wiring
  (09-03).
</success_criteria>

<output>
Create `.planning/phases/09-data-vault/09-02-SUMMARY.md` when done. Record the EX-03 approval
(including the explicit grayscale confirmation), the final element count, the role-label form used
(guillemet vs ASCII), the kb primitives the example actually composed (for 09-03 resolver wiring),
and any loop iterations needed.
</output>
