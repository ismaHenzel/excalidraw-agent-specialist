---
phase: 07-er-class
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/class.md
  - .claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw
  - .claude/agents/excalidraw/examples/class_order_domain.png
autonomous: false
requirements: [UML-02]
user_setup: []
must_haves:
  truths:
    - "diagram-types/class.md exists, composes the locked compartmented-box (three compartments) + the new relationship-endpoint primitive + notation-conventions by @-reference, and re-derives no geometry of its own"
    - "A class request (three-compartment boxes, association/aggregation/composition/generalization/realization/dependency via committed glyph workarounds, stereotypes via guillemets) yields a rendered PNG that passes the full validate→render→verify loop (UML-02)"
    - "Aggregation/composition diamonds are overlaid on the OWNER end while the connector still binds to the box rectangle (endpoints stay anchored), and no illegal arrowhead token appears in the authored JSON"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/class.md"
      provides: "UML class diagram-type recipe (3-compartment boxes, relationship glyph workarounds, stereotypes)"
      contains: "relationship-endpoint"
      min_lines: 55
    - path: ".claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw"
      provides: "Canonical class example source JSON (3-compartment boxes + composed glyph relationships + guillemets)"
      contains: "diamond"
    - path: ".claude/agents/excalidraw/examples/class_order_domain.png"
      provides: "Rendered canonical class PNG sibling that passes the full loop"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/class.md"
      to: ".claude/agents/excalidraw/kb/relationship-endpoint.md"
      via: "@-reference to the shared endpoint-glyph primitive"
      pattern: "relationship-endpoint"
    - from: ".claude/agents/excalidraw/diagram-types/class.md"
      to: ".claude/agents/excalidraw/diagram-types/notation-conventions.md"
      via: "@-reference to the locked legal-encoding table"
      pattern: "notation-conventions"
---

<objective>
Author the `diagram-types/class.md` UML class diagram-type recipe and its canonical example
pair (`class_order_domain.excalidraw` + rendered `class_order_domain.png`), satisfying
UML-02. The recipe composes the locked compartmented-box geometry (extended to THREE
compartments: title + attributes + methods), the new shared `kb/relationship-endpoint.md`
glyph primitive (from Plan 01), and the locked `notation-conventions.md` table — all by
`@`-reference, re-deriving no geometry. The example must pass the full
validate→render→verify loop.

Purpose: Class is the second compartmented-relationship type this phase ships, reusing the
SAME shared endpoint-glyph primitive as ER ("done exactly once"). It exercises the full
relationship glyph set — association, dependency, generalization (filled triangle),
realization (dashed triangle), aggregation (white diamond), composition (solid diamond) —
plus stereotypes via `«»` guillemets, with the composition-glyph-over-anchored-connector
construction (Pattern 3 / Pitfall 2) that keeps endpoints anchored.

Output: `diagram-types/class.md` recipe; `examples_excalidraw/class_order_domain.excalidraw`
source; `examples/class_order_domain.png` rendered sibling that passes the loop. (The
resolver row in README.md is wired in Plan 04, AFTER both examples pass — EX-01/EX-03 /
Pitfall 6.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-er-class/07-RESEARCH.md
@.planning/phases/07-er-class/07-PATTERNS.md
@.planning/phases/07-er-class/07-01-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (excluded from drift verification — they are new):
- New file: `.claude/agents/excalidraw/diagram-types/class.md` (class type recipe)
- New file: `.claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw` (canonical source)
- New file: `.claude/agents/excalidraw/examples/class_order_domain.png` (rendered sibling)
- New PNG sibling may produce `class_order_domain.verifier-report.json` (verifier artifact)

Depends on these symbols produced earlier in this phase (Plan 01 — already authored):
- `.claude/agents/excalidraw/kb/relationship-endpoint.md` — the shared endpoint-glyph primitive `class.md` @-references (diamond/triangle/dashed geometry + grouping mechanics)
- (optional, decision-gated) arrowhead-enum deny-list in `excalidraw_validator.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author diagram-types/class.md recipe (compose primitives by @-reference)</name>
  <files>.claude/agents/excalidraw/diagram-types/class.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/snowflake-schema.md
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
    - .claude/agents/excalidraw/diagram-types/README.md
    - .planning/phases/07-er-class/07-RESEARCH.md
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>Create `diagram-types/class.md` mirroring `snowflake-schema.md`'s section structure: a Layer-header one-liner, "Purpose", "How to draw it", "Composes (primitive layer)", and "Ground truth". The Layer-header line MUST be the analog one-liner adapted for class: "> Layer: TYPE recipe. Composes primitives from [`../kb/`](../kb/README.md) and the shared [`compartmented-box.md`](./compartmented-box.md) construction; does not re-derive their geometry. Indexed in [`README.md`](./README.md)." In "Purpose", state that a UML class diagram models classes (name + attributes + operations) and the structural relationships among them. In "How to draw it": (1) state class boxes ARE the compartmented-box construction with THREE compartments (title + attributes + methods), which is the same locked recipe as ER/star plus ONE extra full-width `line` divider between the attributes and methods compartments, landing on a row boundary `box.y + 40 + 20*k` per `@./compartmented-box.md` ("Between-row dividers"). All offsets (header 40, row pitch 20, left-pad 12, fontSize 16, box-width/height rules) are reused VERBATIM — do NOT re-derive. (2) Attribute and method rows are per-row monospace texts (`fontFamily:3`, `fontSize:16`), one `text` per row, NEVER a multi-line block; visibility uses ASCII `+ - #` (never lock/key emoji, Pitfall 5). (3) Stereotypes are authored with `«»` guillemets (e.g. `«interface»`) as a monospace text — these are NOT emoji and are safe under `fontFamily:3` (Assumption A1; if they tofu in render, fall back to ASCII `<<interface>>`). (4) The relationship glyph set comes ONLY from `@./notation-conventions.md` + `@../kb/relationship-endpoint.md`: association=`endArrowhead:"arrow"` solid; dependency=`arrow`+`strokeStyle:"dashed"`; generalization=`endArrowhead:"triangle"` FILLED solid (house convention — surface to the user that inheritance is shown filled because Excalidraw lacks a hollow head); realization=`triangle`+`dashed`; aggregation=a ~14px white-fill (`backgroundColor:"#ffffff"`) composed `diamond` grouped at the OWNER end; composition=a ~14px solid-fill (`backgroundColor:"#1e1e1e"`) composed `diamond` grouped at the owner end. (5) Include the composition-glyph-over-anchored-connector rule (Pattern 3 / Pitfall 2): the connector `arrow` binds to the box RECTANGLE (so `check_arrow_endpoint_unanchored` stays green); the diamond is a SEPARATE ~14px element overlaid at the owner end and added to the connector's `groupIds` so it travels with it — do NOT terminate the connector at the diamond. (6) Binding-rules block: every connector `elbowed:true`, `roundness:null`, orthogonal points; legal arrowhead tokens are ONLY `arrow|bar|dot|triangle|null` (crowsfoot/diamond/hollow/open render silently as bare lines); bind via `startBinding`/`endBinding` to box rectangle ids. In "Composes (primitive layer)", `@`-reference AT LEAST: `@./compartmented-box.md` (offsets + extra divider verbatim), `@../kb/relationship-endpoint.md` (diamond/triangle/dashed glyph geometry + grouping mechanics), and `@./notation-conventions.md` (legal-encoding table + stereotype/guillemet convention). In "Ground truth", point at `../examples/class_order_domain.png`. Place NO pixel/coordinate math in this file.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && grep -q "Layer: TYPE recipe" .claude/agents/excalidraw/diagram-types/class.md && grep -q "relationship-endpoint" .claude/agents/excalidraw/diagram-types/class.md && grep -q "compartmented-box" .claude/agents/excalidraw/diagram-types/class.md && grep -q "notation-conventions" .claude/agents/excalidraw/diagram-types/class.md && grep -Eq 'aggregation|composition' .claude/agents/excalidraw/diagram-types/class.md && grep -Eq 'triangle' .claude/agents/excalidraw/diagram-types/class.md && grep -q '«' .claude/agents/excalidraw/diagram-types/class.md && grep -q "class_order_domain.png" .claude/agents/excalidraw/diagram-types/class.md && test $(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/diagram-types/class.md) -ge 55 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/class.md` exists with the Layer-header one-liner, Purpose, How to draw it, Composes (primitive layer), and Ground truth sections.
    - The Composes section `@`-references all three of: `@./compartmented-box.md`, `@../kb/relationship-endpoint.md`, `@./notation-conventions.md`.
    - The recipe documents the three-compartment box (extra divider at `box.y + 40 + 20*k`), the full relationship glyph set (association/dependency/generalization filled-triangle/realization dashed-triangle/aggregation white-diamond/composition solid-diamond), and stereotypes via `«»` guillemets.
    - The recipe states the composition-glyph-over-anchored-connector rule: connector binds to box rectangle, diamond overlaid at owner end and added to connector groupIds (Pitfall 2).
    - The recipe states the five legal arrowhead tokens and names crowsfoot/diamond/hollow as illegal; the file contains NO coordinate/pixel math and has at least 55 non-blank lines.
  </acceptance_criteria>
  <done>`class.md` exists, composes the locked primitives by @-reference exactly like snowflake-schema.md, documents the full glyph set + stereotypes + anchored-glyph rule, and carries no re-derived geometry.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the canonical class_order_domain.excalidraw and render it through the full loop</name>
  <files>.claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw, .claude/agents/excalidraw/examples/class_order_domain.png</files>
  <read_first>
    - .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw
    - .claude/agents/excalidraw/diagram-types/class.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md
    - .claude/agents/excalidraw/scripts/render/validate_and_render.sh
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>Author `examples_excalidraw/class_order_domain.excalidraw` by copying the top-level envelope from `snowflake_schema.excalidraw` and building a small order-management class model that exercises the relationship glyph set. Include at least 4 classes — e.g. `Order`, `OrderLine`, `Customer`, and an interface `«Payable»` (or a `PaymentMethod` abstraction) — so the example shows: a generalization (filled `triangle`), a realization (`triangle`+`dashed`) to the `«interface»`, a composition (solid `diamond`) Order→OrderLine, and an aggregation (white `diamond`) for an owned-but-shared part, plus a plain association (`arrow`). Each class box is the locked compartmented-box construction with THREE compartments: sharp `rectangle` (`roundness:null`, `roughness:0`), a full-width header `line` divider at `box.y+40`, a SECOND full-width `line` divider between attributes and methods at `box.y + 40 + 20*k` (k = number of attribute rows), one title `text` bound via `containerId`, attribute rows and method rows as per-row monospace texts (one `text` each, NEVER multi-line). Use ASCII visibility markers (`+`, `-`, `#`) on members. Add at least one `«interface»` stereotype text in monospace guillemets. Size each box width to the longest row (`len*0.6*16` rounded UP to 20-grid) so `text_overflow_static` passes; all elements of a box share ONE `groupIds` id. For aggregation/composition: the connector `arrow` binds to the box RECTANGLE ids (`startBinding`/`endBinding`, gap 4) and the ~14px `diamond` (width:14,height:14, `roundness:null`, white `#ffffff` for aggregation / solid `#1e1e1e` for composition, `fillStyle:"solid"`) is a SEPARATE element overlaid at the OWNER end sharing the connector's `groupIds` id — the arrow itself still ends on the box border (Pitfall 2). For generalization/realization, set `endArrowhead:"triangle"` (realization adds `strokeStyle:"dashed"`). Every connector is `elbowed:true`, `roundness:null`, orthogonal points. Use ONLY legal arrowhead tokens `arrow|bar|dot|triangle|null` — encode aggregation/composition via the composed diamond, NOT an illegal `diamond` arrowhead token. Keep coordinates on the 20-grid (left-pad 12 excepted). Do NOT imitate the legacy `star_schema.excalidraw`. Then render and verify through the FULL loop: run `validate_and_render.sh` to produce `class_order_domain.png`; fix any structural verifier findings (overflow, unanchored-arrow, not-elbow, raw-emoji, non-monospace) by mutating the JSON and re-rendering up to the 3-iteration cap. The PNG must be produced and the structural verifier must return no errors before this task is done; the visual/EX-03 gate is Task 3.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && python3 .claude/agents/excalidraw/scripts/render/excalidraw_validator.py .claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw && python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw && test -f .claude/agents/excalidraw/examples/class_order_domain.png && python3 -c "import json; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw')); els=d['elements']; heads=[el.get(k) for el in els for k in ('startArrowhead','endArrowhead') if k in el]; legal={'arrow','bar','dot','triangle',None}; bad=[h for h in heads if h not in legal]; tri=any(el.get('endArrowhead')=='triangle' for el in els); dia=any(el.get('type')=='diamond' for el in els); print('BAD',bad,'tri',tri,'dia',dia) or exit(0 if (not bad and tri and dia) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `examples_excalidraw/class_order_domain.excalidraw` exists with at least 4 grouped THREE-compartment class boxes (each: 1 rectangle + 2 line dividers + bound title text + per-row monospace attribute/method texts, all sharing one groupIds id).
    - At least one `«...»` guillemet stereotype text is present; member rows use ASCII visibility markers (`+`/`-`/`#`), no key/lock emoji.
    - The validator exits 0 and the structural verifier reports no errors; every arrowhead value is in `{arrow,bar,dot,triangle,null}` (no illegal token).
    - At least one connector uses `endArrowhead:"triangle"` (generalization/realization) and at least one `diamond` element (width/height 14, white or solid fill) is overlaid at an owner end and shares its connector's groupIds (aggregation/composition).
    - The sibling `examples/class_order_domain.png` exists (rendered through `validate_and_render.sh`).
  </acceptance_criteria>
  <done>The canonical class source passes the validator + structural verifier with only legal arrowheads, three-compartment boxes, guillemet stereotypes, and composed diamond/triangle glyphs on anchored connectors; the rendered PNG sibling exists — ready for the visual EX-03 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: EX-03 visual verification of the class canonical example</name>
  <what-built>A canonical UML class example `examples_excalidraw/class_order_domain.excalidraw` and its rendered sibling `examples/class_order_domain.png`: at least four THREE-compartment class boxes (Order / OrderLine / Customer / «interface»), each with a title, attribute, and method compartment, ASCII visibility markers, and at least one `«»` stereotype. Relationships use the committed glyph workarounds — filled triangle (generalization), dashed triangle (realization), white diamond (aggregation), solid diamond (composition), plus a plain association arrow. The validator and structural verifier already pass with only legal arrowhead tokens (Task 2).</what-built>
  <how-to-verify>
    1. Open `.claude/agents/excalidraw/examples/class_order_domain.png`.
    2. Confirm each class box renders as a sharp THREE-compartment box (title over attributes over methods) with full-width dividers and monospace rows that do not overflow the border.
    3. Confirm the `«interface»` (or other) stereotype renders with readable guillemets `«»` — NOT tofu/missing-glyph boxes. (If they tofu, the fix is ASCII `<<interface>>` — report it.)
    4. Confirm the relationship glyphs are visually distinct and correct: generalization shows a FILLED triangle head; realization shows a triangle on a DASHED line; aggregation shows a WHITE (hollow) diamond at the owner end; composition shows a SOLID (filled) diamond at the owner end; plain association is an arrow.
    5. Confirm each composed diamond sits at the owner end ATTACHED to its connector (not floating detached), and no relationship that should carry a glyph renders as a plain bare line (the silent-illegal-token failure mode).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the PNG shows three-compartment class boxes with non-overflowing monospace rows and readable `«»` stereotypes.
    - Operator confirms generalization (filled triangle), realization (dashed triangle), aggregation (white diamond), and composition (solid diamond) are each visually distinguishable (UML-02 / SC-3).
    - Operator confirms no composed diamond is detached and no intended-glyph relationship renders as a plain bare line.
  </acceptance_criteria>
  <resume-signal>Type "approved" to satisfy the class EX-03 gate, or describe the visual defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- `excalidraw_validator.py` and `verifier_structural.py` both return clean on `class_order_domain.excalidraw` (structural automated half).
- All arrowhead tokens in the class JSON are within the legal set; aggregation/composition are composed diamonds (not illegal `diamond` arrowhead tokens), overlaid on anchored connectors.
- `class.md` composes `@./compartmented-box.md`, `@../kb/relationship-endpoint.md`, and `@./notation-conventions.md` by reference and re-derives no geometry.
- Operator approves the rendered PNG (visual EX-03 half) before Plan 04 wires the resolver row.
</verification>

<success_criteria>
- UML-02 foundation: `diagram-types/class.md` exists and a class request (three-compartment boxes, glyph-workaround relationships, guillemet stereotypes) yields a PNG that passes the full loop.
- The canonical class example pair exists and passes the structural + visual loop (EX-01/EX-03 for class), using only legal arrowhead tokens (SC-1) with endpoints anchored despite composed glyphs (SC-3).
</success_criteria>

<output>
Create `.planning/phases/07-er-class/07-03-SUMMARY.md` when done, recording the example
subject (`class_order_domain`), the structural-verifier result, the EX-03 visual approval,
and (per Assumption A1) whether `«»` guillemets rendered correctly or fell back to ASCII.
The resolver row + REQUIREMENTS UML-02 tick are wired in Plan 04.
</output>
