---
phase: 07-er-class
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/kb/relationship-endpoint.md
  - .claude/agents/excalidraw/scripts/render/excalidraw_validator.py
autonomous: false
requirements: [DM-02, UML-02]
must_haves:
  truths:
    - "A single kb/relationship-endpoint.md primitive pins the deferred glyph pixel geometry once, ready for both er.md and class.md to @-reference"
    - "Every relationship-endpoint encoding in the primitive uses only legal arrowhead tokens (arrow|bar|dot|triangle|null) plus composed line/diamond/ellipse glyphs or textual multiplicity"
    - "The arrowhead-legality decision is resolved explicitly; if adopted, excalidraw_validator.py rejects any start/endArrowhead outside the five legal tokens"
  artifacts:
    - path: ".claude/agents/excalidraw/kb/relationship-endpoint.md"
      provides: "Shared relationship-endpoint glyph primitive (diamond/ellipse/triangle/bar/dot geometry + grouping mechanics)"
      contains: "Used by types: er, class"
      min_lines: 40
    - path: ".claude/agents/excalidraw/scripts/render/excalidraw_validator.py"
      provides: "Phase-1 validator, optionally extended with arrowhead-enum deny-list"
  key_links:
    - from: ".claude/agents/excalidraw/kb/relationship-endpoint.md"
      to: ".claude/agents/excalidraw/diagram-types/notation-conventions.md"
      via: "references the locked legal-encoding table"
      pattern: "notation-conventions"
---

<objective>
Build the shared relationship-endpoint-glyph convention ONCE as a new primitive
(`kb/relationship-endpoint.md`) that pins the glyph pixel geometry `notation-conventions.md`
deferred to this phase, and resolve the arrowhead-legality enforcement decision (Open
Question 1) — optionally adding a deny-list check to the Phase-1 validator so illegal
arrowhead tokens become a loud authoring-time error instead of a silent bare-line render.

Purpose: ER and class are co-located so the endpoint-glyph work is done exactly once. This
plan produces the single source both type files (`er.md`, `class.md`) will `@`-reference,
plus the only automated guard for Success Criterion 1 ("no illegal arrowhead value appears
in any authored JSON").

Output: `kb/relationship-endpoint.md` primitive; a resolved decision on the validator check
(and the validator edit if adopted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-er-class/07-RESEARCH.md
@.planning/phases/07-er-class/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS phase (so source-grounding excludes them from drift verification):
- New file: `.claude/agents/excalidraw/kb/relationship-endpoint.md` (primitive)
- New file: `.claude/agents/excalidraw/diagram-types/er.md` (Plan 02)
- New file: `.claude/agents/excalidraw/diagram-types/class.md` (Plan 03)
- New files: `.claude/agents/excalidraw/examples_excalidraw/er_<subject>.excalidraw` + `.claude/agents/excalidraw/examples/er_<subject>.png` (Plan 02)
- New files: `.claude/agents/excalidraw/examples_excalidraw/class_<subject>.excalidraw` + `.claude/agents/excalidraw/examples/class_<subject>.png` (Plan 03)
- New markdown back-ref line `> Used by types: er, class` in `kb/relationship-endpoint.md`
- New (optional) function/loop in `excalidraw_validator.py`: arrowhead-enum deny-list appending to the existing `errors` list
- New resolver-table cell values in `diagram-types/README.md` for the `er` and `class` rows (Plans 02/03)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide whether to add the arrowhead-enum validator check</name>
  <decision>Add a deny-list check to scripts/render/excalidraw_validator.py that rejects any element whose startArrowhead/endArrowhead is not in {arrow, bar, dot, triangle, null}, OR leave the validator untouched and rely on KB discipline + the verifier's visual review for Success Criterion 1.</decision>
  <context>The v1.0 validator is nominally FROZEN (all v1.1 work is additive). RESEARCH (Open Question 1, Assumption A3, Pitfall 1) verified that NEITHER excalidraw_validator.py NOR verifier_structural.py inspects arrowhead-token legality. An illegal token (crowsfoot, diamond, hollow) renders SILENTLY as a plain line in 0.17.3 — no error, no warning. Success Criterion 1 of this phase ("no illegal arrowhead value appears in any authored JSON") therefore currently has ZERO automated guard. The proposed check is purely additive: it rejects values that were always illegal, appending to the existing `errors` list (validator lines 17-69 need no other change). This is a frozen-file edit, so it requires explicit sign-off (A3, MEDIUM risk).</context>
  <options>
    <option id="add-check">
      <name>Add the arrowhead-enum deny-list check (RECOMMENDED by RESEARCH)</name>
      <pros>Converts the silent-render failure into a loud Phase-1 error; gives SC-1 an automated guard; rejects only what was always illegal (additive hardening); sub-second cost.</pros>
      <cons>Touches the nominally-frozen excalidraw_validator.py; must be reviewed as an exception to "validator FROZEN".</cons>
    </option>
    <option id="no-check">
      <name>Leave the validator untouched; rely on KB discipline + verifier visual review</name>
      <pros>Honors "validator FROZEN" with zero exception.</pros>
      <cons>SC-1 has NO automated guard — illegal tokens can ship silently as bare lines and are caught only if the human visual review notices an indistinguishable relationship. This MUST be recorded explicitly in the SUMMARY.</cons>
    </option>
  </options>
  <read_first>
    - .claude/agents/excalidraw/scripts/render/excalidraw_validator.py
    - .planning/phases/07-er-class/07-RESEARCH.md
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - The chosen option is recorded as a decision in the plan SUMMARY with the rationale.
    - If "no-check" is chosen, the SUMMARY explicitly states that SC-1 has no automated guard and relies on KB discipline + verifier visual review.
  </acceptance_criteria>
  <resume-signal>Select: add-check or no-check</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the arrowhead-enum validator check (only if Task 1 = add-check)</name>
  <files>.claude/agents/excalidraw/scripts/render/excalidraw_validator.py</files>
  <read_first>
    - .claude/agents/excalidraw/scripts/render/excalidraw_validator.py
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>If Task 1 resolved to "no-check", SKIP this task entirely and note the skip in the SUMMARY. If Task 1 resolved to "add-check": add a new deny-list loop in validate_excalidraw() that mirrors the existing "label" deny-list (validator lines 30-37). Iterate `elements`; the legal set is the five values {"arrow", "bar", "dot", "triangle", None}. For each element, inspect both the `startArrowhead` and `endArrowhead` keys: a key that is PRESENT and whose value is not in the legal set is an illegal token. Collect each offending (element id, key, value) and append a single descriptive message to the existing `errors` list (e.g. naming the element id, the offending key, the illegal value, and the legal set). Do NOT change the report/exit-code machinery (lines 54-69) — appending to `errors` already drives the non-zero exit. Treat a missing key and an explicit None/null value as legal (no head). Place the new loop after the existing "label" check block.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && python3 -c "import json,tempfile,subprocess,os; d={'type':'excalidraw','version':2,'elements':[{'type':'arrow','id':'bad','endArrowhead':'crowsfoot'}]}; f=tempfile.NamedTemporaryFile('w',suffix='.excalidraw',delete=False); json.dump(d,f); f.close(); r=subprocess.run(['python3','.claude/agents/excalidraw/scripts/render/excalidraw_validator.py',f.name]); os.unlink(f.name); exit(0 if r.returncode==1 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - (add-check) Running the validator on a file containing endArrowhead:"crowsfoot" exits non-zero (1) and the report lists the offending element id and the illegal value "crowsfoot".
    - (add-check) Running the validator on a file whose arrows use only {arrow,bar,dot,triangle,null} (or omit the keys) does NOT add an arrowhead error.
    - (no-check) Task skipped; SUMMARY records the skip and the SC-1 coverage caveat.
  </acceptance_criteria>
  <done>Validator either rejects illegal arrowhead tokens with a non-zero exit (add-check), or is provably untouched with the caveat recorded (no-check).</done>
</task>

<task type="auto">
  <name>Task 3: Author kb/relationship-endpoint.md primitive (pin deferred glyph geometry)</name>
  <files>.claude/agents/excalidraw/kb/relationship-endpoint.md</files>
  <read_first>
    - .claude/agents/excalidraw/kb/tree-hierarchy.md
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md
    - .planning/phases/07-er-class/07-RESEARCH.md
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>Create kb/relationship-endpoint.md as a PRIMITIVE-layer file mirroring tree-hierarchy.md's section layout EXACTLY: a back-ref header line, a "When to use" section, a "Geometry" section, a "JSON skeleton" section, a "See in examples" section, and a "Notes" section. The back-ref header line MUST be the literal markdown `> Used by types: er, class` (both type files consume this primitive — the literal "done exactly once" link). In "Geometry", PIN the deferred glyph pixel numbers (deferred by notation-conventions.md lines 55-58): composed `diamond` size width:14 height:14; the ellipse glyph size for the ER "zero/optional" alternative; the textual-multiplicity label offset from the endpoint; and the rule that any composed glyph is OVERLAID on the OWNER end and added to the connector's `groupIds` so it travels with the connector while the arrow itself still binds to the box rectangle border (Pattern 3 / Pitfall 2). Keep coordinates on the 20-grid where applicable. In "JSON skeleton", provide copy-paste blocks for each legal endpoint encoding taken verbatim from RESEARCH Code Examples: ER "one" = endArrowhead "bar"; ER "zero/optional" = endArrowhead "dot" or a small composed ellipse; ER "many" = a free textual multiplicity text "0..*"/"1..*" placed OUTSIDE every box bbox; aggregation = white-fill (backgroundColor #ffffff) ~14px diamond grouped at owner end; composition = solid-fill (backgroundColor #1e1e1e) ~14px diamond grouped at owner end; generalization = endArrowhead "triangle" FILLED solid stroke; realization = endArrowhead "triangle" + strokeStyle "dashed"; association = endArrowhead "arrow"; dependency = endArrowhead "arrow" + strokeStyle "dashed". State explicitly that the ONLY legal arrowhead tokens are arrow|bar|dot|triangle|null and that crowsfoot/diamond/hollow/open render silently as bare lines. Do NOT re-derive compartmented-box offsets here — reference @../diagram-types/compartmented-box.md and @../diagram-types/notation-conventions.md for the convention table. In "See in examples", leave placeholders pointing at ../examples/er_<subject>.png and ../examples/class_<subject>.png (the canonical pairs authored in Plans 02-03; fill the real names there).</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && grep -q "Used by types: er, class" .claude/agents/excalidraw/kb/relationship-endpoint.md && grep -Eq 'arrow.*bar.*dot.*triangle' .claude/agents/excalidraw/kb/relationship-endpoint.md && grep -q "notation-conventions" .claude/agents/excalidraw/kb/relationship-endpoint.md && test $(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/kb/relationship-endpoint.md) -ge 40 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - kb/relationship-endpoint.md exists and contains the literal line "> Used by types: er, class".
    - File lists the five legal arrowhead tokens arrow|bar|dot|triangle|null and names crowsfoot/diamond/hollow as illegal (silent bare line).
    - File pins diamond width:14/height:14, ellipse glyph size, and the multiplicity-label offset, and states the glyph-overlaid-on-anchored-connector rule (arrow binds to box rectangle; diamond joins connector groupIds).
    - File references @../diagram-types/notation-conventions.md and @../diagram-types/compartmented-box.md rather than re-deriving the convention table or box offsets.
    - File has at least 40 non-blank lines and follows tree-hierarchy.md's section layout (When to use / Geometry / JSON skeleton / See in examples / Notes).
  </acceptance_criteria>
  <done>The shared endpoint-glyph primitive exists with the bidirectional back-ref, the pinned pixel geometry, and the legal-token callout — ready for er.md and class.md to @-reference.</done>
</task>

</tasks>

<verification>
- Run the structural-only gate on any test JSON: it remains green (no regression in verifier_structural.py — frozen, untouched).
- If add-check adopted: validator rejects an illegal arrowhead token with exit code 1; accepts legal-only JSON.
- kb/relationship-endpoint.md carries the `> Used by types: er, class` back-ref and pins the deferred glyph geometry once.
</verification>

<success_criteria>
- A single shared endpoint-glyph primitive exists; geometry is pinned ONCE; both later type files can compose it by @-reference (SC-1 foundation).
- The arrowhead-legality decision is resolved explicitly and recorded; SC-1 either gains an automated guard or the caveat is documented.
</success_criteria>

<output>
Create `.planning/phases/07-er-class/07-01-SUMMARY.md` when done, recording the Task-1
decision (add-check vs no-check) and its SC-1 coverage implication.
</output>
