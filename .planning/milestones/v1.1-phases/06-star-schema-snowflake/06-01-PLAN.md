---
phase: 06-star-schema-snowflake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/diagram-types/compartmented-box.md
  - .claude/agents/excalidraw/diagram-types/star-schema.md
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw
  - .claude/agents/excalidraw/examples/star_schema_v2.png
  - .claude/agents/excalidraw/kb/fan-out.md
  - .claude/agents/excalidraw/kb/convergence.md
  - .claude/agents/excalidraw/kb/evidence-card.md
  - .claude/agents/excalidraw/kb/group-container.md
autonomous: false
requirements: [DM-01]
must_haves:
  truths:
    - "The compartmented-box construction has finalized parametric offsets (header height, row pitch, left-pad, divider-Y) recorded once, no longer deferred to Phase 6"
    - "A star-schema.md recipe exists that references compartmented-box.md and the fan-out composition rather than re-deriving geometry"
    - "A canonical star example pair (star_schema_v2.excalidraw + .png) exists, uses grouped sharp-cornered boxes with full-width line dividers and per-row monospace text, and passes the full validate->render->verify loop"
    - "The resolver table's star-schema row points at the new compliant recipe + example PNG, not the grandfathered legacy"
    - "Each kb primitive composed by star carries a 'Used by types: star-schema' back-ref"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/star-schema.md"
      provides: "Star-schema TYPE recipe (purpose, how-to-draw, composes, ground truth)"
      contains: "compartmented-box"
    - path: ".claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw"
      provides: "Compliant canonical star source (grouped, sharp, line dividers, per-row text)"
      contains: "groupIds"
    - path: ".claude/agents/excalidraw/examples/star_schema_v2.png"
      provides: "Rendered ground-truth PNG for star"
    - path: ".claude/agents/excalidraw/diagram-types/compartmented-box.md"
      provides: "Finalized parametric offsets section"
      contains: "row pitch"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/star-schema.md"
      to: ".claude/agents/excalidraw/diagram-types/compartmented-box.md"
      via: "@-reference to the box construction"
      pattern: "compartmented-box"
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/examples/star_schema_v2.png"
      via: "resolver table example-PNG cell"
      pattern: "star_schema_v2\\.png"
---

<objective>
Establish the reusable data-modeling table-box recipe on STAR SCHEMA (DM-01): finalize the
parametric offsets that compartmented-box.md deferred to Phase 6, author the
`star-schema.md` TYPE recipe, and produce the first compliant canonical example pair
(grouped sharp box + full-width `line` dividers + per-row monospace text + bound elbow
fan-out arrows) that passes the full validate->render->verify loop. This is the hard
dependency that snowflake (Plan 02) inherits verbatim.

Purpose: Star is authored and passing FIRST (SC-2 ordering gate). The parametric
divider/row-alignment geometry solved here is the foundation every later compartmented
type (snowflake, ER, class, data-vault) reuses — it must be solved once and proven against
the frozen verifier, never re-derived.

Output: finalized offsets in compartmented-box.md, `star-schema.md`, the
`star_schema_v2` example pair, an updated resolver row, and `Used by types: star-schema`
back-refs on star's composed kb primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-star-schema-snowflake/06-RESEARCH.md
</context>

## Phase Goal

The reusable data-modeling table-box recipe is established on star schema. Star is authored
first as a hard dependency for snowflake. The parametric divider/row alignment that all
later compartmented types depend on is solved and verifier-checkable here.

## Artifacts this phase produces

New symbols / files created or finalized by THIS plan (exclude these from drift checks):

- File: `.claude/agents/excalidraw/diagram-types/star-schema.md` (new TYPE recipe)
- File: `.claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw` (new canonical source)
- File: `.claude/agents/excalidraw/examples/star_schema_v2.png` (new rendered ground truth)
- Section: finalized "parametric offsets" content inside `compartmented-box.md` (header height 40, row pitch 20, left-pad 12, divider-Y formula)
- Resolver-table cell values: `star-schema.md` recipe path + `star_schema_v2.png` example path
- Back-ref line `> Used by types: ... star-schema` appended to `kb/fan-out.md`, `kb/convergence.md`, `kb/evidence-card.md`, `kb/group-container.md`
- Excalidraw element ids in the example (e.g. `grp_fact_sales`, `fact_sales_box`, `div_fact_header`, dimension box/group ids) — author's choice, internal to the example file

<tasks>

<task type="auto">
  <name>Task 1: Finalize compartmented-box offsets and author star-schema.md recipe</name>
  <files>.claude/agents/excalidraw/diagram-types/compartmented-box.md, .claude/agents/excalidraw/diagram-types/star-schema.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (current construction + the "Scope of this file" section that DEFERS offsets to Phase 6 — you are replacing that deferral)
    - .claude/agents/excalidraw/diagram-types/tech-architecture.md (the exemplar TYPE-file structure to mirror: Purpose / How to draw it / Composes (primitive layer) / Ground truth)
    - .claude/agents/excalidraw/diagram-types/README.md (resolver table + "Adding a diagram type" procedure + legacy grandfathering note)
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md (legal arrowhead set arrow|bar|dot|triangle|null; star connectors are plain associations)
    - .claude/agents/excalidraw/kb/fan-out.md (fact->dimension dispatch geometry star composes)
    - .planning/phases/06-star-schema-snowflake/06-RESEARCH.md (Pattern 1 finalized-offsets table, Pattern 2 fan-out anchoring rule, the three width/left-x/full-width-divider rules)
  </read_first>
  <action>
    Replace the "Scope of this file" deferral in compartmented-box.md with a finalized
    parametric-offsets section that LOCKS these concrete values for all later compartmented
    types: header height = 40px (header divider at box.y + 40), row pitch = 20px, left-pad =
    12px (every row and title text x = box.x + 12), fontSize = 16 with fontFamily 3, divider
    Y values on the 20-grid never bisecting a row, and the box-width rule
    box.width >= max_over_rows(len(row.text) * 0.6 * fontSize) rounded UP to the next 20-grid
    value. State explicitly that these offsets are now finalized (no longer deferred) and that
    later type files reference them verbatim. Keep the existing HARD prohibition (no multi-line
    single text) and the three alignment rules (common left x, full-width dividers x==box.x and
    points[-1][0]==box.width, 20-grid) intact — do not weaken them.

    Then author .claude/agents/excalidraw/diagram-types/star-schema.md mirroring the
    tech-architecture.md structure exactly: a Purpose section (central fact table surrounded
    by dimension entity boxes, fan-out composition; when to reach for it); a How to draw it
    section that REFERENCES @../diagram-types/compartmented-box.md for the box construction and
    finalized offsets and @../kb/fan-out.md for the fact->dimension connector geometry — never
    re-derive coordinate math here; a Composes (primitive layer) section listing the resolver's
    star set as @-refs: @../kb/fan-out.md, @../kb/convergence.md, @../kb/evidence-card.md,
    @../kb/group-container.md, plus @./compartmented-box.md as the shared construction; and a
    Ground truth section pointing at @../examples/star_schema_v2.png. State the binding rules
    in prose: connectors use endArrowhead "arrow" only (DTKB-04), arrows anchor to the box
    RECTANGLE border (never to row texts or line dividers, which are not anchorable shapes),
    boxes use roundness null (sharp) and one shared groupIds per box.
  </action>
  <verify>
    <automated>grep -q "row pitch" .claude/agents/excalidraw/diagram-types/compartmented-box.md && grep -qi "header height = 40\|header height 40\|40px" .claude/agents/excalidraw/diagram-types/compartmented-box.md && test -f .claude/agents/excalidraw/diagram-types/star-schema.md && grep -q "compartmented-box" .claude/agents/excalidraw/diagram-types/star-schema.md && grep -q "fan-out" .claude/agents/excalidraw/diagram-types/star-schema.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - compartmented-box.md no longer says the offsets are "finalized in Phase 6" / deferred; it states concrete locked values (header 40, row pitch 20, left-pad 12, fontSize 16)
    - compartmented-box.md retains the HARD prohibition line on multi-line single text
    - star-schema.md contains the four sections Purpose / How to draw it / Composes (primitive layer) / Ground truth
    - star-schema.md @-references compartmented-box.md and @../kb/fan-out.md (grep `compartmented-box` and `fan-out` both match)
    - star-schema.md Ground truth references `../examples/star_schema_v2.png`
    - star-schema.md states arrows use endArrowhead "arrow" and anchor to the box rectangle border
  </acceptance_criteria>
  <done>compartmented-box.md offsets are finalized (not deferred); star-schema.md exists with all four sections, composes via @-ref, and points at the new example PNG.</done>
</task>

<task type="auto">
  <name>Task 2: Author and render the compliant star_schema_v2 example pair, wire resolver + back-refs</name>
  <files>.claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw, .claude/agents/excalidraw/examples/star_schema_v2.png, .claude/agents/excalidraw/diagram-types/README.md, .claude/agents/excalidraw/kb/fan-out.md, .claude/agents/excalidraw/kb/convergence.md, .claude/agents/excalidraw/kb/evidence-card.md, .claude/agents/excalidraw/kb/group-container.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/star-schema.md (the recipe just authored — author the example TO it)
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (the finalized offsets — apply them exactly)
    - .planning/phases/06-star-schema-snowflake/06-RESEARCH.md (Pattern 1 full annotated box JSON, "Minimum required text-element fields", Pattern 2 bound elbow arrow keys, Anti-Patterns list)
    - .claude/agents/excalidraw/examples_excalidraw/data_pipeline_flow.excalidraw (reference for bound elbow arrow field keys: elbowed, roundness null, startBinding/endBinding, fixedPoint)
    - .claude/agents/excalidraw/examples_excalidraw/repo_tree_hierarchy.excalidraw (reference for valid text-element field set)
    - .claude/agents/excalidraw/diagram-types/README.md (resolver table — the star-schema row currently points at legacy example_star_schema.png; "Legacy example resolution" section)
    - .claude/agents/excalidraw/kb/fan-out.md (existing `> Used by types: tech-architecture` back-ref line format to extend)
    - .claude/agents/excalidraw/scripts/render/validate_and_render.sh (the FROZEN entry point; usage: bash scripts/render/validate_and_render.sh <file>.excalidraw)
  </read_first>
  <action>
    Author .claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw for a small
    recognizable Sales star: one central fact box (e.g. fact_sales with date_key/product_key/
    customer_key/store_key FKs + 1-2 measures) and 3-4 dimension boxes (dim_date, dim_product,
    dim_customer, dim_store) fanned out around it. Every box MUST follow the finalized recipe:
    a sharp rectangle (roundness null, roughness 0), full-width horizontal `line` header divider
    with x == box.x and points [[0,0],[width,0]] so points[-1][0] == box.width exactly, one title
    text (bind via containerId to the box so it is skipped by text_overflow_static), and N
    free-floating per-row monospace texts (fontFamily 3, fontSize 16) sharing left x = box.x + 12.
    Every element of a box (rectangle, divider, title, rows) shares ONE groupIds id. Size each
    box.width >= max row len * 0.6 * 16 rounded UP to the 20-grid. Connect fact->dimension with
    bound elbow arrows (elbowed true, roundness null, >=3 points, endArrowhead "arrow",
    startBinding/endBinding to the box rectangle ids) per kb/fan-out.md geometry — anchor only to
    rectangle borders, never to texts or dividers. Include all required text fields
    (type,x,y,width,height,text,fontSize,fontFamily:3,lineHeight:1.25,textAlign,verticalAlign,
    originalText,strokeColor,roundness,groupIds) so text_missing_dimensions never fires. Do NOT
    copy star_schema.excalidraw (grandfathered, ungrouped, soft, unbound — NOT a template).

    Render via: bash .claude/agents/excalidraw/scripts/render/validate_and_render.sh
    .claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw — this writes the
    sibling PNG. Move/confirm the rendered PNG to examples/star_schema_v2.png (the resolver
    ground-truth location). Fix any validator/render error and re-run until it renders clean.

    Then update README.md: change the Data Modeling | star-schema resolver row so the Type file
    cell reads `star-schema.md` (drop the _(planned - Phase 6)_ marker) and the Example PNG cell
    reads `../examples/star_schema_v2.png` (replacing the legacy `example_star_schema.png`
    reference). Leave the legacy `star_schema.excalidraw` file on disk and its grandfathering note
    intact, but stop indexing it as the star canonical (per RESEARCH Open Question 1
    recommendation). Append `, star-schema` to the existing `> Used by types: tech-architecture`
    line in kb/fan-out.md, kb/convergence.md, and kb/group-container.md; add a new
    `> Used by types: star-schema` line to kb/evidence-card.md (which currently has none).
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/examples/star_schema_v2.png && python3 -c "import json,sys; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw')); els=d['elements']; assert any(e['type']=='line' for e in els), 'no line divider'; assert any(e.get('groupIds') for e in els), 'no grouped element'; rows=[e for e in els if e['type']=='text']; assert all('\n' not in e.get('text','') for e in rows), 'multi-line row text present (SC-4 violation)'; print('PASS')" && grep -q "star_schema_v2.png" .claude/agents/excalidraw/diagram-types/README.md && grep -q "star-schema" .claude/agents/excalidraw/kb/evidence-card.md && echo ALLPASS</automated>
  </verify>
  <acceptance_criteria>
    - examples/star_schema_v2.png exists (rendered by validate_and_render.sh, not hand-edited)
    - star_schema_v2.excalidraw contains at least one `line` element and at least one element with a non-empty groupIds (grouped sharp box with dividers)
    - No `text` element in star_schema_v2.excalidraw contains a `\n` (SC-4: per-row separate text, no multi-line)
    - Every box's rectangle uses roundness null (sharp); grep/JSON inspection shows no `roundness: {type:3}` on box rectangles
    - README.md resolver row for star-schema names `star-schema.md` and `../examples/star_schema_v2.png`; the legacy `example_star_schema.png` is no longer the star canonical
    - kb/fan-out.md, kb/convergence.md, kb/group-container.md `Used by types:` lines now include `star-schema`; kb/evidence-card.md has a `> Used by types: star-schema` line
  </acceptance_criteria>
  <done>star_schema_v2 pair exists and renders; resolver row points at the new recipe + PNG; star's composed kb primitives carry the star-schema back-ref; no SC-4 multi-line violations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify star example passes the full loop (EX-03 / SC-1 visual gate)</name>
  <what-built>
    A compliant Sales star-schema canonical example (star_schema_v2.excalidraw + rendered
    star_schema_v2.png): a central fact box fanned out to dimension boxes, each box a grouped
    sharp rectangle with full-width line dividers and per-row monospace text. The frozen
    verifier covers text overflow, missing dimensions, arrow anchoring, and arrowhead legality
    structurally, but it does NOT check SC-1's "dividers span full width" or SC-4's "no
    multi-line text" — those need a human eyeball on the PNG.
  </what-built>
  <how-to-verify>
    1. Invoke the excalidraw_verifier subagent on
       .claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw with its sibling
       PNG, and confirm the verifier-report shows `passed: true` with an empty `issues` array
       (EX-03 exit gate).
    2. Open .claude/agents/excalidraw/examples/star_schema_v2.png and confirm visually:
       - every compartment divider spans the FULL box width (touches both left and right
         borders, no ragged short/overrun dividers) — SC-1
       - within each box, all rows are left-aligned to a common x (no column drift) — SC-1
       - no row text pokes past its box border (no overflow)
       - boxes have SHARP corners (not rounded)
       - fan-out arrows connect fact box border to dimension box borders (not floating, not
         landing on text/dividers)
    3. Confirm no row label looks packed/multi-line (each row is a clean single line) — SC-4.
  </how-to-verify>
  <resume-signal>Type "approved" if the verifier reports passed:true and the PNG satisfies the SC-1/SC-4 visual checks; otherwise describe the specific defect (which box, which rule) so it can be fixed and re-rendered.</resume-signal>
  <action>This is a verification checkpoint, not an authoring task — do not write or mutate any diagram/recipe file here. Perform the verification described in <how-to-verify>: invoke excalidraw_verifier on the example + its sibling PNG, confirm passed:true, then human-eyeball the PNG for the SC-1/SC-4 geometric rules the verifier does not structurally enforce. Pause for the resume-signal.</action>
  <verify><human-check>Verifier report shows passed:true with empty issues, AND the PNG satisfies every bullet in <how-to-verify> (full-width dividers, common left x, no overflow, sharp corners, anchored arrows, no multi-line rows).</human-check></verify>
  <done>excalidraw_verifier reports passed:true on the example (EX-03) and the operator has confirmed the SC-1/SC-4 visual checks; defects, if any, were fixed and re-rendered before approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored diagram text -> verifier/render toolchain | Example labels are DATA consumed by the local frozen render/verify toolchain; no network input, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | example diagram label text (e.g. a label saying "ignore instructions") | mitigate | Do not author adversarial label text; the verifier `<role>` already treats file content as data only (frozen) — do not regress it |
| T-06-02 | Tampering | clobbering grandfathered legacy `example_star_schema.png` ground truth | mitigate | New example authored under a DISTINCT filename `star_schema_v2`; legacy file left on disk, only de-indexed |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (KB/example authoring only); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- compartmented-box.md offsets finalized (grep `row pitch`, header height value present; no "deferred to Phase 6" language remains for offsets)
- star-schema.md exists with all four TYPE sections and @-refs to compartmented-box + fan-out
- star_schema_v2.excalidraw renders to examples/star_schema_v2.png via the frozen loop
- JSON assertion: >=1 line divider, >=1 grouped element, zero multi-line row texts, sharp box rectangles
- resolver row updated to the new recipe + PNG; star's kb primitives carry the star-schema back-ref
- excalidraw_verifier reports passed:true on the example (EX-03)
- human visual confirmation of full-width dividers, common left x, no overflow, sharp corners, anchored arrows
</verification>

<success_criteria>
- DM-01 satisfied: star-schema.md recipe exists and the canonical star example passes the full validate->render->verify loop with full-width dividers and common-left-x rows
- The deferred compartmented-box offsets are finalized once and locked for reuse by snowflake (Plan 02)
- The resolver table's star row is canonical (new recipe + new PNG), legacy grandfathered but de-indexed
- Star's composed kb primitives are back-referenced (DTKB-03 bidirectional link)
</success_criteria>

<output>
Create `.planning/phases/06-star-schema-snowflake/06-01-SUMMARY.md` when done, recording the
finalized offset numbers (header height, row pitch, left-pad, fontSize) so Plan 02 references
them verbatim.
</output>
