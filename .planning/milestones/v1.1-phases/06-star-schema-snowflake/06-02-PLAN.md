---
phase: 06-star-schema-snowflake
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/snowflake-schema.md
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw
  - .claude/agents/excalidraw/examples/snowflake_schema.png
  - .claude/agents/excalidraw/kb/tree-hierarchy.md
  - .claude/agents/excalidraw/kb/linear-pipeline.md
autonomous: false
requirements: [DM-03]
must_haves:
  truths:
    - "A snowflake-schema.md recipe exists that builds on star-schema by normalizing a dimension into a tree-hierarchy of sub-tables, referencing star's finalized box geometry verbatim rather than re-deriving it"
    - "A canonical snowflake example pair (snowflake_schema.excalidraw + .png) exists, reuses the locked compartmented-box offsets, normalizes at least one dimension into sub-tables via tree-hierarchy, and passes the full validate->render->verify loop"
    - "The snowflake example is authored only after the star example passes (SC-2 ordering gate)"
    - "The resolver table's snowflake-schema row points at the new recipe + example PNG"
    - "kb/tree-hierarchy.md and kb/linear-pipeline.md carry a 'Used by types: snowflake-schema' back-ref"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/snowflake-schema.md"
      provides: "Snowflake-schema TYPE recipe building on star + tree-hierarchy normalization"
      contains: "star-schema"
    - path: ".claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw"
      provides: "Compliant canonical snowflake source (star boxes + normalized dimension tree)"
      contains: "groupIds"
    - path: ".claude/agents/excalidraw/examples/snowflake_schema.png"
      provides: "Rendered ground-truth PNG for snowflake"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/snowflake-schema.md"
      to: ".claude/agents/excalidraw/diagram-types/star-schema.md"
      via: "@-reference building on the star recipe + its finalized box geometry"
      pattern: "star-schema"
    - from: ".claude/agents/excalidraw/diagram-types/snowflake-schema.md"
      to: ".claude/agents/excalidraw/kb/tree-hierarchy.md"
      via: "@-reference for normalized dimension sub-table layout"
      pattern: "tree-hierarchy"
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/examples/snowflake_schema.png"
      via: "resolver table example-PNG cell"
      pattern: "snowflake_schema\\.png"
---

<objective>
Extend the star recipe to its normalized variant, SNOWFLAKE (DM-03): author
`snowflake-schema.md` that builds on star (a star where one or more dimensions are
normalized into a tree-hierarchy of sub-tables) referencing star's finalized box geometry
verbatim, and produce a compliant canonical example pair that passes the full
validate->render->verify loop. Snowflake = star's composed set + `kb/tree-hierarchy.md`
normalization; it is authored only AFTER star passes (SC-2 ordering gate).

Purpose: Snowflake is literally star plus normalized dimension sub-tables. By inheriting
star's locked compartmented-box offsets verbatim (Plan 01) and adding only the
tree-hierarchy extension, the two types stay consistent and the box geometry is never
re-derived (the future-drift bug RESEARCH Pitfall 6 warns against).

Output: `snowflake-schema.md`, the `snowflake_schema` example pair, an updated resolver
row, and `Used by types: snowflake-schema` back-refs on tree-hierarchy and linear-pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-star-schema-snowflake/06-RESEARCH.md
@.planning/phases/06-star-schema-snowflake/06-01-SUMMARY.md
</context>

## Phase Goal

The data-modeling table-box recipe established on star schema (Plan 01) is extended to its
normalized snowflake variant. Snowflake is authored only after star is passing; it reuses
star's finalized box geometry verbatim and adds only the normalized dimension tree-hierarchy.

## Artifacts this phase produces

New symbols / files created by THIS plan (exclude these from drift checks):

- File: `.claude/agents/excalidraw/diagram-types/snowflake-schema.md` (new TYPE recipe)
- File: `.claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw` (new canonical source)
- File: `.claude/agents/excalidraw/examples/snowflake_schema.png` (new rendered ground truth)
- Resolver-table cell values: `snowflake-schema.md` recipe path + `snowflake_schema.png` example path
- Back-ref line `> Used by types: snowflake-schema` added to `kb/tree-hierarchy.md` and `kb/linear-pipeline.md`
- Excalidraw element ids in the example (fact/dimension/sub-dimension box + group ids) — author's choice, internal to the example file

<tasks>

<task type="auto">
  <name>Task 1: Author snowflake-schema.md building on star + tree-hierarchy normalization</name>
  <files>.claude/agents/excalidraw/diagram-types/snowflake-schema.md</files>
  <read_first>
    - .planning/phases/06-star-schema-snowflake/06-01-SUMMARY.md (the finalized offset numbers and the star recipe's binding rules — reference these verbatim, do NOT re-derive)
    - .claude/agents/excalidraw/diagram-types/star-schema.md (the recipe snowflake builds on; mirror its structure and inherit its box rules)
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md (the now-finalized offsets snowflake inherits)
    - .claude/agents/excalidraw/diagram-types/tech-architecture.md (canonical TYPE-file section structure)
    - .claude/agents/excalidraw/kb/tree-hierarchy.md (normalized dimension sub-table layout: children indent +60px x, ~40px y-step, thin elbow connectors)
    - .claude/agents/excalidraw/kb/linear-pipeline.md (resolver-listed for snowflake)
    - .claude/agents/excalidraw/diagram-types/README.md (resolver "Adding a diagram type" procedure)
    - .planning/phases/06-star-schema-snowflake/06-RESEARCH.md (Pattern 3 snowflake = star + normalized dimension tree-hierarchy)
  </read_first>
  <action>
    Author .claude/agents/excalidraw/diagram-types/snowflake-schema.md mirroring star-schema.md
    structure (Purpose / How to draw it / Composes (primitive layer) / Ground truth). In Purpose,
    define snowflake as a star where one or more dimensions are NORMALIZED into a hierarchy of
    sub-tables (e.g. dim_product -> dim_category -> dim_department); use it when dimension
    attributes themselves have parent->child structure worth separating. In How to draw it,
    state explicitly that snowflake REUSES star's finalized compartmented-box geometry VERBATIM
    (reference @./star-schema.md and @./compartmented-box.md — header 40, row pitch 20, left-pad
    12, fontSize 16, sharp roundness null, one groupIds per box) and adds ONLY the normalized
    dimension extension via @../kb/tree-hierarchy.md (children indent +60px x, ~40px y-step, thin
    strokeWidth 1.5 elbow connectors anchored to sub-box RECTANGLE borders). Connectors remain
    plain associations (endArrowhead "arrow"). In Composes, list star's set plus the additions:
    @./star-schema.md, @./compartmented-box.md, @../kb/tree-hierarchy.md, @../kb/linear-pipeline.md.
    In Ground truth, point at @../examples/snowflake_schema.png. Do not re-derive any box offset
    numbers — defer to the referenced files.
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/diagram-types/snowflake-schema.md && grep -q "star-schema" .claude/agents/excalidraw/diagram-types/snowflake-schema.md && grep -q "tree-hierarchy" .claude/agents/excalidraw/diagram-types/snowflake-schema.md && grep -q "snowflake_schema.png" .claude/agents/excalidraw/diagram-types/snowflake-schema.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - snowflake-schema.md contains the four sections Purpose / How to draw it / Composes (primitive layer) / Ground truth
    - snowflake-schema.md @-references star-schema.md and compartmented-box.md (states it reuses star's box geometry verbatim, does not re-derive offsets)
    - snowflake-schema.md @-references ../kb/tree-hierarchy.md for the normalized dimension sub-tables
    - snowflake-schema.md Ground truth references `../examples/snowflake_schema.png`
    - snowflake-schema.md does not restate raw box offset numbers as its own derivation (it cites star/compartmented-box for them)
  </acceptance_criteria>
  <done>snowflake-schema.md exists, builds on star by reference (no re-derived geometry), and composes tree-hierarchy for the normalized dimension.</done>
</task>

<task type="auto">
  <name>Task 2: Author and render the snowflake_schema example pair, wire resolver + back-refs</name>
  <files>.claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw, .claude/agents/excalidraw/examples/snowflake_schema.png, .claude/agents/excalidraw/diagram-types/README.md, .claude/agents/excalidraw/kb/tree-hierarchy.md, .claude/agents/excalidraw/kb/linear-pipeline.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/snowflake-schema.md (the recipe just authored — author the example TO it)
    - .claude/agents/excalidraw/examples_excalidraw/star_schema_v2.excalidraw (the PASSING star example from Plan 01 — reuse its exact box construction for fact + dimension boxes; snowflake = this + normalized sub-tables)
    - .planning/phases/06-star-schema-snowflake/06-01-SUMMARY.md (finalized offsets to apply verbatim)
    - .claude/agents/excalidraw/kb/tree-hierarchy.md (sub-table indent/y-step/thin-elbow geometry; existing back-ref line format)
    - .claude/agents/excalidraw/kb/linear-pipeline.md (existing `> Used by types:` line to extend)
    - .claude/agents/excalidraw/examples_excalidraw/repo_tree_hierarchy.excalidraw (reference tree-hierarchy elbow connector field set)
    - .claude/agents/excalidraw/diagram-types/README.md (resolver snowflake-schema row currently _(planned)_)
    - .claude/agents/excalidraw/scripts/render/validate_and_render.sh (frozen render entry point)
  </read_first>
  <action>
    PRECONDITION (SC-2 ordering gate): confirm examples/star_schema_v2.png exists and Plan 01's
    verifier pass is recorded in 06-01-SUMMARY.md before authoring snowflake. If star is not
    passing, stop — snowflake must not be authored against un-finalized geometry.

    Author .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw by taking
    the Sales star and normalizing one dimension into a sub-table chain: e.g. keep fact_sales +
    dim_date + dim_customer as star boxes, and normalize dim_product -> dim_category ->
    dim_department as a tree-hierarchy of three smaller compartmented boxes. EVERY box (fact,
    dimensions, and normalized sub-tables) reuses the locked recipe verbatim: sharp rectangle
    (roundness null), full-width line header divider (x == box.x, points end at box.width), bound
    title text, per-row monospace texts (fontFamily 3, fontSize 16) at left x = box.x + 12, one
    shared groupIds per box, box.width >= longest row * 0.6 * 16 rounded to the 20-grid, all
    required text fields present. Normalized sub-tables indent +60px x and step ~40px y per
    kb/tree-hierarchy.md, connected with thin (strokeWidth 1.5) bound elbow arrows anchored to
    sub-box RECTANGLE borders (never to texts/dividers), endArrowhead "arrow". Fact->dimension
    connectors remain the star fan-out arrows. No multi-line text (SC-4).

    Render: bash .claude/agents/excalidraw/scripts/render/validate_and_render.sh
    .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw — writes the
    sibling PNG; confirm/place it as examples/snowflake_schema.png. Fix any validator/render
    error and re-run until clean.

    Update README.md: in the Data Modeling | snowflake-schema resolver row, set the Type file
    cell to `snowflake-schema.md` (drop _(planned - Phase 6)_) and the Example PNG cell to
    `../examples/snowflake_schema.png`. Append `, snowflake-schema` to the `> Used by types:`
    line in kb/tree-hierarchy.md (add the line if none exists) and in kb/linear-pipeline.md.
  </action>
  <verify>
    <automated>test -f .claude/agents/excalidraw/examples/snowflake_schema.png && python3 -c "import json; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw')); els=d['elements']; assert sum(1 for e in els if e['type']=='line')>=2, 'expected dividers across multiple boxes'; assert any(e.get('groupIds') for e in els), 'no grouped element'; assert all('\n' not in e.get('text','') for e in els if e['type']=='text'), 'multi-line row text (SC-4 violation)'; print('PASS')" && grep -q "snowflake_schema.png" .claude/agents/excalidraw/diagram-types/README.md && grep -q "snowflake-schema" .claude/agents/excalidraw/kb/tree-hierarchy.md && echo ALLPASS</automated>
  </verify>
  <acceptance_criteria>
    - examples/snowflake_schema.png exists (rendered by validate_and_render.sh)
    - snowflake_schema.excalidraw has >=2 `line` dividers (multiple compartmented boxes incl. normalized sub-tables) and at least one grouped element
    - No `text` element contains a `\n` (SC-4)
    - At least one dimension is normalized into a sub-table chain laid out per tree-hierarchy (indented sub-boxes connected by thin elbow arrows)
    - README.md snowflake-schema resolver row names `snowflake-schema.md` and `../examples/snowflake_schema.png` (no longer _(planned)_)
    - kb/tree-hierarchy.md and kb/linear-pipeline.md `Used by types:` lines include `snowflake-schema`
  </acceptance_criteria>
  <done>snowflake_schema pair exists and renders; resolver row is canonical; tree-hierarchy/linear-pipeline carry the snowflake back-ref; no SC-4 violations; star confirmed passing first.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify snowflake example passes the full loop (EX-03 / SC-1/SC-2 gate)</name>
  <what-built>
    A compliant snowflake canonical example (snowflake_schema.excalidraw + rendered
    snowflake_schema.png): the Sales star with one dimension normalized into a tree-hierarchy
    of sub-tables, every box using the locked compartmented-box recipe. The frozen verifier
    checks overflow/dimensions/arrow-anchoring/arrowhead structurally but not SC-1 full-width
    dividers, SC-4 no-multi-line, or the snowflake normalization layout — those need a human eye.
  </what-built>
  <how-to-verify>
    1. Invoke the excalidraw_verifier subagent on
       .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw with its sibling
       PNG; confirm the report shows `passed: true` with empty `issues` (EX-03 exit gate).
    2. Open .claude/agents/excalidraw/examples/snowflake_schema.png and confirm:
       - it reads as a STAR with at least one dimension normalized into a sub-table chain
         (parent dimension -> child sub-tables indented and connected) — the snowflake shape
       - every compartment divider spans the full box width; rows share a common left x (SC-1)
       - no overflow; sharp corners; arrows anchor to box borders, not texts/dividers
       - the normalized sub-table boxes use the SAME box style as the star boxes (consistent
         geometry inherited verbatim, not a different-looking box)
    3. Confirm no row text is packed/multi-line (SC-4).
  </how-to-verify>
  <resume-signal>Type "approved" if the verifier reports passed:true and the PNG shows a correct snowflake (star + normalized tree) with consistent box geometry and SC-1/SC-4 satisfied; otherwise describe the defect so it can be fixed and re-rendered.</resume-signal>
  <action>This is a verification checkpoint, not an authoring task — do not write or mutate any diagram/recipe file here. Perform the verification described in <how-to-verify>: invoke excalidraw_verifier on the example + its sibling PNG, confirm passed:true, then human-eyeball the PNG for the SC-1/SC-4 geometric rules the verifier does not structurally enforce. Pause for the resume-signal.</action>
  <verify><human-check>Verifier report shows passed:true with empty issues, AND the PNG satisfies every bullet in <how-to-verify> (full-width dividers, common left x, no overflow, sharp corners, anchored arrows, no multi-line rows).</human-check></verify>
  <done>excalidraw_verifier reports passed:true on the example (EX-03) and the operator has confirmed the SC-1/SC-4 visual checks; defects, if any, were fixed and re-rendered before approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored diagram text -> verifier/render toolchain | Example labels are DATA consumed by the local frozen render/verify toolchain; no network input, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | snowflake example label text | mitigate | Do not author adversarial label text; verifier `<role>` treats file content as data only (frozen) — do not regress |
| T-06-03 | Tampering | re-deriving box offsets instead of inheriting star's locked geometry (drift bug) | mitigate | snowflake-schema.md cites star/compartmented-box for offsets; example reuses star_schema_v2 box construction verbatim |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- Star precondition confirmed passing (SC-2) before snowflake authored
- snowflake-schema.md exists with all four sections, builds on star by reference, composes tree-hierarchy
- snowflake_schema.excalidraw renders to examples/snowflake_schema.png via the frozen loop
- JSON assertion: >=2 line dividers, >=1 grouped element, zero multi-line row texts
- resolver row updated to the new recipe + PNG; tree-hierarchy/linear-pipeline carry the snowflake back-ref
- excalidraw_verifier reports passed:true on the example (EX-03)
- human visual confirmation of the snowflake shape, consistent box geometry, full-width dividers, anchored arrows
</verification>

<success_criteria>
- DM-03 satisfied: snowflake-schema.md recipe exists (building on star + tree-hierarchy) and the canonical snowflake example passes the full validate->render->verify loop
- Snowflake authored only after star passes (SC-2); box geometry inherited verbatim, not re-derived
- The resolver table's snowflake row is canonical (new recipe + new PNG)
- tree-hierarchy and linear-pipeline are back-referenced (DTKB-03 bidirectional link)
</success_criteria>

<output>
Create `.planning/phases/06-star-schema-snowflake/06-02-SUMMARY.md` when done.
</output>
