---
phase: 05-tech-architecture-activity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/excalidraw/kb/decision-branch.md
  - .claude/agents/excalidraw/kb/decision-marker.md
  - .claude/agents/excalidraw/kb/feedback-loop.md
  - .claude/agents/excalidraw/kb/task-list.md
  - .claude/agents/excalidraw/kb/linear-pipeline.md
  - .claude/agents/excalidraw/kb/group-container.md
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "tech-architecture.md names all six composed kb sub-patterns (no geometry re-derived)"
    - "Every kb primitive that Activity or Tech Architecture composes carries a `> Used by types:` back-ref naming each consuming type"
    - "The two-layer cross-reference is bidirectional and cannot silently drift"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/tech-architecture.md"
      provides: "Tech Architecture recipe naming 6 composed kb primitives"
      contains: "Composes (primitive layer)"
    - path: ".claude/agents/excalidraw/kb/decision-branch.md"
      provides: "decision-branch primitive with activity back-ref"
      contains: "Used by types"
    - path: ".claude/agents/excalidraw/kb/task-list.md"
      provides: "task-list primitive with activity back-ref"
      contains: "Used by types"
  key_links:
    - from: ".claude/agents/excalidraw/kb/linear-pipeline.md"
      to: "diagram-types/tech-architecture.md and diagram-types/activity.md"
      via: "Used by types back-ref"
      pattern: "Used by types:.*tech-architecture.*activity|Used by types:.*activity"
---

<objective>
Satisfy ARCH-01 by confirming `tech-architecture.md` already reframes the architecture/overview
material and names its six composed sub-patterns, then complete the bidirectional two-layer
cross-references (DTKB-03 / ARCH-01 criterion 4 / UML-04 criterion 4) by adding `> Used by types:`
back-refs to every `kb/` primitive consumed by Tech Architecture and Activity.

Purpose: ARCH-01's substantive work is verification + back-ref completion (per 05-RESEARCH.md the
recipe file already exists and is fully wired). The activity-specific primitives currently lack
back-refs, and the two shared primitives name only `tech-architecture`. Completing them now means
Plan 03 can author and index the Activity example against a primitive layer that already declares
its consumers — the two layers cannot drift.

Output: 4 kb primitives gain a new back-ref; 2 shared kb primitives gain `activity` alongside the
existing `tech-architecture`; tech-architecture.md confirmed (likely no change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tech-architecture-activity/05-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan creates no new symbols, files, classes, functions, or CLI flags. It edits existing
Markdown files only. New strings introduced:
- `> Used by types: activity` blockquote line in `kb/decision-branch.md`, `kb/decision-marker.md`,
  `kb/feedback-loop.md`, `kb/task-list.md`
- `activity` appended to the existing `> Used by types: tech-architecture` line in
  `kb/linear-pipeline.md` and `kb/group-container.md` (becoming `> Used by types: tech-architecture, activity`)
No code, no schema, no new file paths.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm tech-architecture.md satisfies ARCH-01 (verify-only)</name>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/tech-architecture.md (the recipe under audit)
    - .planning/REQUIREMENTS.md (ARCH-01 wording)
    - .planning/phases/05-tech-architecture-activity/05-RESEARCH.md (Summary + Open Question 1: treat ARCH-01 as verify-and-complete)
  </read_first>
  <action>
    Read tech-architecture.md and confirm it satisfies ARCH-01 criterion 1: it must (a) state the
    type purpose as technologies/services/clouds and their relationships, (b) name the six composed
    sub-patterns group-container, icon-block, multi-zoom-overview, fan-out, convergence,
    linear-pipeline via `@../kb/<pattern>.md` links, and (c) reuse `../examples/architecture_overview.png`
    as ground truth, with zero `x:`/`y:` coordinate math in the file (geometry stays in kb).
    If all three hold, make NO edits — record in the SUMMARY that the file already satisfies ARCH-01
    criterion 1 (per 05-RESEARCH.md Assumption A2). Only if a concrete gap against the criterion is
    found (a missing sub-pattern link, missing purpose prose, or inlined coordinates) do you add the
    minimal prose/link to close exactly that gap — do not rewrite the file.
  </action>
  <verify>
    <automated>cd .claude/agents/excalidraw && test $(grep -c '@\.\./kb/' diagram-types/tech-architecture.md) -ge 6 && grep -qi 'technolog' diagram-types/tech-architecture.md && grep -q 'architecture_overview.png' diagram-types/tech-architecture.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@\.\./kb/' diagram-types/tech-architecture.md` returns ≥ 6 (six composed primitive links present)
    - The six links resolve to group-container, icon-block, multi-zoom-overview, fan-out, convergence, linear-pipeline (verify by grepping each filename)
    - The file contains the string `architecture_overview.png` (ground-truth reuse)
    - No line in the file matches `"x":` or `"y":` followed by a number (no geometry re-derivation)
    - If no edit was needed, SUMMARY states "tech-architecture.md already satisfies ARCH-01 criterion 1 — no change"
  </acceptance_criteria>
  <done>tech-architecture.md confirmed to name all six sub-patterns, state its purpose, and reuse the example PNG; edits made only if a concrete criterion gap existed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete bidirectional two-layer back-refs on Activity + shared primitives</name>
  <read_first>
    - .claude/agents/excalidraw/kb/linear-pipeline.md (shows the canonical back-ref format on line 3: `> Used by types: tech-architecture`)
    - .claude/agents/excalidraw/kb/decision-branch.md (target — currently no back-ref)
    - .claude/agents/excalidraw/kb/decision-marker.md (target — currently no back-ref)
    - .claude/agents/excalidraw/kb/feedback-loop.md (target — currently no back-ref)
    - .claude/agents/excalidraw/kb/task-list.md (target — currently no back-ref)
    - .claude/agents/excalidraw/kb/group-container.md (target — currently `> Used by types: tech-architecture`)
    - .claude/agents/excalidraw/kb/README.md (documents the two-layer cross-ref relationship — confirm it already exists, do not edit)
  </read_first>
  <action>
    Add the `> Used by types:` blockquote back-ref to each Activity-composed primitive, matching the
    exact format used in linear-pipeline.md (a blockquote line placed immediately after the H1 title,
    separated by blank lines). For the four primitives with NO existing back-ref — decision-branch.md,
    decision-marker.md, feedback-loop.md, task-list.md — insert a new line `> Used by types: activity`
    after the H1. For the two primitives already consumed by tech-architecture — linear-pipeline.md and
    group-container.md — change the existing `> Used by types: tech-architecture` line to
    `> Used by types: tech-architecture, activity` (comma-separated, do not duplicate the line).
    Do not touch kb/README.md: per 05-RESEARCH.md it already documents the two-layer relationship.
    Use the exact type slug `activity` (matching the resolver-table row that Plan 03 will promote).
  </action>
  <verify>
    <automated>cd .claude/agents/excalidraw/kb && for f in decision-branch decision-marker feedback-loop task-list; do grep -q '^> Used by types:.*activity' $f.md || { echo "FAIL $f"; exit 1; }; done && grep -q '^> Used by types: tech-architecture, activity' linear-pipeline.md && grep -q '^> Used by types: tech-architecture, activity' group-container.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `kb/decision-branch.md`, `kb/decision-marker.md`, `kb/feedback-loop.md`, `kb/task-list.md` each contain a line matching `^> Used by types:.*activity`
    - `kb/linear-pipeline.md` and `kb/group-container.md` each contain exactly one line `> Used by types: tech-architecture, activity` (the prior `tech-architecture`-only line is replaced, not duplicated — `grep -c '^> Used by types:'` returns 1 in each)
    - The back-ref line in each edited file is a `>` blockquote placed immediately after the H1 `# Pattern:` heading
    - `grep -rL '^> Used by types' kb/decision-branch.md kb/decision-marker.md kb/feedback-loop.md kb/task-list.md kb/linear-pipeline.md kb/group-container.md` returns no files (all six carry a back-ref)
  </acceptance_criteria>
  <done>All six Activity/Tech-Architecture-composed primitives carry a `> Used by types:` back-ref naming each consuming type; the two-layer link is bidirectional and drift-proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits author-controlled Markdown only. No untrusted input, network, auth, or runtime state crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | kb back-ref edits drift from resolver-table type slug | mitigate | Use the exact slug `activity` matching the README resolver row Plan 03 promotes; acceptance criteria grep for it |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (05-RESEARCH.md Package Legitimacy Audit: "Not applicable"); nothing to slopcheck |
</threat_model>

<verification>
- ARCH-01 criterion 1: `grep -c '@\.\./kb/' diagram-types/tech-architecture.md` ≥ 6
- ARCH-01 criterion 4 / DTKB-03: every consumed primitive carries a `> Used by types:` back-ref
- No coordinate math leaked into the TYPE-layer recipe file
</verification>

<success_criteria>
- tech-architecture.md confirmed to satisfy ARCH-01 criterion 1 (purpose + 6 sub-patterns + example reuse, no geometry)
- decision-branch, decision-marker, feedback-loop, task-list each gain `> Used by types: activity`
- linear-pipeline, group-container updated to `> Used by types: tech-architecture, activity`
- Bidirectional two-layer cross-reference complete and verifiable by grep
</success_criteria>

<output>
Create `.planning/phases/05-tech-architecture-activity/05-01-SUMMARY.md` when done
</output>
