---
phase: 09-data-vault
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/kb/fan-out.md
  - .claude/agents/excalidraw/kb/tree-hierarchy.md
  - .claude/agents/excalidraw/kb/convergence.md
  - .claude/agents/excalidraw/kb/group-container.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DM-04]
must_haves:
  truths:
    - "The data-vault resolver row in diagram-types/README.md is wired (no longer marked _(planned)_), names data-vault.md, the actually-composed kb sub-patterns, and the real example PNG ../examples/data_vault_sales.png"
    - "Each kb primitive the example composes carries a `> Used by types: ... data-vault` back-ref (fan-out, tree-hierarchy, convergence, group-container) so the two layers cannot drift"
    - "DM-04 is marked complete in REQUIREMENTS.md (checkbox + traceability row)"
    - "An end-to-end regression confirms every wired canonical example (old + new) still passes verifier_structural.py with empty issues"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/README.md"
      provides: "Single authoritative resolver table with data-vault wired"
      contains: "data_vault_sales.png"
    - path: ".claude/agents/excalidraw/kb/tree-hierarchy.md"
      provides: "Primitive with data-vault added to its back-ref"
      contains: "data-vault"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/examples/data_vault_sales.png"
      via: "resolver row Example PNG column"
      pattern: "data_vault_sales"
    - from: ".claude/agents/excalidraw/kb/tree-hierarchy.md"
      to: ".claude/agents/excalidraw/diagram-types/data-vault.md"
      via: "> Used by types: back-ref (bidirectional two-layer link)"
      pattern: "data-vault"
---

<objective>
Wire the reserved `data-vault` resolver row in `diagram-types/README.md` (the single authoritative
family→type map), complete the two-layer back-references on the composed kb primitives, mark DM-04
complete in REQUIREMENTS.md, and run the end-to-end regression. This is the Wave-3 finalization —
the exact analog of Phase 8's `08-04-PLAN.md` resolver-wiring plan (single type instead of two).

Per Pitfall 7 / EX-03 gate: the resolver row is wired ONLY after the canonical example (09-02)
passes the full validate→render→verify loop with EX-03 human visual approval INCLUDING the
explicit grayscale check. Wiring a placeholder row before the example passes means the specialist
imitates broken ground truth forever.

This phase ships the LAST v1.1 type — after DM-04 is marked complete, all 21 v1.1 requirements
mapped to a phase are accounted for.

Purpose: Close the two-layer cross-reference loop (the type file already links down to primitives;
this plan completes the primitive-side `> Used by types: ... data-vault` back-refs) and flip the
requirement traceability so the phase — and the milestone's type roster — is verifiably done.

Output:
- `diagram-types/README.md` — data-vault row wired, "Wired rows" note updated
- `kb/fan-out.md`, `kb/tree-hierarchy.md`, `kb/convergence.md`, `kb/group-container.md` back-refs
  extended with `, data-vault` (only those the example actually composes)
- `REQUIREMENTS.md` — DM-04 marked complete
- End-to-end regression record in the SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-vault/09-RESEARCH.md
@.claude/agents/excalidraw/diagram-types/README.md
@.planning/phases/09-data-vault/09-01-SUMMARY.md
@.planning/phases/09-data-vault/09-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files MODIFIED by THIS plan (exclude from drift verification):
- `diagram-types/README.md` — the reserved data-vault resolver row changed from `_(planned)_` to
  wired; "Wired rows" note paragraph extended with data-vault
- `kb/fan-out.md`, `kb/tree-hierarchy.md`, `kb/convergence.md`, `kb/group-container.md` —
  `> Used by types:` lines extended with `, data-vault` (only the primitives the example composes)
- `REQUIREMENTS.md` — DM-04 checkbox + traceability row flipped to Complete

<tasks>

<task type="auto">
  <name>Task 1: Confirm the data-vault EX-03 gate passed before wiring (gate)</name>
  <files>.planning/phases/09-data-vault/09-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-data-vault/09-02-SUMMARY.md (must record the data-vault EX-03 visual approval INCLUDING the explicit grayscale confirmation + the structural-green example + which kb primitives the example actually composed)
    - .planning/phases/09-data-vault/09-RESEARCH.md (Pitfall 7 — wire ONLY after validate_and_render.sh exits 0, verifier issues == [], AND EX-03 human approval with grayscale confirmation; A4 — if the example composed a SUBSET of the reserved composition list, the resolver row must be updated to match what was actually composed)
  </read_first>
  <action>
Confirm the EX-03 gate is cleared before touching the resolver table (Pitfall 7). Verify the
example file pair exists (`examples_excalidraw/data_vault_sales.excalidraw` +
`examples/data_vault_sales.png`), re-run the structural verifier and confirm issues == [], and
confirm `09-02-SUMMARY.md` records EX-03 visual approval WITH explicit grayscale confirmation.
Also determine from the SUMMARY (or by inspecting the example) which kb primitives the example
actually composes (the reserved list is group-container, fan-out, tree-hierarchy, convergence,
evidence-card — but the example may compose a subset; per A4 the resolver row + back-refs must
match what was actually composed, not the speculative reserved list). If the gate is not cleared,
STOP and report — never wire a row whose example has not passed. This is a hard gate.
  </action>
  <verify>
    <automated>python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/data_vault_sales.excalidraw &amp;&amp; test -f .claude/agents/excalidraw/examples/data_vault_sales.png &amp;&amp; test -f .planning/phases/09-data-vault/09-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The example pair exists (source + rendered PNG) for data-vault
    - `verifier_structural.py data_vault_sales.excalidraw` returns issues == [] (exit 0)
    - `09-02-SUMMARY.md` records EX-03 visual approval INCLUDING the explicit grayscale confirmation
    - The set of kb primitives the example actually composes is determined (for use in Task 2's composition cell + back-refs)
    - IF the gate is uncleared: the task halts with an explicit report (do NOT continue to Task 2)
  </acceptance_criteria>
  <done>The data-vault EX-03 gate is confirmed cleared (structural-green + visual approval with grayscale confirmation) and the actually-composed primitive set is known, OR execution halts with a report.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the data-vault resolver row + complete two-layer back-refs</name>
  <files>.claude/agents/excalidraw/diagram-types/README.md, .claude/agents/excalidraw/kb/fan-out.md, .claude/agents/excalidraw/kb/tree-hierarchy.md, .claude/agents/excalidraw/kb/convergence.md, .claude/agents/excalidraw/kb/group-container.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/README.md (FULL — the reserved `data-vault` row at ~line 24 marked `_(planned — Phase 9)_` with composition list `group-container, fan-out, tree-hierarchy, convergence, evidence-card` and Example PNG `_(planned)_`; the "Wired rows" note paragraph at ~line 30; the "Adding a diagram type" 4-step procedure at ~line 48)
    - .claude/agents/excalidraw/kb/fan-out.md (line with `> Used by types: tech-architecture, star-schema` — extend with `, data-vault` if the example composes fan-out)
    - .claude/agents/excalidraw/kb/tree-hierarchy.md (line with `> Used by types: snowflake-schema` — extend with `, data-vault` if composed)
    - .claude/agents/excalidraw/kb/convergence.md (line with `> Used by types: tech-architecture, star-schema` — extend with `, data-vault` if composed)
    - .claude/agents/excalidraw/kb/group-container.md (line with `> Used by types: tech-architecture, activity, star-schema, use-case` — extend with `, data-vault` if composed, e.g. for the legend box)
    - .planning/phases/09-data-vault/09-02-SUMMARY.md (the actually-composed primitive set from Task 1 — wire ONLY those)
  </read_first>
  <action>
Apply the resolver wiring and back-refs for the EX-03-cleared data-vault type:

1. Replace the reserved `data-vault` resolver row in README.md: Type file cell reads `data-vault.md`
   (drop the `_(planned — Phase 9)_` marker), Composes cell reads the primitives the example
   ACTUALLY composes (per Task 1 / 09-02-SUMMARY — e.g. `group-container, fan-out, tree-hierarchy,
   convergence`; drop `evidence-card` if the example did not use it, per A4), Example PNG cell reads
   `../examples/data_vault_sales.png` (replacing the `_(planned)_` marker).
2. Update the "Wired rows" note paragraph (~line 30) to add: data-vault (wired as of Phase 9 —
   canonical `data_vault_sales.excalidraw` passes the full validate→render→verify loop; EX-03
   visual gate approved with grayscale confirmation; resolver points at `data_vault_sales.png`).
3. Extend each composed primitive's `> Used by types:` back-ref line with `, data-vault` — for
   ONLY the primitives the example actually composes (fan-out, tree-hierarchy, convergence,
   group-container as applicable). Do NOT add the back-ref to a primitive the example does not
   compose.

Do NOT modify any other resolver row. Do NOT touch evidence-card's back-ref unless the example
composed it.
  </action>
  <verify>
    <automated>grep -q 'data_vault_sales.png' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; ! grep -E 'data-vault\.md.*_\(planned' .claude/agents/excalidraw/diagram-types/README.md &amp;&amp; grep -q 'data-vault' .claude/agents/excalidraw/kb/tree-hierarchy.md</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/README.md` data-vault row names `data-vault.md`, the actually-composed kb sub-patterns, Example PNG `../examples/data_vault_sales.png`, and is no longer marked `_(planned)_`
    - The "Wired rows" note paragraph mentions data-vault as wired in Phase 9 with grayscale confirmation
    - Each composed primitive's `> Used by types:` line includes `data-vault` (at minimum tree-hierarchy, which the satellite attachment composes); a primitive the example did NOT compose is NOT given the back-ref
    - No other resolver row was modified — `git diff` shows changes confined to the data-vault row + the note paragraph + the composed-primitive back-ref lines
  </acceptance_criteria>
  <done>The data-vault resolver row is wired to the real recipe + example PNG, the "Wired rows" note is updated, and the two-layer back-refs are complete and bidirectional for exactly the primitives the example composes.</done>
</task>

<task type="auto">
  <name>Task 3: Mark DM-04 complete and run end-to-end regression</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DM-04 checkbox at line 38; Traceability table row for DM-04 at ~line 109 — flip Status from Pending to Complete, matching the format used for the already-Complete DM-01 / DM-03 / UML-01 rows)
    - .claude/agents/excalidraw/diagram-types/README.md (the wired resolver table — the regression confirms every wired type still resolves and every wired example still passes structural verification)
  </read_first>
  <action>
(a) In `REQUIREMENTS.md`: flip the `DM-04` checkbox from `[ ]` to `[x]`, and change its
Traceability-table Status cell from `Pending` to `Complete` — matching the exact format used for
the already-Complete DM-01, DM-03, UML-01 rows.

(b) End-to-end regression (SC-4 analog from Phases 7/8): re-run `verifier_structural.py` on every
already-wired canonical example to confirm Phase 9's additions did not regress prior types — at
minimum `activity_order_fulfillment`, `star_schema_v2`, `snowflake_schema`, `er_retail_orders`,
`class_order_domain`, `sequence_login_flow`, `use_case_checkout`, plus the new
`data_vault_sales`. Each must still return issues == []. Record the per-file pass/fail results in
the SUMMARY.
  </action>
  <verify>
    <automated>grep -q '\[x\] \*\*DM-04' .planning/REQUIREMENTS.md &amp;&amp; for f in activity_order_fulfillment star_schema_v2 snowflake_schema er_retail_orders class_order_domain sequence_login_flow use_case_checkout data_vault_sales; do python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/$f.excalidraw || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `REQUIREMENTS.md` shows `[x] **DM-04`
    - `REQUIREMENTS.md` Traceability row for DM-04 reads `Complete`
    - Every wired canonical example (activity, star_schema_v2, snowflake_schema, er_retail_orders, class_order_domain, sequence_login_flow, use_case_checkout, data_vault_sales) returns issues == [] under `verifier_structural.py` (exit 0)
    - The SUMMARY records the per-file regression results
  </acceptance_criteria>
  <done>DM-04 is marked complete in REQUIREMENTS.md, and the full end-to-end regression confirms no prior wired type regressed and data-vault passes structural verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver table → specialist dispatch | The resolver row is the contract the specialist reads to pick a recipe + example; a wrong/premature row poisons every future data-vault diagram. No network/auth/persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Repudiation | Resolver row wired before its example passed EX-03 (broken ground truth imitated forever) | mitigate | Task 1 gate confirms structural-green + EX-03 approval with grayscale confirmation before Task 2 wires (Pitfall 7) |
| T-09-08 | Tampering | Two-layer back-ref left incomplete or added to an uncomposed primitive, allowing TYPE/PRIMITIVE layers to silently drift | mitigate | Acceptance asserts back-refs added for exactly the composed primitives (A4); the type file links down to them |
| T-09-09 | Tampering | Phase 9 back-ref/resolver change regresses an already-wired prior example | mitigate | Task 3 regression runs `verifier_structural.py` over all wired examples; each must return empty issues |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- The data-vault resolver row is wired to the real recipe file + example PNG, with the "Wired
  rows" note updated and grayscale confirmation recorded.
- Two-layer back-refs are complete and bidirectional for exactly the composed primitives.
- DM-04 is marked complete in REQUIREMENTS.md.
- The end-to-end regression confirms all wired examples (old + new) pass structural verification.
</verification>

<success_criteria>
- The single authoritative resolver table dispatches the data-vault type to its recipe + example;
  the two KB layers cross-reference each other so they cannot drift.
- DM-04 is verifiably complete and the phase introduces no regression to prior types — shipping
  the last v1.1 type.
</success_criteria>

<output>
Create `.planning/phases/09-data-vault/09-03-SUMMARY.md` when done. Record the per-file regression
results, the actually-composed primitive set wired into the resolver, and confirm the EX-03 gate
(with grayscale confirmation) was cleared before wiring.
</output>
