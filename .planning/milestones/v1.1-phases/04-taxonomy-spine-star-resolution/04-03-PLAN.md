---
phase: 04-taxonomy-spine-star-resolution
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .claude/commands/excalidraw.md
  - .claude/agents/excalidraw/excalidraw_specialist.md
autonomous: false
requirements: [TAX-01, TAX-02, TAX-03, INT-01]

must_haves:
  truths:
    - "Running /excalidraw presents exactly four families (Tech Architecture, Data Modeling, UML / SW-Engineering, Flow / Process) as the top-level AskUserQuestion"
    - "A family with multiple types triggers a second type sub-pick within the 4-option cap, with a documented plain-text numbered-menu fallback for any family that grows past four types"
    - "The resolved family+type drives dispatch via the single authoritative resolver table in diagram-types/README.md (no duplicated mapping)"
    - "excalidraw_specialist.md, given a resolved family+type, reads diagram-types/<type>.md FIRST and consults the composed sub-patterns it names"
    - "The v1.0 render->verify->fix loop is untouched: sections 4-6 of the command are byte-identical, and the validator/verifier/scripts are unchanged"
    - "The scaffolding is smoke-tested end-to-end against one existing type (Tech Architecture / architecture_overview) through the new resolver path"
  artifacts:
    - path: ".claude/commands/excalidraw.md"
      provides: "Two-tier family->type picker (sections 1-3) feeding the authoritative resolver"
      contains: "Tech Architecture"
    - path: ".claude/agents/excalidraw/excalidraw_specialist.md"
      provides: "diagram-types/ asset path + read-type-recipe-first mandate"
      contains: "diagram-types"
  key_links:
    - from: ".claude/commands/excalidraw.md"
      to: ".claude/agents/excalidraw/diagram-types/README.md"
      via: "resolver-table read in step 2"
      pattern: "diagram-types/README"
    - from: ".claude/agents/excalidraw/excalidraw_specialist.md"
      to: ".claude/agents/excalidraw/diagram-types/"
      via: "read type recipe first mandate"
      pattern: "diagram-types"
---

<objective>
Wire the new diagram-types/ layer into the live flow: rewrite ONLY sections 1-3 of /excalidraw to a two-tier family-to-type picker that resolves dispatch through the authoritative resolver table (TAX-01/02/03), revise excalidraw_specialist.md to read the resolved diagram-types/<type>.md recipe FIRST (INT-01), and smoke-test the spine end-to-end against one existing type (Tech Architecture) WITHOUT touching the frozen v1.0 render-verify-fix loop, validator, or verifier.

Purpose: This is the integration proof of Phase 4. The family picker and resolver wiring must fire correctly for any type to be testable end-to-end. Proving it against Tech Architecture (reuses architecture_overview.png, no new example needed) confirms the family-to-type-to-bundle-to-specialist flow before any new notation is authored in Phase 5+. Sections 4-6 of the command (the verify-fix loop) are FROZEN and MUST NOT be touched (Anti-Pattern 4).

Output: revised command (sections 1-3 only), revised specialist (additive asset path + mandate), and an executed smoke test of the resolver path against Tech Architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.claude/commands/excalidraw.md
@.claude/agents/excalidraw/excalidraw_specialist.md
@.claude/agents/excalidraw/diagram-types/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /excalidraw sections 1-3 to the two-tier family/type picker + resolver (TAX-01/02/03) — sections 4-6 FROZEN</name>
  <read_first>
    - .claude/commands/excalidraw.md (the file being modified — rewrite sections 1-3; sections 4, 5, 6 and trailing "## Rules" are FROZEN, byte-identical)
    - .claude/agents/excalidraw/diagram-types/README.md (the authoritative resolver table the command points at in step 2 — created by Plan 04-01)
    - .planning/research/ARCHITECTURE.md (Pattern 3 command-side resolver table; Pattern 4 two-tier picker within the 4-option cap; the family/type selection to specialist dispatch data flow)
    - .planning/research/PITFALLS.md (Anti-Pattern 3 flat single-question picker; UX Pitfalls family names match mental models and single-type families skip the sub-pick; Anti-Pattern 4 touching the verify-fix loop)
    - .planning/REQUIREMENTS.md (TAX-01, TAX-02, TAX-03 exact wording)
  </read_first>
  <action>
    Edit ONLY sections 1, 2, 3 of .claude/commands/excalidraw.md. Sections 4, 5, 6 and the trailing "## Rules" MUST remain byte-for-byte unchanged (FROZEN constraint, Anti-Pattern 4). Do NOT touch scripts/, the validator, or the verifier.

    Section 1 (TAX-01/TAX-02): replace the "Q1 — diagram kind" four vague kinds with a two-tier pick. Q1 is one AskUserQuestion with exactly 4 family options: Tech Architecture, Data Modeling, UML / SW-Engineering, Flow / Process; each option description names its member types so users self-route (Data Modeling: star, snowflake, ER, data vault; UML / SW-Engineering: sequence, class, use-case, activity). Type sub-pick: when the chosen family has more than one type, ask a SECOND AskUserQuestion listing that family's types (<=4, within cap). Single-type families (Tech Architecture) skip the sub-pick (family IS the type). Document the plain-text NUMBERED-MENU fallback for any family that grows past four types (TAX-02 escape hatch). Explicitly prohibit a flat single-question list of all types (Anti-Pattern 3). Keep Q2 (Propose / Describe / Just generate) intact in intent.

    Section 2 (TAX-03): after the type is resolved, instruct the command to READ diagram-types/README.md, find the row for the chosen type, and read off the bundle: type recipe diagram-types/<type>.md, the composed kb/<sub-pattern>.md list, the example PNG. The authoritative mapping lives ONLY in that table — the command MUST NOT restate/hard-code the mapping (Anti-Pattern 2).

    Section 3 (TAX-03): in the specialist dispatch prompt, ADD the resolved type name and the absolute path to diagram-types/<type>.md (read FIRST), alongside the existing kb/<pattern>.md list and example PNG. The dispatch MECHANISM (Agent tool, author mode, capture the .excalidraw path) is unchanged — only the bundle contents grow.
  </action>
  <acceptance_criteria>
    - Section 1 presents the four families "Tech Architecture", "Data Modeling", "UML / SW-Engineering" (or "UML / SW Eng"), "Flow / Process" as the top-level AskUserQuestion.
    - Sections 1-3 document the second type sub-pick within the 4-option cap AND a plain-text numbered-menu fallback for families exceeding four types (words "numbered" and "fallback" both appear).
    - Section 2 instructs reading diagram-types/README.md to resolve the bundle (string "diagram-types/README" present).
    - The command does NOT hard-code a duplicate resolver table (the literal "Composes (kb sub-patterns)" appears zero times in excalidraw.md).
    - FROZEN: the region from the "### 4." heading to end-of-file has sha256 exactly c1604b18abcf442774a7a097646901691dcddaa18af637237f0d21409eb6bcba.
    - scripts/, the validator, and the verifier file are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>f=.claude/commands/excalidraw.md; grep -q "Tech Architecture" "$f" && grep -q "Data Modeling" "$f" && grep -Eq "UML / SW" "$f" && grep -q "Flow / Process" "$f" && grep -qi "numbered" "$f" && grep -qi "fallback" "$f" && grep -q "diagram-types/README" "$f" && [ "$(grep -c 'Composes (kb sub-patterns)' "$f")" -eq 0 ] && [ "$(awk '/^### 4\. The verify/{p=1} p' "$f" | sha256sum | cut -d' ' -f1)" = "c1604b18abcf442774a7a097646901691dcddaa18af637237f0d21409eb6bcba" ]</automated>
  </verify>
  <done>Sections 1-3 implement the two-tier family-to-type picker resolving dispatch through diagram-types/README.md with a documented numbered-menu fallback and no duplicated resolver table; sections 4-6 + Rules are byte-identical (sha256 match); pipeline files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Revise excalidraw_specialist.md to read the type recipe FIRST (INT-01) — loop untouched</name>
  <read_first>
    - .claude/agents/excalidraw/excalidraw_specialist.md (the file being modified — edit <asset_paths> and <operational_mandates> #1 only; the <delivery_contract> render-verify-fix behavior is untouched)
    - .planning/research/ARCHITECTURE.md (MODIFIED files row for excalidraw_specialist.md — exact edit sites: +1 asset path, +1 mandate, edit to the Pattern-First mandate)
    - .claude/agents/excalidraw/diagram-types/README.md (the resolver table the specialist also consults — created by Plan 04-01)
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md and .claude/agents/excalidraw/diagram-types/compartmented-box.md (shared convention docs the specialist references for formal-notation types — created by Plan 04-02)
    - .planning/REQUIREMENTS.md (INT-01 exact wording)
  </read_first>
  <action>
    Make purely ADDITIVE edits to excalidraw_specialist.md (INT-01). Do NOT change the render-verify-fix behavior, the <delivery_contract> (blocks A-E), the always-render mandate, or any validator/verifier interaction. In <asset_paths>: add a "Diagram-Type KB" bullet for diagram-types/, stating that when the orchestrator names a resolved type, the specialist reads diagram-types/<type>.md FIRST (it names which kb/ primitives to compose and any notation workarounds), then reads those composed kb/ primitives; note the shared convention docs (notation-conventions.md, compartmented-box.md) for formal-notation types. In <operational_mandates> #1 (Pattern First): prepend a directive that IF the orchestrator names a diagram-types/<type>.md recipe, read it FIRST before decomposing into kb/ patterns. The loop, 3-iteration cap, always-render rule, sibling-artefact discipline, and "you do not verify" contract are FROZEN — leave them unchanged.
  </action>
  <acceptance_criteria>
    - The <asset_paths> section names the diagram-types/ directory.
    - The prompt contains a directive to read diagram-types/<type>.md FIRST (strings "diagram-types" and "first" both appear, case-insensitive).
    - The always-render and "do NOT verify" contract language is intact (strings "validate_and_render.sh" and a "do NOT verify"/"You Author and Fix" phrase still present).
    - scripts/, the validator, and the verifier file are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>f=.claude/agents/excalidraw/excalidraw_specialist.md; grep -qi "diagram-types" "$f" && grep -qi "first" "$f" && grep -q "validate_and_render.sh" "$f" && grep -Eqi "do NOT verify|You Author and Fix" "$f"</automated>
  </verify>
  <done>excalidraw_specialist.md gains a diagram-types/ asset-path bullet and a "read diagram-types/<type>.md first" directive in mandate #1, while the render-verify-fix loop, always-render rule, and "do not verify" contract remain unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke-test the resolver path end-to-end against one existing type (Tech Architecture)</name>
  <read_first>
    - .claude/commands/excalidraw.md (sections 1-3 as edited in Task 1 — the picker + resolver path being exercised)
    - .claude/agents/excalidraw/diagram-types/README.md (the resolver row for tech-architecture — type recipe + composed kb list + example PNG)
    - .claude/agents/excalidraw/diagram-types/tech-architecture.md (the recipe the specialist must read first)
    - .claude/agents/excalidraw/excalidraw_specialist.md (the read-type-recipe-first mandate)
  </read_first>
  <what-built>
    Sections 1-3 of /excalidraw now drive a two-tier family-to-type picker that resolves the dispatch bundle from diagram-types/README.md, and the specialist reads diagram-types/<type>.md first. The smoke test proves the family-to-type-to-bundle-to-specialist flow fires correctly against ONE existing type (Tech Architecture, which reuses architecture_overview.png and needs no new example) without touching the frozen sections 4-6 loop, validator, or verifier.
  </what-built>
  <how-to-verify>
    1. In an interactive Claude Code session, run /excalidraw with a tech-architecture request (e.g. "draw a tech architecture overview of a data platform").
    2. Confirm Q1 presents exactly the four families and that selecting "Tech Architecture" SKIPS the type sub-pick (single-type family).
    3. Confirm the command reads diagram-types/README.md, resolves the tech-architecture row, and the specialist dispatch prompt names diagram-types/tech-architecture.md (to read first) + the composed kb/ primitives + architecture_overview.png.
    4. Confirm the specialist reads diagram-types/tech-architecture.md before the kb/ primitives, then authors + renders, and the EXISTING sections 4-6 verify-fix loop runs unchanged to a verified PNG (or honest failure).
    5. Confirm no scripts/, validator, or verifier file was modified during the run.
  </how-to-verify>
  <acceptance_criteria>
    - The /excalidraw run routes a Tech Architecture request through the new resolver path: family picker -> diagram-types/README.md lookup -> dispatch naming diagram-types/tech-architecture.md + composed kb primitives + architecture_overview.png.
    - The single-type Tech Architecture family skips the type sub-pick.
    - The sections 4-6 verify-fix loop executes unchanged (no edits to sections 4-6, validator, verifier, or scripts/).
    - The operator confirms the end-to-end flow worked (or reports the precise failure point) in the resume signal.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the Tech Architecture request routed through the new resolver path end-to-end with the sections 4-6 loop unchanged; otherwise describe where the flow broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> /excalidraw picker | Untrusted free-text + AskUserQuestion selections cross into the resolver; a mis-route loads the wrong KB/example. |
| Command -> specialist dispatch | The dispatch prompt names which type recipe + kb files + example the subagent reads. |
| Edits -> frozen v1.0 loop | The single largest regression risk: an edit to sections 4-6, the validator, or the verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | command sections 4-6 / validator / verifier (frozen) | mitigate | sha256 freeze assertion on the section-4-to-EOF region (Task 1); acceptance criteria assert pipeline files unmodified; specialist edits are additive only. |
| T-04-07 | Spoofing/mis-route | family/type picker -> asset bundle | mitigate | Single authoritative resolver in diagram-types/README.md (no duplicate); smoke test (Task 3) confirms the right bundle loads for the wired type. |
| T-04-08 | Denial of service | over-questioning picker friction | accept | Single-type families skip the sub-pick (UX Pitfall mitigation); two-tier only when a family has >1 type. |
</threat_model>

<verification>
- /excalidraw sections 1-3 implement the two-tier picker + resolver-table dispatch; sections 4-6 + Rules byte-identical (sha256 c1604b18...).
- excalidraw_specialist.md reads diagram-types/<type>.md first; loop/contract unchanged.
- Smoke test routes a Tech Architecture request end-to-end through the new resolver path with the frozen loop intact.
- No scripts/, validator, or verifier files touched.
</verification>

<success_criteria>
- TAX-01: /excalidraw presents exactly the four families as the top-level AskUserQuestion.
- TAX-02: multi-type families trigger a second type sub-pick within the 4-option cap, with a documented plain-text numbered-menu fallback past four types; single-type families skip the sub-pick.
- TAX-03: resolved family+type drives dispatch via the single authoritative resolver table in diagram-types/README.md.
- INT-01: the specialist reads diagram-types/<type>.md first and consults the composed sub-patterns without changing the render-verify-fix loop, validator, or verifier.
- Smoke test: the spine is proven end-to-end against one existing type (Tech Architecture).
</success_criteria>

<output>
Create .planning/phases/04-taxonomy-spine-star-resolution/04-03-SUMMARY.md when done.
</output>
