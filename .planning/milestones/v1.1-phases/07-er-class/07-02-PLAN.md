---
phase: 07-er-class
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/er.md
  - .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw
  - .claude/agents/excalidraw/examples/er_retail_orders.png
autonomous: false
requirements: [DM-02]
user_setup: []
must_haves:
  truths:
    - "diagram-types/er.md exists, composes the locked compartmented-box + the new relationship-endpoint primitive + notation-conventions by @-reference, and re-derives no geometry of its own"
    - "An ER request (entity boxes with PK/FK row prefixes, committed cardinality notation) yields a rendered PNG that passes the full validate→render→verify loop (DM-02)"
    - "Each relationship's cardinality (one=bar, zero/optional=dot/ellipse, many=textual 0..*/1..*) is visually distinguishable from a plain association, and no illegal arrowhead token appears in the authored JSON"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/er.md"
      provides: "ER diagram-type recipe (entity boxes with PK/FK rows, committed cardinality)"
      contains: "relationship-endpoint"
      min_lines: 50
    - path: ".claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw"
      provides: "Canonical ER example source JSON (compartmented entity boxes + bound elbow connectors + cardinality)"
      contains: "groupIds"
    - path: ".claude/agents/excalidraw/examples/er_retail_orders.png"
      provides: "Rendered canonical ER PNG sibling that passes the full loop"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/er.md"
      to: ".claude/agents/excalidraw/kb/relationship-endpoint.md"
      via: "@-reference to the shared endpoint-glyph primitive"
      pattern: "relationship-endpoint"
    - from: ".claude/agents/excalidraw/diagram-types/er.md"
      to: ".claude/agents/excalidraw/diagram-types/compartmented-box.md"
      via: "@-reference to the locked box offsets (used verbatim)"
      pattern: "compartmented-box"
---

<objective>
Author the `diagram-types/er.md` ER diagram-type recipe and its canonical example pair
(`er_retail_orders.excalidraw` + rendered `er_retail_orders.png`), satisfying DM-02. The
recipe composes the locked compartmented-box geometry, the new shared
`kb/relationship-endpoint.md` glyph primitive (from Plan 01), and the locked
`notation-conventions.md` cardinality table — all by `@`-reference, re-deriving no geometry
of its own. The example must pass the full validate→render→verify loop.

Purpose: ER is the first of the two compartmented-relationship types this phase ships. It
proves the shared endpoint-glyph primitive works end-to-end on entity boxes with PK/FK rows
and committed cardinality notation (one=bar, zero/optional=dot/ellipse, many=textual
multiplicity), with each cardinality visually distinguishable from a plain association.

Output: `diagram-types/er.md` recipe; `examples_excalidraw/er_retail_orders.excalidraw`
source; `examples/er_retail_orders.png` rendered sibling that passes the loop. (The resolver
row in README.md is wired in Plan 04, AFTER both examples pass — EX-01/EX-03 / Pitfall 6.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-er-class/07-RESEARCH.md
@.planning/phases/07-er-class/07-PATTERNS.md
@.planning/phases/07-er-class/07-01-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (excluded from drift verification — they are new):
- New file: `.claude/agents/excalidraw/diagram-types/er.md` (ER type recipe)
- New file: `.claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw` (canonical source)
- New file: `.claude/agents/excalidraw/examples/er_retail_orders.png` (rendered sibling)
- New PNG sibling may produce `er_retail_orders.verifier-report.json` (verifier artifact)

Depends on these symbols produced earlier in this phase (Plan 01 — already authored):
- `.claude/agents/excalidraw/kb/relationship-endpoint.md` — the shared endpoint-glyph primitive `er.md` @-references
- (optional, decision-gated) arrowhead-enum deny-list in `excalidraw_validator.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author diagram-types/er.md recipe (compose primitives by @-reference)</name>
  <files>.claude/agents/excalidraw/diagram-types/er.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/snowflake-schema.md
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md
    - .claude/agents/excalidraw/diagram-types/notation-conventions.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
    - .claude/agents/excalidraw/diagram-types/README.md
    - .planning/phases/07-er-class/07-RESEARCH.md
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>Create `diagram-types/er.md` mirroring `snowflake-schema.md`'s section structure: a Layer-header one-liner, a "Purpose" section, a "How to draw it" section, a "Composes (primitive layer)" section, and a "Ground truth" section. The Layer-header line MUST be the exact analog one-liner adapted for ER: "> Layer: TYPE recipe. Composes primitives from [`../kb/`](../kb/README.md) and the shared [`compartmented-box.md`](./compartmented-box.md) construction; does not re-derive their geometry. Indexed in [`README.md`](./README.md)." In "Purpose", state that an ER (entity-relationship) diagram models entities, their attributes (with PK/FK marked rows), and the cardinality of relationships between them; describe when to reach for it. In "How to draw it": (1) state entity boxes ARE the compartmented-box construction with 2 compartments (title + attribute rows), and that ALL parametric offsets (header 40, row pitch 20, left-pad 12, fontSize 16, box-width rule `len*0.6*16` rounded up to 20-grid) are reused VERBATIM from `@./compartmented-box.md` — do NOT re-derive them. (2) PK/FK rows are monospace text-prefix rows authored as `PK  id` and `FK  region_id` (two-space prefix, never a key emoji per Pitfall 5) — one `text` element per row, never a multi-line block. (3) Cardinality uses ONLY the committed encodings from `@./notation-conventions.md`: "one"=`endArrowhead:"bar"`, "zero/optional"=`endArrowhead:"dot"` (or a small composed ellipse glyph), "many"=a free textual multiplicity label `0..*`/`1..*` placed OUTSIDE every box bbox (Pitfall 3) — the grouped 3-line crow's-foot glyph is the alternative and is NOT used. (4) Include a binding-rules block stating: every connector is `elbowed:true`, `roundness:null`, with orthogonal points; the ONLY legal arrowhead tokens are `arrow|bar|dot|triangle|null` (crowsfoot/diamond/hollow render silently as bare lines); every arrow's start and end points must land within 8px of a box RECTANGLE border via `startBinding`/`endBinding` to the box rectangle ids — never bound to row texts or line dividers. (5) State that any composed glyph geometry (ellipse size, multiplicity-label offset) comes from `@../kb/relationship-endpoint.md` and is NOT re-derived here. In "Composes (primitive layer)", provide a bulleted `@`-reference list naming AT LEAST: `@./compartmented-box.md` (finalized box offsets used verbatim), `@../kb/relationship-endpoint.md` (endpoint-glyph geometry: bar/dot ends, ellipse glyph, textual multiplicity), and `@./notation-conventions.md` (legal-encoding cardinality table). In "Ground truth", point at `../examples/er_retail_orders.png` and describe what to imitate (entity boxes with PK/FK prefix rows, distinguishable cardinality endpoints). Do NOT place any pixel/coordinate math in this file — geometry lives only in the primitive layer.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && grep -q "Layer: TYPE recipe" .claude/agents/excalidraw/diagram-types/er.md && grep -q "relationship-endpoint" .claude/agents/excalidraw/diagram-types/er.md && grep -q "compartmented-box" .claude/agents/excalidraw/diagram-types/er.md && grep -q "notation-conventions" .claude/agents/excalidraw/diagram-types/er.md && grep -Eq 'PK' .claude/agents/excalidraw/diagram-types/er.md && grep -Eq 'bar' .claude/agents/excalidraw/diagram-types/er.md && grep -q "er_retail_orders.png" .claude/agents/excalidraw/diagram-types/er.md && test $(grep -vc '^[[:space:]]*$' .claude/agents/excalidraw/diagram-types/er.md) -ge 50 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `diagram-types/er.md` exists with the Layer-header one-liner, Purpose, How to draw it, Composes (primitive layer), and Ground truth sections.
    - The Composes section `@`-references all three of: `@./compartmented-box.md`, `@../kb/relationship-endpoint.md`, `@./notation-conventions.md`.
    - The recipe documents PK/FK monospace text-prefix rows (`PK  id` / `FK  ...`) and the committed cardinality encodings: one=`bar`, zero/optional=`dot`/ellipse, many=textual `0..*`/`1..*` outside box bbox.
    - The recipe states the five legal arrowhead tokens and names crowsfoot/diamond/hollow as illegal (silent bare line), and the bind-to-box-rectangle rule.
    - The file contains NO coordinate/pixel math (offsets are referenced from compartmented-box.md, glyph geometry from relationship-endpoint.md) and has at least 50 non-blank lines.
  </acceptance_criteria>
  <done>`er.md` exists, composes the locked primitives by @-reference exactly like snowflake-schema.md, and carries no re-derived geometry — ready to drive ER authoring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the canonical er_retail_orders.excalidraw and render it through the full loop</name>
  <files>.claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw, .claude/agents/excalidraw/examples/er_retail_orders.png</files>
  <read_first>
    - .claude/agents/excalidraw/examples_excalidraw/snowflake_schema.excalidraw
    - .claude/agents/excalidraw/diagram-types/er.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
    - .claude/agents/excalidraw/diagram-types/compartmented-box.md
    - .claude/agents/excalidraw/scripts/render/validate_and_render.sh
    - .planning/phases/07-er-class/07-PATTERNS.md
  </read_first>
  <action>Author `examples_excalidraw/er_retail_orders.excalidraw` by copying the top-level envelope from `snowflake_schema.excalidraw` (keys `type, version, source, elements, appState, files`) and building a small retail ER model with 3 entity boxes — `customer`, `order`, `product` — connected by two relationships (customer 1—* order; order *—* product, or a join entity if preferred). Each entity box is the locked compartmented-box construction: a sharp `rectangle` (`roundness:null`, `roughness:0`, `backgroundColor:"transparent"`) with a full-width header `line` divider (`x==box.x`, `points=[[0,0],[width,0]]`), one title `text` bound via `containerId`, and N free-floating monospace (`fontFamily:3`, `fontSize:16`) per-row texts — one `text` per row, NEVER a multi-line block. Mark key rows with `PK  ` / `FK  ` two-space prefixes (e.g. `PK  customer_id`, `FK  customer_id`). Size each box width to the longest row per the locked rule (`len*0.6*16` rounded UP to 20-grid) so `text_overflow_static` passes. Every box shares ONE `groupIds` id (frame + divider + title + rows). Connectors are bound elbow `arrow`s (`elbowed:true`, `roundness:null`, orthogonal points) with `startBinding`/`endBinding` to the box RECTANGLE ids (gap 4) — never to row texts or dividers. Encode cardinality per `er.md` / `notation-conventions.md`: use `endArrowhead:"bar"` for a "one" end and a free `text:"0..*"` (or `"1..*"`) placed OUTSIDE every box bbox for a "many" end; optionally a `dot`/ellipse for an "optional" end. Use ONLY legal arrowhead tokens (`arrow|bar|dot|triangle|null`). Keep all coordinates on the 20-grid (left-pad 12 is the one exception). Do NOT imitate the legacy `star_schema.excalidraw` (0 groupIds, unbound arrows, soft roundness). Then render and verify through the FULL loop: run `validate_and_render.sh` on the file to produce the sibling `er_retail_orders.png`. Fix any structural verifier findings (overflow, unanchored-arrow, not-elbow, raw-emoji, non-monospace) by mutating the JSON and re-rendering, up to the loop's 3-iteration cap. The PNG must be produced and the structural verifier must return no errors before this task is done; the visual/EX-03 human-verify gate is Task 3.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && python3 .claude/agents/excalidraw/scripts/render/excalidraw_validator.py .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw && python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw && test -f .claude/agents/excalidraw/examples/er_retail_orders.png && python3 -c "import json; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw')); heads=[el.get(k) for el in d['elements'] for k in ('startArrowhead','endArrowhead') if k in el]; legal={'arrow','bar','dot','triangle',None}; bad=[h for h in heads if h not in legal]; print('BAD',bad) or exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `examples_excalidraw/er_retail_orders.excalidraw` exists with at least 3 grouped compartmented entity boxes (each: 1 rectangle + ≥1 line divider + bound title text + per-row monospace texts, all sharing one groupIds id).
    - At least one row text uses a `PK  ` prefix and at least one uses an `FK  ` prefix (monospace, never a key emoji).
    - The validator (`excalidraw_validator.py`) exits 0 and the structural verifier (`verifier_structural.py`) reports no errors.
    - Every `startArrowhead`/`endArrowhead` value in the JSON is in `{arrow,bar,dot,triangle,null}` (no illegal token); at least one connector uses `endArrowhead:"bar"` (cardinality "one") and at least one free text matching `0..*` or `1..*` exists for a "many" end.
    - The sibling `examples/er_retail_orders.png` exists (rendered through `validate_and_render.sh`).
  </acceptance_criteria>
  <done>The canonical ER source passes the validator + structural verifier with only legal arrowheads and PK/FK prefix rows, and the rendered PNG sibling exists — ready for the visual EX-03 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: EX-03 visual verification of the ER canonical example</name>
  <what-built>A canonical ER example `examples_excalidraw/er_retail_orders.excalidraw` and its rendered sibling `examples/er_retail_orders.png`: three compartmented entity boxes (customer / order / product) with PK/FK prefix rows, connected by bound elbow connectors whose cardinality is encoded with the committed legal tokens (bar for "one", textual `0..*`/`1..*` for "many", optionally dot/ellipse for "optional"). The validator and structural verifier already pass with only legal arrowhead tokens (Task 2).</what-built>
  <how-to-verify>
    1. Open `.claude/agents/excalidraw/examples/er_retail_orders.png`.
    2. Confirm each entity box renders as a sharp compartmented box: a title compartment over an attribute compartment, full-width divider, rows in monospace, no text overflowing the box border.
    3. Confirm PK/FK prefixes render as plain text (`PK`, `FK`) — NOT tofu/missing-glyph boxes and NOT key emoji.
    4. Confirm each relationship's cardinality is VISUALLY DISTINGUISHABLE from a plain association: the "one" end shows a bar, the "many" end shows a readable `0..*`/`1..*` label placed clear of every box, and no connector that should carry cardinality renders as an undecorated plain line.
    5. Confirm no connector renders as a bare line where a cardinality glyph was intended (the silent-illegal-token failure mode).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the PNG shows compartmented entity boxes with non-overflowing PK/FK monospace rows.
    - Operator confirms every relationship's cardinality is distinguishable from a plain association (DM-02 / SC-2).
    - Operator confirms no intended-cardinality connector renders as a plain bare line and no tofu glyphs appear.
  </acceptance_criteria>
  <resume-signal>Type "approved" to satisfy the ER EX-03 gate, or describe the visual defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- `excalidraw_validator.py` and `verifier_structural.py` both return clean on `er_retail_orders.excalidraw` (structural automated half).
- All arrowhead tokens in the ER JSON are within the legal set `{arrow,bar,dot,triangle,null}`.
- `er.md` composes `@./compartmented-box.md`, `@../kb/relationship-endpoint.md`, and `@./notation-conventions.md` by reference and re-derives no geometry.
- Operator approves the rendered PNG (visual EX-03 half) before Plan 04 wires the resolver row.
</verification>

<success_criteria>
- DM-02 foundation: `diagram-types/er.md` exists and an ER request (entity boxes with PK/FK rows, committed cardinality) yields a PNG that passes the full loop, cardinality distinguishable from a plain association.
- The canonical ER example pair exists and passes the structural + visual loop (EX-01/EX-03 for ER), using only legal arrowhead tokens (SC-1).
</success_criteria>

<output>
Create `.planning/phases/07-er-class/07-02-SUMMARY.md` when done, recording the example
subject (`er_retail_orders`), the structural-verifier result, and the EX-03 visual approval.
The resolver row + REQUIREMENTS DM-02 tick are wired in Plan 04.
</output>
