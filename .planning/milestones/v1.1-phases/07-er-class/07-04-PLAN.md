---
phase: 07-er-class
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - .claude/agents/excalidraw/diagram-types/README.md
  - .claude/agents/excalidraw/kb/relationship-endpoint.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DM-02, UML-02]
user_setup: []
must_haves:
  truths:
    - "The diagram-types/README.md resolver table has real (non-placeholder) er and class rows naming the relationship-endpoint sub-pattern and the real example PNG paths, listed in the Wired rows note"
    - "kb/relationship-endpoint.md's 'See in examples' section points at the real canonical PNGs (er_retail_orders.png, class_order_domain.png) instead of placeholders"
    - "The full end-to-end resolve→author→validate→render→verify loop passes for both er and class: each canonical example validates and structurally verifies clean, and both EX-03 visual gates were approved in Plans 02/03"
  artifacts:
    - path: ".claude/agents/excalidraw/diagram-types/README.md"
      provides: "Resolver table with wired er + class rows and updated Wired-rows note"
      contains: "er_retail_orders.png"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DM-02 and UML-02 marked complete in the requirements list and traceability table"
      contains: "DM-02"
  key_links:
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/diagram-types/er.md"
      via: "resolver row naming the recipe file + example PNG"
      pattern: "er\\.md"
    - from: ".claude/agents/excalidraw/diagram-types/README.md"
      to: ".claude/agents/excalidraw/diagram-types/class.md"
      via: "resolver row naming the recipe file + example PNG"
      pattern: "class\\.md"
---

<objective>
Close out Phase 7: wire the `er` and `class` resolver rows in `diagram-types/README.md`
(now that both canonical examples pass the full loop), fill the "See in examples" section
of `kb/relationship-endpoint.md` with the real canonical PNG names, mark DM-02 and UML-02
complete in REQUIREMENTS.md, and verify the full end-to-end
resolve→author→validate→render→verify loop passes for both diagram types.

Purpose: EX-01/EX-03 require that a resolver row is wired ONLY after its canonical example
passes the full loop (Pitfall 6 — never index broken ground truth). Plans 02 and 03 produced
and verified the two example pairs (structural + EX-03 visual approval); this plan performs
the indexing + bidirectional-link + requirements bookkeeping that those gates unblock, and
confirms nothing regressed.

Output: two wired resolver rows; updated `relationship-endpoint.md` back-link to the real
examples; DM-02 + UML-02 ticked in REQUIREMENTS.md; a verification record that both examples
still pass the loop and both EX-03 gates were approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-er-class/07-RESEARCH.md
@.planning/phases/07-er-class/07-PATTERNS.md
@.planning/phases/07-er-class/07-02-PLAN.md
@.planning/phases/07-er-class/07-03-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols/files MODIFIED by THIS plan (the new recipe/example files were created in Plans 02/03):
- EDIT: `.claude/agents/excalidraw/diagram-types/README.md` — replace the `er` and `class` `_(planned — Phase 7)_` placeholder cells with real recipe-file/sub-pattern/example-PNG values; add both to the "Wired rows" note paragraph
- EDIT: `.claude/agents/excalidraw/kb/relationship-endpoint.md` — fill "See in examples" with `../examples/er_retail_orders.png` and `../examples/class_order_domain.png`
- EDIT: `.planning/REQUIREMENTS.md` — tick DM-02 and UML-02 in the DM/UML lists and flip both rows to Complete in the traceability table

Consumes (already authored, must exist and pass before this plan runs):
- `diagram-types/er.md` + `examples_excalidraw/er_retail_orders.excalidraw` + `examples/er_retail_orders.png` (Plan 02, EX-03 approved)
- `diagram-types/class.md` + `examples_excalidraw/class_order_domain.excalidraw` + `examples/class_order_domain.png` (Plan 03, EX-03 approved)
- `kb/relationship-endpoint.md` (Plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-wire gate — confirm both canonical examples pass the loop before indexing</name>
  <files>(read-only verification; no file writes)</files>
  <read_first>
    - .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw
    - .claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw
    - .planning/phases/07-er-class/07-02-PLAN.md
    - .planning/phases/07-er-class/07-03-PLAN.md
  </read_first>
  <action>Before touching the resolver (Pitfall 6 — never index broken ground truth), re-confirm both canonical examples still pass the structural half of the loop and that both rendered PNG siblings exist. Run the validator and structural verifier on BOTH `er_retail_orders.excalidraw` and `class_order_domain.excalidraw`; assert both sibling PNGs (`examples/er_retail_orders.png`, `examples/class_order_domain.png`) are present; re-confirm every arrowhead token in both JSON files is within the legal set `{arrow,bar,dot,triangle,null}`. Also confirm the two EX-03 visual gates were approved by reading the Plan 02 and Plan 03 SUMMARY files (`07-02-SUMMARY.md`, `07-03-SUMMARY.md`) for the recorded "approved" EX-03 outcome — if either SUMMARY is missing the EX-03 approval, STOP and do NOT wire the resolver rows (the gate is not satisfied). Record the four-way check (validator, structural verifier, legal-arrowheads, EX-03 approvals) in this plan's SUMMARY. If any check fails, do not proceed to Tasks 2-4.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && for f in er_retail_orders class_order_domain; do python3 .claude/agents/excalidraw/scripts/render/excalidraw_validator.py .claude/agents/excalidraw/examples_excalidraw/$f.excalidraw || exit 1; python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/$f.excalidraw || exit 1; test -f .claude/agents/excalidraw/examples/$f.png || exit 1; python3 -c "import json,sys; d=json.load(open('.claude/agents/excalidraw/examples_excalidraw/$f.excalidraw')); legal={'arrow','bar','dot','triangle',None}; bad=[el.get(k) for el in d['elements'] for k in ('startArrowhead','endArrowhead') if k in el and el.get(k) not in legal]; sys.exit(1 if bad else 0)" || exit 1; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both `er_retail_orders.excalidraw` and `class_order_domain.excalidraw` pass the validator (exit 0) and the structural verifier (no errors).
    - Both sibling PNGs `examples/er_retail_orders.png` and `examples/class_order_domain.png` exist.
    - Every arrowhead token in both JSON files is within `{arrow,bar,dot,triangle,null}`.
    - Both `07-02-SUMMARY.md` and `07-03-SUMMARY.md` record an EX-03 visual "approved" outcome; if either is missing, the plan halts before wiring.
  </acceptance_criteria>
  <done>Both canonical examples are confirmed loop-clean and both EX-03 gates approved — the resolver rows are safe to wire (Pitfall 6 satisfied).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the er + class resolver rows and fill the relationship-endpoint back-link</name>
  <files>.claude/agents/excalidraw/diagram-types/README.md, .claude/agents/excalidraw/kb/relationship-endpoint.md</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/README.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
    - .claude/agents/excalidraw/diagram-types/er.md
    - .claude/agents/excalidraw/diagram-types/class.md
  </read_first>
  <action>In `diagram-types/README.md`, edit the two placeholder rows in the resolver table. (1) The `er` row (currently `| Data Modeling | er | `er.md` _(planned — Phase 7)_ | evidence-card, group-container, tree-hierarchy | _(planned)_ |`): replace `er.md` _(planned — Phase 7)_` with plain `er.md`; set the "Composes (kb sub-patterns)" cell to the real sub-patterns the recipe actually uses INCLUDING `relationship-endpoint` (e.g. `compartmented-box, relationship-endpoint, evidence-card, group-container`); set the "Example PNG" cell to `../examples/er_retail_orders.png`. (2) The `class` row (currently `| UML | class | `class.md` _(planned — Phase 7)_ | tree-hierarchy, group-container, evidence-card (+ relationship-glyph) | _(planned)_ |`): replace `class.md` _(planned — Phase 7)_` with plain `class.md`; set the Composes cell to the real sub-patterns INCLUDING `relationship-endpoint` (e.g. `compartmented-box, relationship-endpoint, group-container`); set the Example PNG cell to `../examples/class_order_domain.png`. (3) In the "> Wired rows:" note paragraph (currently ending with snowflake-schema), append `er` and `class` as wired — naming the canonical examples `er_retail_orders.excalidraw` and `class_order_domain.excalidraw` and stating each passes the full validate→render→verify loop, consistent with how the prior wired rows are described. Do NOT change any other row or the two-layer/legacy sections. Then, in `kb/relationship-endpoint.md`, replace the "See in examples" placeholders (left by Plan 01 pointing at `../examples/er_<subject>.png` / `../examples/class_<subject>.png`) with the real paths `../examples/er_retail_orders.png` and `../examples/class_order_domain.png`, each with a one-line description of what the example demonstrates (ER cardinality endpoints; class aggregation/composition/generalization glyphs).</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && grep -q "er_retail_orders.png" .claude/agents/excalidraw/diagram-types/README.md && grep -q "class_order_domain.png" .claude/agents/excalidraw/diagram-types/README.md && ! grep -q "er.md\` _(planned" .claude/agents/excalidraw/diagram-types/README.md && ! grep -q "class.md\` _(planned" .claude/agents/excalidraw/diagram-types/README.md && grep -Eq '\| .*\| er \|.*relationship-endpoint' .claude/agents/excalidraw/diagram-types/README.md && grep -Eq '\| .*\| class \|.*relationship-endpoint' .claude/agents/excalidraw/diagram-types/README.md && grep -q "er_retail_orders.png" .claude/agents/excalidraw/kb/relationship-endpoint.md && grep -q "class_order_domain.png" .claude/agents/excalidraw/kb/relationship-endpoint.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The `er` resolver row names `er.md` (no `_(planned)_` marker), lists `relationship-endpoint` among its sub-patterns, and points its Example PNG cell at `../examples/er_retail_orders.png`.
    - The `class` resolver row names `class.md` (no `_(planned)_` marker), lists `relationship-endpoint` among its sub-patterns, and points its Example PNG cell at `../examples/class_order_domain.png`.
    - Neither `er` nor `class` retains the `_(planned — Phase 7)_` placeholder; the "Wired rows" note names both new examples as passing the full loop.
    - `kb/relationship-endpoint.md`'s "See in examples" section references both `../examples/er_retail_orders.png` and `../examples/class_order_domain.png` (placeholders replaced).
  </acceptance_criteria>
  <done>Both diagram types are wired in the single authoritative resolver table with real example PNGs, the relationship-endpoint primitive links back to the real canonical PNGs (bidirectional link closed), and no placeholder remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mark DM-02 and UML-02 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>In `.planning/REQUIREMENTS.md`, mark both phase requirements complete. (1) In the "Data Modeling Types (DM)" list, flip the DM-02 checkbox from `- [ ] **DM-02**` to `- [x] **DM-02**`. NOTE the A4/Open-Question-3 filename discrepancy: DM-02's prose says `diagram-types/er-diagram.md` but the resolver + Success Criteria + the file authored in Plan 02 use `er.md`. Resolve it by editing DM-02's text to say `diagram-types/er.md` (match the resolver and the shipped file), so REQUIREMENTS and the resolver agree. (2) In the "UML Types (UML)" list, flip the UML-02 checkbox from `- [ ] **UML-02**` to `- [x] **UML-02**`. (3) In the Traceability table, change the `| DM-02 | Phase 7 | Pending |` row to `| DM-02 | Phase 7 | Complete |` and the `| UML-02 | Phase 7 | Pending |` row to `| UML-02 | Phase 7 | Complete |`. Do not alter any other requirement's status.</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && grep -q '\- \[x\] \*\*DM-02\*\*' .planning/REQUIREMENTS.md && grep -q '\- \[x\] \*\*UML-02\*\*' .planning/REQUIREMENTS.md && grep -Eq '\| DM-02 \| Phase 7 \| Complete \|' .planning/REQUIREMENTS.md && grep -Eq '\| UML-02 \| Phase 7 \| Complete \|' .planning/REQUIREMENTS.md && grep -q 'diagram-types/er.md' .planning/REQUIREMENTS.md && ! grep -q 'er-diagram.md' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DM-02 and UML-02 are both checked `- [x]` in their respective requirement lists.
    - The Traceability table shows both DM-02 and UML-02 as `Complete` under Phase 7.
    - DM-02's prose now references `diagram-types/er.md` (the `er-diagram.md` string no longer appears anywhere), resolving the A4 filename discrepancy.
  </acceptance_criteria>
  <done>DM-02 and UML-02 are recorded complete and the ER filename discrepancy is resolved so REQUIREMENTS and the resolver table agree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final end-to-end regression — both diagram types pass the full loop and the KB layers are consistent</name>
  <files>(read-only verification; no file writes)</files>
  <read_first>
    - .claude/agents/excalidraw/diagram-types/README.md
    - .claude/agents/excalidraw/diagram-types/er.md
    - .claude/agents/excalidraw/diagram-types/class.md
    - .claude/agents/excalidraw/kb/relationship-endpoint.md
  </read_first>
  <action>Perform a final regression pass confirming the phase deliverables are mutually consistent and nothing regressed. (1) Re-run the validator + structural verifier on both `er_retail_orders.excalidraw` and `class_order_domain.excalidraw` — both must still be clean. (2) Confirm the two-layer cross-reference is intact in BOTH directions: `er.md` and `class.md` each `@`-reference `../kb/relationship-endpoint.md`, and `kb/relationship-endpoint.md` carries the `> Used by types: er, class` back-ref — so the layers cannot silently drift. (3) Confirm the resolver rows resolve to files that actually exist on disk: `er.md`, `class.md`, `er_retail_orders.png`, `class_order_domain.png`. Record the end-to-end result (both types loop-clean; bidirectional KB link intact; resolver targets exist) in the SUMMARY. This is the phase-level exit gate for SC-4 ("a canonical example pair exists and is indexed for both ER and class, each passing the full loop").</action>
  <verify>
    <automated>cd /home/linuxzinho/coding/excaildraw-claude && python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/er_retail_orders.excalidraw && python3 .claude/agents/excalidraw/scripts/verifier/verifier_structural.py .claude/agents/excalidraw/examples_excalidraw/class_order_domain.excalidraw && grep -q "relationship-endpoint" .claude/agents/excalidraw/diagram-types/er.md && grep -q "relationship-endpoint" .claude/agents/excalidraw/diagram-types/class.md && grep -q "Used by types: er, class" .claude/agents/excalidraw/kb/relationship-endpoint.md && test -f .claude/agents/excalidraw/diagram-types/er.md && test -f .claude/agents/excalidraw/diagram-types/class.md && test -f .claude/agents/excalidraw/examples/er_retail_orders.png && test -f .claude/agents/excalidraw/examples/class_order_domain.png && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both canonical example JSON files still pass the structural verifier (no regression).
    - `er.md` and `class.md` both `@`-reference `../kb/relationship-endpoint.md`, and `relationship-endpoint.md` carries `> Used by types: er, class` (bidirectional link intact).
    - Every resolver target file exists on disk: `er.md`, `class.md`, `er_retail_orders.png`, `class_order_domain.png`.
  </acceptance_criteria>
  <done>The full end-to-end loop passes for both ER and class, the two KB layers cross-reference consistently, and every wired resolver target exists — Phase 7 exit criteria (SC-1..SC-4) satisfied.</done>
</task>

</tasks>

<verification>
- Pre-wire gate: both canonical examples pass validator + structural verifier and both EX-03 gates were approved before any resolver row is wired (Pitfall 6).
- Resolver: `er` and `class` rows are wired with `relationship-endpoint` in their sub-patterns and the real example PNG paths; no `_(planned)_` placeholder remains for either.
- KB link: `relationship-endpoint.md` "See in examples" points at the real PNGs and carries `> Used by types: er, class`; `er.md`/`class.md` link down to it (bidirectional, no drift).
- Requirements: DM-02 + UML-02 checked and Complete in the traceability table; the `er-diagram.md` vs `er.md` discrepancy resolved.
</verification>

<success_criteria>
- SC-4: a canonical example pair exists and is indexed in the resolver for both ER and class, each passing the full validate→render→verify loop.
- DM-02 and UML-02 are recorded complete; the two-layer KB stays consistent (type files link down to relationship-endpoint, primitive links back up).
- The full end-to-end resolve→author→validate→render→verify loop is confirmed passing for both diagram types.
</success_criteria>

<output>
Create `.planning/phases/07-er-class/07-04-SUMMARY.md` when done, recording the pre-wire
gate result, the wired resolver rows, the DM-02/UML-02 completion + filename-discrepancy
resolution, and the final end-to-end regression outcome for both diagram types.
</output>
