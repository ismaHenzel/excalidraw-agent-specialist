# emoji-as-icon — Operator Request

> Copy this entire block (everything below the horizontal rule) into the Claude Code session.
> Replace `<ABSOLUTE_PATH_TO_WORKING_COPY>` with the absolute path where the diagram should be saved
> (e.g. `<repo>/fixtures/e2e/working/emoji-as-icon.excalidraw`). No seed file is required —
> the specialist authors this diagram from scratch.

---

Create a small diagram showing a single rounded rectangle labelled `deploy-success` with a green checkmark icon to the left of the label. Save it to `<ABSOLUTE_PATH_TO_WORKING_COPY>`.
