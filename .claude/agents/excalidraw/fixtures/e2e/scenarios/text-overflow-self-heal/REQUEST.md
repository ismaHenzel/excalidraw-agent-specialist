# text-overflow-self-heal — Operator Request

> Copy this entire block (everything below the horizontal rule) into the Claude Code session
> after copying the seed to `working/text-overflow-self-heal.excalidraw`. Replace
> `<ABSOLUTE_PATH_TO_WORKING_COPY>` with the absolute path to that file.

---

I have a diagram at `<ABSOLUTE_PATH_TO_WORKING_COPY>` where the label `Databricks Workspace Setup` is being cut off — the text visibly overflows the rectangle it sits inside. Please fix it so the label fits cleanly inside its container, and confirm the result renders correctly.
